# 本地开发版与它的历次备份。晋级靠整份复制,这些不进仓库。
# 用通配是因为旧版按「重命名不删除」的规矩留档,例如 80fen-dev-v0.7.0.bak.html。
80fen-dev*.html

# 一次性的补丁、临时输出
scratchpad/

# 教学版走查脚本 --shots 的输出
learn/test/shots/

# 走查脚本早期版本往根目录扔的截图,别再让它们进来
[a-z]-*.png
