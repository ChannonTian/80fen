# 80分 启发式 AI —— 设计极简笔记

## 架构:三层单向依赖

```
界面(UI) ──调用──► AI ──调用──► 引擎(纯函数)
```

引擎只回答"什么是合法的"(`classify` / `isLegalFollow` / `resolveTrick`);AI 只回答"合法之中选哪个";界面只负责画和收输入。AI 绝不自己重新实现规则——所有候选都经 `isLegalFollow` 验证,不合法直接作废回退。这保证 AI 永远不会出违规牌,也让规则改动(如参数开关)自动被 AI 遵守。

## 核心思路:候选生成 → 打分 → 择优,每一步带理由

不做搜索(不穷举对手可能手牌),而是模仿人的决策结构:

**1. 记牌(makeMemory)。** 信息 = 自己手牌 + 全部公开出牌 + (庄家还记得底牌)。用"每种牌全场只有2张"倒推出**在外未见牌**的字典。由此可判断:某张/某对是否"在外已无更大"(钢板,`isBossPlay`)。不做概率推断(谁断门、牌在谁手里),这是最大的已知短板。

**2. 候选生成(构造式,不枚举)。** 跟牌的合法组合数是指数级的,不能穷举。改为按意图构造少量候选:
- `minWinFollow`:最省的吃法——本门压过或断门用主敲脱,结构必须匹配,挑刚好能赢的最小牌;
- `buildFollow('dump')`:贴分——义务(跟门、有对必对、拖拉机)填完后,自由位优先塞 5/10/K;
- `buildFollow('cheap')`:跟小——自由位挑最小且无分的牌。

**3. 打分(手写权重)。** 每个候选算一个分,规则如:队友稳赢(钢板或我是末手)→贴分得高分,确定性系数 ×1/0.9/0.4;对手暂大且台面有分→吃的价值 = 30+台面分×1.5+末手加成−所耗大牌成本;都不行→跟小。领出:钢板优先收墩,拖拉机压人,否则小牌探路,惩罚白送分。

**4. 理由字符串。** 每个候选自带一句中文理由("队友稳赢,贴分")。选中后存入局面——这不是日志装饰,而是教练模式的接口:教练 = 把同一套 AI 跑在玩家座位上,展示它的首选和理由,再对比玩家实际出牌。

## 验证方法

规则单元测试(~80条)+ 场景测试(构造牌面断言 AI 行为:该用Q吃就不动A、队友大就贴10分)+ **对照实验**:同样 120 副牌,启发式防守 vs 随机防守,断言平均抓分显著更高。最后 300 局全 AI 自对弈冒烟,保证零违规零崩溃。

## 已知短板(按痛感排序)

无跨墩规划:不知道"留大王护底""诱敌消耗主牌"这类多步计划,单张钢板大王会被"先收一墩"规则早早打掉(现已加惩罚项缓解,但治标);无对手建模:不推断谁断门、剩余大牌在敌在友;甩牌从不主动使用;埋底与亮主决策仍是简单规则。改进方向依次是:墩间状态机(开局/中盘/收官分权重)、断门推断、蒙特卡洛发牌采样评估。

## 经验教训

构造式候选生成远比枚举可靠且够用;"理由先行"的设计让教练模式几乎零成本;权重全部手写意味着可解释、可调,但也意味着每个怪异行为(如乱打大王)都对应一条可指认的评分规则——调 AI = 调几个数字,而不是黑盒。
