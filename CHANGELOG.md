# 更新日志

每版**结论**在这里。**v0.7.6 起,推演过程、实验数据、被否掉的尝试也直接写在这里**;
v0.7.5 及以前那部分在 **[`NOTES/ai-journal.md`](NOTES/ai-journal.md)**(同名章节可直接对照)。

当前:**正式版 v0.7.12** / **测试版 v0.7.13**。v0.7.9~v0.7.12 已于 2026-08-28 晋级合入正式版。
`contest/` 与 `AI-API.md`(AI 比赛)不进 build,单独记在下面前两条,不占版本号。

---

## 版本总表

| 版本 | 一句话 | 落在 |
|---|---|---|
| **v0.7.13** | 托管按钮从设置里搬到牌桌上(庄家区);竖屏按钮改按文案自适应;两份规则书/设计文档补齐「照着能做出同样东西」的那一半 | 测试版 |
| **v0.7.12** | 需求表里的托管 / 最后一手自动出;顺带补上「结算之后还有定时器在飞」的洞 | **两版** |
| **v0.7.11** | 竖屏状态栏被空转的网格余量撑高;记录区小牌加下限;多张垫/贴改逐张取(报障「AI 在凑对子」) | **两版** |
| **v0.7.10** | 术语表并回单文件 + 英语档;界面五项;**同桌对照的 SE 一直高估 2~3 倍**(配对口径) | **两版** |
| **v0.7.9** | 三条报障根因:贴分的存活率口径、稳拿时兑现主分牌、甩牌只能甩整门(+0.88 ±0.28) | **两版** |
| **v0.7.8** | 「差一张就是钢板」的期权价值 —— AA 打完不该先打 Q,四个版本以来第一条量得到的 AI 收益 | **两版** |
| **v0.7.7** | 速通模式;记录页签三合一 + 跨局复盘;教练理由「文不对题」的体检与修复 | **两版** |
| **v0.7.6** | 三条问题修复单独摘进正式版(关卡口径、竖屏发牌遮挡、甩牌诊断),不带 AI 改动 | **正式版** |
| **v0.7.5** | 定点反事实回放:病灶是「主牌作为毙牌机会」的机会成本,不是任何一条规则 | 测试版 |
| v0.7.4 | 「他肯不肯花」接进 `pSurvive`;自对弈与固定参照第一次给出相反答案 | 测试版 |
| v0.7.3 | 毙牌门槛从「他有没有更大的主」换成「他肯不肯花」(`oppSpendCeil`) | 测试版 |
| v0.7.2 | 送分:队友「压回来」要分清是被大牌压还是被毙;埋分影子成本被证伪 | 测试版 |
| v0.7.1 | 关卡口径收紧为「坐庄守住」;竖屏出牌区被覆盖的下限;甩牌拆解诊断 | 测试版 |
| **v0.7.0** | 收官蒙特卡洛 —— 最后 5 墩用推演代替启发式 | **正式版** |
| v0.6.1 | 两条结构性重构做了、都没挣到分;记下为什么值得留开关 | — |
| v0.6.0 | 领出侧牌权概率改问「本队」;最后一墩四个部件补齐;三个标定常数重扫 | — |
| v0.5.9 | 调王的诊断与三次失败的尝试 | — |
| v0.5.8 | 九条实战反馈的根因修复(单条收益最大的一版) | — |
| v0.5.7 | 对子被压的概率按组合算(`pBeaterIn`);甩牌漏毙结案 | — |
| v0.5.6 | 持有区间的下界;调主成为独立候选;副色探路降级 | — |
| v0.5.5 | 理论最大对子(`pPairAbove`);甩牌不毙的复查 | — |
| v0.5.4 | 不埋副 A;断门 + 台面有分默认就毙 | — |
| v0.5.3 | 调王轮三家不接:查清了,只修得动中盘那一半 | — |
| v0.5.2 | 必打关卡只实现了一半 | — |
| v0.5.1 | 护底权重、竖屏出牌区被压塌、悬停态 | — |
| v0.5.0 | AI v4:牌权、收官底分、造反判据 | — |
| v0.4.2 | 手牌选中态、发牌期布局抖动、新开一局 | — |
| v0.4.1 | 非 AI 需求清理 | — |
| v0.4.0 | AI v3:亮主/扣底/出牌三阶段全部公式化 | — |
| v0.3.0 | 界面重做与规则新增合并进正式版 | — |
| v0.2.0 | 版本号与变更日志上线 | — |
| v0.1.0 | 第一个能打完整局的版本 | — |

---

## 比赛赛制定案(2026-08-31,不进 build)

第一版是「参赛者从基线 fork 起步、做增量改进」,现在整个转向:**参赛者拿不到我们的
引擎和 AI,照 `RULES.md` 自己写**,写歪了用罚分说话 —— 代码和策略一样计分。
排名也从「都对同一个陪练打」改成**联赛,两两都要对过局**。

### 空屋:只隔离引擎是不够的(第二次)

上一条已经修过一次「提交本身跑在宿主 realm 里」。这次发现更深的一层:参赛者和陪练
共用同一种 realm,而那个 realm 跑过块① —— 全局 `module.exports` 上就挂着整个
`ENGINE`,写一行 `globalThis.module.exports` 能把引擎连同 101 个 AI 内部函数整个
读出来。「自己照规则书写引擎」这条本来就废了。

现在参赛者走 `guestRealm`:**一行我们的代码都不加载**,只有 JS 内建和一个只能读
磁盘 .js 的极简 require,没有 `fs`/`process`/网络/npm。`selftest.js` 钉着这一条
(夹具会主动去摸,摸到就红)。

### 罚分:封在结果上,不是封在罚分上

口径是「裁判替你出了几张牌,就罚几张 × 5 分」,罚分记给对方队。

第一版没封顶,实测一个完全不合法的提交一场被罚一万五千分,把闲家得分推到极端值,
「每局净分」算出 +133 这种数(牌面上一局总共 200 分),**而且污染了跟它打过的
所有人的平均分**。第二版封罚分本身(每队每局最多 200)—— 还是没用,两队各罚 200
仍能让结算跑到 [-200, 400]。封在**结果**上才对:闲家最终得分夹回 [0, 200],
「罚到这局输光为止」。

两类不罚也不算违规:**甩牌被吃**(赌博输了,基线自己 240 场也输 9 次)、
**亮主压不过现在的**(牌是真有,只是不够大,判断失误不是 bug)。后者第一版每场
记上千次「违规」,把报表淹了。

### 自家选手要用显式白名单

判「哪些提交给我们的引擎」原本按 `contest/` 前缀,结果把
`contest/public/example/`(发给参赛者的模板)也判成自家 —— 模板在我们这儿能跑、
发出去就跑不了。改成只认包装了我们 AI 的那两个文件。

### 联赛

`contest/league.js`,多进程并行(每对一个工人,跨进程天然隔离)。规模是平方的:
17 名选手 = 136 对,单核 11 小时、8 核 1.5 小时。每对 30 副够 —— 排名靠对所有人的
累计战绩,单对噪声会被平均掉。

对局记录分两层:默认每局一行摘要(排名和申诉够用),`--log-hands` 才存每一手
(17 人全量约 2400 万手)。

### 发出去的那一份

`contest/public/` 原样复制到参赛 repo 即可:手册、`RULES.md` 副本、提交模板。
**手册里没有我们的任何代码,也没有任何关于我们这个 AI 的线索** —— 上一版写的
「五条可超越点」整个撤掉了:那泄露的是我们这个实现的弱点,等于让所有人往一处打。

模板本身是一份可跑的最小提交,实测 412 次跟牌不合法、罚 735 分、0-4 输光 ——
它跑得通,说明空屋接口对「什么都不知道」的提交是成立的。

---

## 比赛基础设施(2026-08-30,不进 build)

**三份 html 一个字节都没改** —— 所以它不占版本号。新增 `contest/` 与
[`AI-API.md`](AI-API.md):让不同的人各写一个 AI,在同一套规则和同一个裁判下比。

赛制是**考全套 + 打级**:亮主、造反、扣底、出牌全由参赛者决定,一场从 2 打到 A
(平均约 30 局)。所有人对同一个基线打同一批种子,N 个人跑 N 次而不是 N² 次。

现成的东西比预想的多。这个项目本来就按「AI 只能通过 `view` 看牌桌」分层,
AI 决策路径里**零** `Math.random`/`Date.now`(核过块① 全段)—— 可复现是白送的;
非法输出早就有引擎兜底(`isLegalFollow` 不过退回 `genFollow`,`checkThrow` 不过罚
`forced`);`test/ai-h2h.js` 已经是「按座位分派两套 AI」的对照台。缺的是接口契约、
隔离、以及一条在 node 里跑得完的**完整**对局流水线 —— 原来的 h2h 只复刻了
「发完牌硬选最强的主」,亮主和打级整个不在里面。

### 搭的过程里被自己的测试抓到的三件事

* **只隔离引擎是不够的。** 第一版把引擎放进独立 vm realm,提交本身却用宿主
  `require()` 加载 —— 于是 `ai-cheater.js` 里一句 `Array.prototype.slice = ...`
  当场把裁判打穿。现在提交的代码也在 realm 里跑(`createRealm` 自带一个极简的
  CommonJS 加载器),realm 里没有 `fs`/`process`。**这不是安全沙箱**(vm 从来不是),
  是把「不小心」和「顺手作弊」挡住。
* **违规统计只数了前 50 条样本。** `onDeal` 每局调 100 次、瞬间占满明细上限,
  之后 `discard`/`lead`/`follow` 的违规只加总数不进样本,`summary()` 就看不见了。
  改成全量按种类计数,明细另存。
* **配对口径的坑又踩了一次。** 第一版跑分器只打逐场口径:两边跑**同一个** AI 时,
  它报出 ±20.4% 的胜率 SE 和 2.07 的级数差 SE —— 而配对口径下那两个数恰好是 0。
  同一个教训 `NOTES/measurement.md` 写过一整页(逐副口径高估 2~3 倍),
  换个场景照样掉进去。已在 `DESIGN.md` §12.4 记下「新写任何对照工具,
  先想清楚配对单位是什么」。

另外还改了裁判里两处**与界面块有意不同**的地方,都是为了公平:界面的亮主窗口只给
人类(AI 发完最后一张就不再决策),裁判给所有座位绕圈;界面 `aiLowRebel` 的循环是
`s=1;s<4`,**跳过座位 0**(人类的位置),纯 AI 对局里座位 0 也得被问。

### 赛制标定

* **无偏**:两边跑同一个 AI 时,配对口径的级数差与净分**恰好 0.00**,行为不同的种子 0%。
* **判别力**(120 种子,基线 vs「基线但从不低分造反」—— 只差一个布尔值):
  级数差 **+1.56 级/场**(SE 0.35,t=4.47)、每局净分 +2.42 分/局(t=3.93)、
  胜率 59.2%(t=3.11)、符号检验 p=0.0004。三个口径里**级数差最灵敏**。
* 顺带说明低分造反是真有价值的 —— 而基线在这件事上恰恰没有判断(界面里是掷骰子的)。
* 样本量:排名用 120 种子够;但这个项目历史上量到过的 AI 改动是 +0.56~+0.88 分/局
  那个量级,**120 种子分辨不了**,得跑 500 上下(约 75 分钟)。表在 `AI-API.md` §6。

### 甩牌被罚不是违规

标定跑完发现基线自己也"违规"了(240 场 9 次,全是 `lead:甩牌被罚`)。甩牌本来就是赌
没人吃得下最小的那一组,赌输是**规则内的结果**,不是契约违规 —— 混在一起报会让参赛者
以为自己写错了。已拆成单独一项计数。

### 顺带查实的一个死开关

`AIP.gateDamp`(「对手面临必打关卡 → 损失封顶」)在产品里**从未生效**:全项目只有
`aiDiscard` 里一处读取 `opt.gateAhead`,零处传入 —— 界面块调的是
`aiDiscard(hand, trump)` 两个参数。打级赛制第一次把关卡信息放进 `view`,
基线包装也第一次真的传上了。那个参数**从没被标定过**,现在是比赛里明摆着的可捡分。

---

## v0.7.13(2026-08-28,测试版)

一条界面改动 + 两份文档的补齐。**不动 AI,引擎块与自测块与正式版逐字节相同。**

### 托管按钮搬到牌桌上(庄家区)

v0.7.12 的托管只有两个入口:设置面板里的勾选框(开一次要点三下),
以及「出牌」按钮在托管时改口成「停止托管」。报障:**太不方便**。

现在它是庄家区(扣底/看底牌那一区)最左边的一颗独立按钮,整个出牌阶段位置固定 ——
不因「看底牌」出现与否而漂移;开着时改口成「停止托管」并变成醒目色,一眼看得出它开着;
出牌阶段之外整颗收起。「出牌」按钮同时恢复成只管出牌,不再改口。

挡在前面的一直是**宽度**:底部三区的按钮原本按最长文案「亮无将(大)」定死 `min-width`
排一行,再加第四、第五颗(托管 + 看底牌)在手机竖屏必然挤爆 —— 这正是 v0.7.12
退而求其次让「出牌」改口的原因。**竖屏改成按文案自适应**(`min-width:0`)之后,
五颗一共只要 268px,320 / 360 / 393 / 844 / 1280 五种宽度实测都是单行、零横向溢出;
桌面与横屏仍是定宽那一套,排一行照旧齐整。

顺带白捡一条:发牌期的亮主按钮也松快了。同一手牌(两王 + 四门级数牌,六个可亮选项)
在 393px 竖屏,改前发牌区要占 6 行 192px,改后 4 行 126px —— 那几行本来是压在牌桌头上的。

另修一处:`endRound` 现在先 `render()` 再弹结算层,否则弹层底下还留着上一秒的托管按钮。

验证:点开/点关/局末收起各走一遍,三次整局托管的浏览器回归零控制台错误,
引擎自测 219/219,`check-sync` 全部一致。

### 文档:两份「照着能做出同样东西」的补齐

两份文档此前都只写到「能读懂」,没写到「能照着做」,差的是同一类东西 ——
那些一直只活在代码里、从没被写下来的事实。

* **`RULES.md`** 新增第三部分「实现规格」:数据模型与座位约定、一局的完整时序、
  八个判定的伪码、参数总表、**约 60 条自测向量**(取自引擎自测,跑通那张表就是同一个游戏)、
  「容易写错的十个地方」。前两部分补上十几处此前只在代码里的规则事实:座位转向、
  发牌顺序与底牌来源、「只能用已拿到的牌亮主」、发完牌后的亮主窗口、不能反自己、
  无庄局按各自级数抢亮、造反判定在定主之后、扣底无限制、平手先出者胜、
  拖拉机守末墩的 ×8、`ordIdx` 低位段与级数无关、结构匹配的准确含义、
  甩牌罚出的是 `top` 最小的那一组。
* **`DESIGN.md`** 新增第一部分「产品规格」§A–§G:定位与九条硬约束、代码地图、
  数据模型(`G`/`M`/`S`/`C`/`view`/`AIP` 的字段与生命周期)、三条接口契约 +
  五条运行期不变量、界面规格(三套布局、`--cardW` 这把唯一标尺、四个踩过的坑、
  手牌自适应算法)、六个里程碑的复刻路线图、验收清单;新增 §12「工程与验证」。
  **§0–§11 的编号与正文一字未动** —— NOTES / CHANGELOG 里几十处 `§7`、`§11.9`
  的引用全部照旧指得对。

## v0.7.12(2026-08-21,测试版)

需求表 §11.9「已研究讨论、尚未实现」里的两条界面/功能落地。都是同一件事的两半:
**轮到你、但这一手不需要你做决定时,别让你再点一次。**

### 最后一手自动出(默认开)

判据不是「最后一墩」,是「没得选」:手上剩的牌**恰好等于本墩要出的张数** ——
领出时是最后一张,跟牌时是最后一手,全出是唯一的合法解。停 450ms 让你看清出的是什么
再收墩(直接出会像卡了一下)。兜底:万一判成不合法就交还给人,不自作主张。
设置 → 节奏 →「最后一手自动出」可关。

### 托管(本局有效)

设置 → 节奏 →「托管:让 AI 替你出牌」。开着的时候:

* 牌桌上「出牌」按钮改口成**「停止托管」**,一下收回 —— 不加第四颗按钮,
  手机竖屏那一行只放得下三颗(而托管时出牌/重选/问教练本来就都用不上);
* 状态栏挂一条「托管中:AI 正在替你出牌」,免得「我的牌怎么自己出掉了」成为下一条报障;
* 顺带**快进**:四家思考时间压到 350ms、每墩留桌 700ms,并忽略「手动点击进入下一墩」
  ——否则每墩还要点一下,托管就没有意义了。

三条边界都是「不越权」:只管出牌(扣底/亮主/低分造反仍归你,非出牌阶段勾选框是灰的);
按局生效(`G.auto` 挂在 `G` 上不持久化,`endRound` 里清掉,新的一局从你自己的手开始);
**托管的墩不进教练的失误统计** —— 写 `coachNotes` 的只有 `humanPlay` 那一条路,
托管走 `playCards`,天然不计,局末复盘那句「本局没有明显失误」也跟着改口,
不把 AI 打的功劳算到你头上。

### 顺带:结算之后还在飞的定时器

`endRound` 只置 `G.ended`、不改 `G.phase`,于是 `step()` 里「phase 不是 play 就返回」
拦不住局末之后的排程 —— 空手牌上 `aiChooseLead` 取 `cards[0]` 会炸。
以前撞不到:局末最后一手一定是人点出来的,点完就没有下一次排程了;
托管/自动出把那一手也接管之后就撞得到(先在浏览器里复现,改前无、改后有)。
`step()` 现在同时看 `G.ended` 与「四家手牌是否已空」。

三次整局托管的浏览器回归(发牌→扣底→打完→结算):零控制台错误,
`coachNotes` 恒为 0、`autoTricks` 与实际墩数对得上、局末托管自动交还。

## v0.7.11(2026-08-21,测试版)

三条报障:两条手机竖屏的界面,一条 AI。

### 一出牌,顶部状态栏就被撑成两行(竖屏)

`#app` 竖屏那套网格的三行写成 `auto / 1fr / 0.6fr`。CSS 网格对**小于 1 的弹性系数**
有一条反直觉规则:`0.6fr` 只领走剩余空间的 0.6 倍,**剩下的 0.4 倍谁也不要**。
平时看不出来;一旦牌桌那行被自己的 `min-content` 撑到超过应得份额 ——
手机竖屏**一出牌**,`#msg` 多出「目前 X 最大 · 台面 N 分」这一行,正好越线 ——
这笔空转的余量就落到唯一的 `auto` 行(顶部状态栏)头上:`align-content` 默认 `stretch`,
状态栏被拉到 150px,两行字之间豁开一条,看着就是「一出牌状态栏就变两行」。

复现(Chromium,393×652,同一局面只差有没有出牌):状态栏 62px → **146px**,
右栏面板 179px → 93px。**这也解释了为什么它和屏幕高度有关**:844 高不犯,652 高必犯。

两处一起改:比例保持 1 : 0.6 不变,同乘 1/0.6 写成 `1.667fr : 1fr` —— 系数都 ≥1,
余量全部由面板吃掉;状态栏那行从 `auto` 改成 `min-content`,它不再是 `stretch` 的候选。
改后 600~844 高全程状态栏恒 62px,腾出来的空间归面板(652 高:93 → 177px)。

### 记录区的小牌看不清牌面

`--miniW` 是 `--cardW × 0.68`,这个比例照桌面端定的;竖屏 `--cardW` 只有 34~44px,
乘完剩 23~30px —— 四个角的字符互相挤住,`10♣` 尤其糊。两处改:
给 `--miniW` 一个 **32px 的绝对下限**(桌面端不生效,比例照旧),
小牌只留左上角点数 + 左下角花色(和手牌同一套语言,`#hand` 也只留左半边),
省下的宽度把字号从 0.44 提到 0.56。

### 「跟一对牌、垫其他花色废牌时,AI 还在凑对子」

**不是巧合,也不是凑对子 —— 是「一次排序取前 k 张」的副作用。**
`buildFollow` 断门那一支原来把 `discardSort(rest)` 排好序取前 k 张,而排序键是按
**整手牌**算的,取走一张之后局面就变了,一次性排序看不见:

* 副牌的 `ordIdx` 只看点数,♠9 与 ♥9 的键**完全相同**、结果里又紧挨着,于是被成对
  取走 —— 看着就是在凑对子(报障截图正是这一手);
* 贴分那一路的键只有「分值 + `globalIdx`」,既不认对子也不认门长,会把自家的对子
  整对丢出去,或者拆掉留个孤张(♠8♠8 + ♦8 里取走 ♠8 + ♦8);
* 两张分丢两门,哪一门也没短下去 —— 而**造缺**是垫牌的第一条道理。

改成 `fillPick`:逐张贪心,每取一张就按**剩下的牌**重算键。第二张时,被拆开的那张
不再算「对子」(少 14 的罚分)、已经垫过的那门又短了一张(≤2 时拿到造缺的 −5),
两者自己升上来 —— 三个症状同一个改法。真正的平局(两门都还长、点数又一样)再用
一个 0.5 的次序:同分就接着垫已经开始垫的那门。**没有引入新权重**,改的只是
「键该按哪一手牌算」;`discardGreedy=0` 一键退回。

配对口径的行为审计(200 局,同一批决策点上两种口径各算一次):
垫牌路 194 个决策点、16 个不同 —— 同门(造缺)103→119、两门各一张 57→44、
同点不同花 23→20;贴分路 121 个决策点、13 个不同,「拆自家对子」14→12。
场景库补 I1 / I1b / I2 / I1c 四条(含「另一门只剩单张时该垫单张」的对照组 ——
这个修法很容易滑成「一律往同一门垫」)。

同桌配对对照(2400 副 = 1200 种子 × 交换阵营,新版 vs `discardGreedy=0` 的自己):
**+0.17 分/局 ±0.15(t=1.14),符号检验 p=0.86 —— 在分辨率之下,读作打平。**
两版行为不同的只有 131/1200 个种子(10.9%),其余种子配对差恒为 0、不贡献信息。
按 `NOTES/measurement.md` 的规矩记账:这一条**不是**靠聚合尺子成立的,
靠的是行为审计 + 第一性判据(排序键该按取牌之后的手牌算),尺子只用来确认它没变坏。

## v0.7.10(2026-08-19,测试版)

**这一版没动 AI,只动产品本体和文档。**

### 术语表并回 html,并加英语档

沪语术语原来单独挂在 `terms-shanghai.js` 里 —— 产品号称「纯静态、零依赖、单文件」,
却有一个外部依赖挂在外面,双击本地文件时少一个文件就退回普通话。现在 `TERMS` 是
html 里的一张三向表(`cn` / `sh` / `en`),`<script src>` 去掉了。

顺带修掉两处一直没人发现的问题:

* 四个座位名 `n0..n3` 的文字**只写在静态 HTML 里、从不更新** —— 三种中文写法恰好一样,
  所以加英语档之前谁也看不出来。
* 拉丁字母的座位名会和后面的词连在一起(`E` + `declare` → `Edeclare`)。
  `seatAbbr()` 给它补一个空格,中文档不受影响。

正式版 v0.7.8 仍在用 `terms-shanghai.js`,所以那个文件先留着,等这一版晋级再删。
(2026-08-28 晋级时已删,仓库里不再有这个文件。)

### 界面五项

| | 改了什么 | 为什么 |
|---|---|---|
| 出牌区牌序 | 和手牌用同一把尺子(`sortForDisplay`) | 原来按打出顺序摆,同一副牌在桌上和手上长得不一样,看的人得先在脑子里映射一次;甩四五张时尤其明显 |
| 记录可倒序 | 「最新在上」/「第1墩在上」 | 局中最常问「刚才那墩谁赢的」,复盘却是从第 1 墩顺着看 |
| 单墩进度条 | 拖到第 N 墩,单独按正常尺寸放大,并显示领出方与累计分 | mini 尺寸下四家的牌看不清 |
| 右栏可加宽 | `⇔ 标准 / 加宽 / 特宽`(倍率 1 / 1.35 / 1.75,上限 46vw) | 复盘时右栏太窄。存倍率而不是绝对宽度,横屏窄栏那套 `@media` 基准才保得住;竖屏自动隐藏 |
| 笔记标局数 | 每条记 `{r:局号, t:文本}`,**重开整场时一并清空** | 一场十几局,回头看「这条是哪局记的」原来只能靠记忆;不清空则局号会串到新的一场 |

进度条拖动时只重画标签和牌区(`seekPaint`),不整块 `renderPanel` ——
否则 `innerHTML` 一换,正在拖的滑块就被拆掉了。旧版笔记(纯字符串)读进来补成
`{r:null,t}`,不显示局号,不会变成「第 undefined 局」。

### 新量具:同桌对照的 SE 一直在高估 2~3 倍

`ai-h2h.js` 交换阵营的两副是**同一副牌**,牌运在它们之间严格反相 ——
两版完全一样时一副 `+d`、另一副必然 `−d`。旧代码把这 2N 副当成 2N 个独立样本求方差,
于是「净胜恒等于 0」和「SE = 2.76 分/局」可以同时出现:那 2.76 全是牌运,
而牌运在配对里是**恰好抵消**的。

改成配对统计量 `D=(d₀+d₁)/2`(先合并同一种子的两副,再对种子求 SE)。
空跑的 SE 从 **2.76 变成精确的 0.00** —— 恒等式级,不是「接近 0」。

同一批 1400 副数据,两种算法:

| 对照 | 净胜(相同) | 逐副 SE/t | 配对 SE/t |
|---|---|---|---|
| 测试版 v0.7.10 vs 正式版 v0.7.8 | +2.13 | 1.12 / t=1.90 | **0.47 / t=4.56** |
| `throwBossSubset` 单开关消融 | +0.91 | 1.12 / t=0.82 | **0.35 / t=2.61** |

第二行是验收:这条开关以前靠 **16 批 22400 副**才凑到 +0.88 ±0.28(t=3.09),
中途一度只有 t=1.30,差点被当噪声丢掉;配对口径**一批 1400 副**给出 +0.91 ±0.35。
数值对得上,成本差一个数量级。同一批数据在逐副口径下是 t=0.82,会被判成「量不出来」。

**均值两个口径完全相同,只有 SE 变** —— 以前的结论都是在被高估的 SE 下做的,
都偏保守,不会有哪条被推翻。工具两个口径都打,旧口径留着和历史数字对得上。
另外配对之后每个种子就是一个独立读数,符号检验不必再等「多批合并」,直接内置
(200 种子时 t 才 2.09,符号检验已经 p=0.0025)。

这条对应 `DESIGN.md` §10.6(j)。原计划是「双明手当控制变量」,动手才发现排在它前面的是
这个更基础的错误;双明手因此降级(108 张牌的完全信息解本来也算不动)。

### 新尺子的第一个用途:v0.7.1~v0.7.5 不是打平

当年读作 **−0.05 ±0.37(打平)**,正式版因此只摘了 `nearBossHold` 一条。
两份 build 直接从 git 取出来(`bbc4019` 的测试版 = v0.7.5,`5c8c643` 的正式版 = v0.7.0),
配对口径 6 批 × 700 种子 = 8400 副:

| 批 | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 净胜 | +1.01 | +0.47 | +0.80 | +0.46 | +0.68 | −0.28 |

**合并 +0.56 ±0.16(t=3.57)**,批间 5/6 正,I²=12%;逐种子符号检验
1350/2513 = 53.7% 为正,双尾 **p=2.1e-4**。跨文件空跑精确 0.00。

**不要把这个变化全算在新口径头上** —— 配对只改 SE 不改均值。均值从 −0.05 移到 +0.56
是样本和配置的差别(当年那次很可能带 `egSearch=0`,这次开着收官蒙特卡洛),
两次读数差 +0.61 ±0.40 = **1.5σ**,并不矛盾;当年那个 SE 就是太粗。

**留下的方法论**:一次「打平」永远有两种解释 —— 真的没效果,或者尺子看不见。
当年没分开这两条就直接按前者行动了。下次读到「打平」,先问尺子的分辨率够不够。

### 文档

`DESIGN.md` 补 §11「产品功能穷举」:状态机、房规选项、界面骨架、教练六项、
记录与复盘、术语、节奏音效、localStorage 键,以及 §11.9「已研究讨论、尚未实现」。
前十节讲 AI 怎么想,这一节第一次把 AI 之外的产品面收进一张总表。

---

## v0.7.9(2026-08-18,测试版)

**「已知下家断门,还把分贴过去」—— 8 条报障的共同根因是一处量纲不同步。**

需求列表里这一簇有 8 条(已知对手断门还领出/主动贴分、已知下家断门跟废送分、
三家断门下家贴分被王分收掉、#16 稳大时贴 15 分 ……)。形状完全一样:
我不是末家 → 身后有对手在这门**已知断门** → 我这一手加了分 → 被那个断门方毙走。

根因在 `scorePlay`:

```
gain = X.ptsTable + my + laterPoints(...)   // 含我要贴的分
p    = pTeamWin(...) -> X.certainty         // 贴分**之前**算好的
```

而 `pSurvive` 的已知断门分支对台面分是敏感的 —— `(ptsTable>0 ? 0.80 : 0.35)`。
于是台面 0 分、后手有已知断门对手时存活率算成 1-0.35x0.85 = **0.70**,
贴上一张 10 之后真实值是 1-0.80x0.85 = **0.32**。**贴得越多 gain 越大,而 p 一动不动。**

这不是一个可调的取舍,是内部不一致:收益按「贴之后」记账,概率按「贴之前」算。
修法就是用贴之后的台面分重算一次(`dumpVoidFeedback`,0 = 退回旧行为)。

**量到的:**

| | 开关关(旧) | 开关开 |
|---|---|---|
| 这类决策触发 | 242 次 | 212 次 |
| 贴出去的分(上界) | 6.53 分/局 | 5.52 分/局 |
| **真被断门方收走(下界)** | **3.18 分/局** | **2.23 分/局** |
| 收走比例 | 48.7% | 40.5% |

新量具 `test/audit-dumpvoid.js`。空跑(全关)**0.00 分/局**,自测 219/219,场景 20/20,
教练理由 16 类仍全部 0% 不符。

**聚合收益 4 批 5600 副合并 +0.25 ±0.56(t=0.45)—— 与噪声不可分。**
和 v0.7.5 一个模式:单类行为改对了,总分上看不见。区别在于这一条是**内部一致性**修复,
不是策略取舍 —— 旧写法没有任何一种模型口径下是对的。按「自对弈是回归护栏、不是裁判」
的规矩,它凭第一性判据成立,不由中性自对弈否决。但**先留测试版实测**,不直接晋级。
(实测通过,已于 2026-08-28 随 v0.7.9~v0.7.12 一并晋级。)

顺带:`ai-h2h.js` 加 `SEED0` 种子偏移(以前只能从种子 1 起,做不了独立批次)。

### 稳拿这墩时,该把「留着也赢不了墩」的主分牌兑现(`cashWinPoints`)

报障截图:♦ 作主、末家毙牌,玩家出 ♦K 而教练说「更好的是 ♦3 —— 最后一手,毙掉收分」。
**两处都错:**

* ♦K 和 ♦3 同为主、同为毙,那句理由对两张**同样成立** ——
  一条无法区分建议牌与实际牌的理由,本身就说明这条建议是错的;
* 真正的区别只有一个:♦K 多收 10 分。而 ♦K 上面压着 **14 张主**
  (大小王各 2、四门级数牌 8、♦A 2),排第 15,留在手里赢不了墩 ——
  期望大致是「一半贴给自己人、一半送给对手」。稳拿这墩时出它 = 确定的牌权 + 确定的 10 分。

`cheapSort` 里两张牌的排序键差 **9 级**,而 `cashPointW` 给 10 分的补偿是 **8 级** ——
差一口气就永远选不到它。**但没有去动那个常数**:v0.5.8 的标定没错,
错的是**拿牌序当未来价值的代理** —— 对一张上面压着 14 张的牌,这个代理彻底失真。
改成多生成一条候选,交给 `scorePlay` 用真正的 `futureValue` 比。

对照组当场抓到过度触发:打 10 时的主级数牌 ♦10 上面只压着两王(4/33),
留着既能赢墩又能带走自己那 10 分,不该兑现。所以在候选生成时设闸,
判据用和 `trumpHold` 同一个信号(在外更大的占未见主的比例),阈值 `cashWinMinAbove: 0.3`:

```
♦K (打4)   14/33 = 0.42  → 兑现
♦10(打10)   4/33 = 0.12  → 留着
♦3         30/33 = 0.91
```

**量到的:**触发率只有 **0.17%**(300 局 17124 次跟牌里改变 29 次),
那些墩多收 0.97 分/局;聚合四批 5600 副 **+0.12 ±0.56(t=0.22)**,
远在分辨率之下。**这条主要是正确性与可信度修复,不是棋力改进** ——
教练在给一条说不出差别的建议,而 AI 把稳到手的分留在了手里。

场景库加一正三反(`F1` 正例、`F1b` 非末家、`F1c` 高主分牌、`F1d` 理由必须提「兑现」),
场景 20 → 24。空跑(关掉)**0.00 分/局**。

### 甩牌只能甩整门 → 可以甩「压不住的那几组」(`throwBossSubset`)

需求列表里三条报障是同一个根因,就在候选生成的一行判据上:

```js
if(s!=='T'&&comps.length>=2&&bySuit[s].length<=6){
  const cl=classify(bySuit[s],trump);   // ← 只有【整门】
```

**AI 只会甩整门,从不甩子集。**于是:

* 「AI 不会撒对」—— ♣10♣10 和 ♣7♣7 都压不住,可手上还有一张带不动的 ♣ 小牌,
  整门就甩不成立,候选压根不生成;
* 「副色 AAK / AKK 不撒」—— 同理,♠A♠K♠K♠5 里 A 和 KK 各自都压不住,
  整门带上 ♠5 就废了;
* 「教练不能辨别撒牌比单张更大」—— 教练用同一套候选,集合里没有子集甩牌,
  只能拿单张去比,自然永远建议单张。

**为什么该甩:**一墩里「后出的牌结构必须和领出的一样才参与比较」([`RULES.md`](RULES.md) E)。
分两次打两个对子,对手有**一个**主对就能端走一次;两对一起甩,他得同时有**两个**主对 ——
概率低得多。**牌型本身就是护甲。**

改法:生成「本门里所有压不住的组件」这个子集当候选。判据只用 `isBossPlay`,
不猜对手断不断门 —— 那归 `ruffable` 管,两件事不混在一起。

**行为变化很大**(200 局):

| | 旧 | 新 |
|---|---|---|
| 领出里甩牌占比 | **0.7%**(27/3776) | **5.4%**(195/3601) |
| 平均每次领出 | 1.32 张 | 1.39 张 |
| 墩/局 | 18.9 | 18.0 |

也就是说改之前 AI **几乎从不甩牌**。教练理由体检里「甩牌:真的一次多张」的触发数
23 → 131,16 类仍全部 0% 不符。

**自测那条 `tricks<13` 是过时的代理量。**甩牌一次带走四张,一局墩数自然变少,
40 局里有一局掉到 12 就误报。先验证了 60 局的完整性(牌全部打光、每墩四家张数一致、
无死循环、0 异常)确认是合法局面,才把断言换成真正的不变量:
`cardsPlayed !== 100`(四家 25 张必须全出完)。**没有为了让测试通过而调松阈值。**

场景 24 → 27(`G1` 正例、`G1b` 只有一个组件压不住时不该甩、`G1c` 不许把带不动的牌捎上)。
空跑(关掉)**0.00 分/局**。

**收益:16 批 22400 副合并 +0.88 ±0.28(t=3.09),16/16 批全为正,
符号检验双尾 p = 3.1×10⁻⁵。**批次分布:最小 +0.17、中位 +0.86、最大 +1.63。

加批的过程本身是一条方法论:

| 批数 | 合并 | t | 符号检验 |
|---|---|---|---|
| 4 | +0.73 ±0.56 | 1.30 | — |
| 8 | +0.74 ±0.40 | 1.87 | p=0.0078 |
| 16 | **+0.88 ±0.28** | **3.09** | **p=3.1×10⁻⁵** |

八批时 t 只有 1.87、按老规矩不够格,但**八批全为正**这件事纯噪声下概率只有 1/2⁸ ——
每局净分差是重尾的,t 检验因此偏保守,批次符号检验更有力。已写进
[`NOTES/measurement.md`](NOTES/measurement.md) 清单第 10 条。

**这是项目至今量到的最大一条 AI 收益**(v0.7.8 的 `nearBossHold` 是 +0.41 ±0.18),
也是今天三条里唯一一条聚合上站得住的 —— 另两条 +0.25 ±0.56 / +0.12 ±0.56
都在分辨率之下,靠第一性判据与行为审计成立。

---

## v0.7.8(2026-08-17,两版同步)

> 先在测试版上线,2026-08-18 晋级正式版:只把 `nearBossHold` 那一段摘过去,
> 不带 v0.7.1~v0.7.5 那一摊。正式版基线上单独量过 **+0.41 ±0.18**(见下)。

**「AA 打完无 K 有 Q,不该先打 Q」—— 病灶不是领了张高牌,是把期权提前作废。**

按 [`NOTES/measurement.md`](NOTES/measurement.md) 的七条清单走了一遍:

**① 触发次数(净口径)。** 150 局 2840 次领出里这类占 **222 次**;扣掉「当时只剩这一门」的
强制情形后是 **221 次** —— v0.7.1 记的「这是上界不是净损失」**不成立**,强制情形只有 1 次。
这类丢掉牌权 **69.2%**,而全部领出的基准是 **39.3%**。

**③ 单次值多少(`test/cf-highlead.js`,新;700 局 1032 个决策点)。** 分层是关键:

| 层 | n | 改领这门最小的一张 | 改领 AI 次优候选 |
|---|---|---|---|
| **在外更大恰 1 张,且这门我还剩 ≥2 张** | 514 | **+1.74 ±0.78** | **+1.59 ±0.79** |
| 在外更大 ≥2 张(对照) | 376 | −0.20 ±0.74 | −0.35 ±0.94 |
| 这门我只剩这一张(对照) | 196 | +0.00 | +0.08 ±1.39 |

上面还剩两张以上时这张牌多半永远当不成钢板,留着反而挡手 —— **那一层是负的,不能一起罚**。
不分层就会得出「领高牌一律该罚」的错误结论,而那正是这条以前做不成的原因。

**落地。** `futureValue` 里副牌的溢价原来是**按 rank 写死**的(A 给 `6+0.26×rem`,
K 给 `2+0.10×rem`),可「离钢板差几张」和 rank 没有固定关系 —— 打 2 的时候♠Q 上面
只剩一张 K,它和「手握 A 时留 K」是同一件事,旧式子一分不给。新的 `nearBossHold`
按位置算:`unseenBeats(...).higher === 1` **且**这门我还有更小的牌可改领时才给。

**④⑤ 做成规则之后。** 扫参 3 → −0.13 ±0.17、6 → −0.14 ±0.25、10 → +0.29 ±0.27、
16 → +0.32 ±0.28,低值段是负的。取 12 与 16 各跑三批 × 700 副:

```
正式版  nearBossHold=12   +0.34 ±0.18 (t=1.9)
正式版  nearBossHold=16   +0.41 ±0.18 (t=2.2)   ← 取这个
测试版  nearBossHold=16   +0.49 ±0.15 (t=3.2,四批)
```

单批摆动很大(同一取值三批 +0.48 / +0.76 / +0.05),**单批下结论会是这个项目第三次栽在这里**。

**行为确实变了**(150 局):这类领出 221 → 117 次,送出的分 1625 → 700。
但注意全部领出的丢牌权率 39.3% → 40.5%、送出总分 11105 → 11305 ——
**省下的分有一部分挪到别处去了**,净收益远小于单次反事实的 +1.74。
这仍是 v0.7.5 那条教训的同一形状,只是这次没被稀释到零。

**⑦ 断言。** 一条正例 + 两条对照,比的是**同一牌面下开关开与关的差**,与默认值解耦。
顺带修了两处旧场景的**单副牌假设** —— 这是两副牌,「上面只剩一张」要求
♦A×2 和 ♦K×1 都已露面;E1b 那条对照组也因为同一个疏漏被新规则误伤,
改成让 ♥K 在两种情形下都是真钢板,它才重新只测「对手断不断门」这一件事。

`nearBossPtK`(按这门在外剩多少分线性加成)一直是 0、没扫过,按规矩不带着上线,已删。

## v0.7.7(2026-08-16,两版同步)

四条需求一次做完,**都不动 AI 的选牌**(正式版空跑 300 副 **0.00 分/局,SE 0.00**)。

* **速通模式**(设置里开关,默认关)—— 级数只走 **2/5/10/K/A**,每局最多前进一档,
  一整场最多 4 局。打开时必打关卡自动失效:阶梯本身就是关卡。见 [`RULES.md`](RULES.md) I 节
* **「对局记录」与「出牌历史」合并**成一个页签、三个视图(事件 / 每墩出牌 / 按花色),
  右栏少一个按钮,同一件事不再分两处看
* **跨局复盘** —— 每局结束时把牌面存进 `M.past`,记录页签第一行可以往回翻任意一局,
  带一行结算摘要(谁坐庄、主什么、闲家多少分、级数怎么走的)
* **「新开一局」改叫「重开整场」** —— 它重置的是整场,不是一局
* **教练理由的事实体检**(`test/audit-reason.js`,新)—— 见下

### 教练建议「文不对题」:扫出来四条,全修

教练页签给玩家看的那句话就是 AI 选中候选自带的 `reason`。**牌打对了、理由说错了**
不影响棋力,却直接毁掉可信度。新增体检脚本:AI 自己打 200 局,每次决策把理由里
**字面自称的事实**和当时的客观牌面对上(说「断门」就必须真断门,说「最后一手」就必须真是末家)。

| 问题 | 旧版占比 | 病因 |
|---|---|---|
| 「贴分/跟分」出的却是 0 分的牌 | **62.4%** | `buildFollow('dump')` 只是「本门里分最多的那手」,这门没分时照样返回一手 0 分的牌,而标签是**按意图**贴的 |
| 「断门毙牌抢分」「毙掉收分」台面根本没分 | **46.8%** | 这句从来不看 `X.ptsTable` |
| 「垫废牌,不送分也不拆主」实际带分或拆了主 | **19.1%** | `discard` 没有废牌可垫时会回落,标签没跟着回落 |
| 「这门的分已经打光」而领出的正是分牌 | 6 次里 4 次 | `unseenPointsIn` 数的是**在外**的分,我自己手上那张不在其中 —— 措辞错,改成「这门在外已无分」 |

改法统一是**看着结果说话**,不按意图说话。修完两版 16 类断言全部 0 不符。
另外体检自己也纠正过一次:「尝试吃,但后手或被毙」被正则误判成「我这手是毙」,
那句话本身没错 —— **量具的假阳性要先排掉,再看数字**。

## v0.7.6(2026-08-16,正式版)

把 v0.7.1 里三条**与 AI 强弱无关**的修复单独摘进 `index.html`,AI 改动一律不带
(diff 36 行,已逐行确认无 `AIP` 泄漏):

* **必打关卡**的「打过」收紧为**坐庄且守住** —— 旧版坐庄丢庄也算过关,是关卡 10 上跳级的根因
* **竖屏发牌手牌遮挡** —— 同一个 media query 里重复的 `#table` 规则把出牌区最小高度盖掉了。
  360×640 实测:129.6px → **139.6px**(= `cardH×3+10` 的下限)
* **甩牌判定加诊断** —— 判成甩牌时把拆解结果打进出牌记录

引擎自测 200/200。

## v0.7.5(2026-08-16,测试版)

**病灶不是规则,是一份跨墩的资源。**

聚合自对弈把一次决策的价值稀释进整局,问不出真话。改用**定点反事实回放**
(`test/cf-ruff.js`):在决策点上分叉,每条支路用同一套 AI 打完整局。
判据是「我在这门断门、这一手是毙、后手还有已知断门的对手、我手上还有更大的主」,
两个版本 × 1200 局 × 850+ 个决策点:

| 支路 | 相对「最省的毙法」 |
|---|---|
| 毙到手上最大的主(v0.7.1~v0.7.4 一直在做的) | +0.07 ±0.71 / −0.71 ±0.70 —— 零 |
| **不毙,垫一张废牌** | **+1.32 ±0.55 / +2.11 ±0.58** |

方向错了四个版本:问题不是毙得不够高,是那张主比这一墩值钱。
但换成正确方向、聚合**仍是零** —— 因为这些亏损不独立,提取的是同一份资源:
优势随「第几次遇到」衰减(+0.88 / +0.17 / −0.41),且只在手上有钢板可守时存在
(0 手 −0.05,1 手 +1.63)。

拆到底:省下这一张,后面靠毙牌多抢 **1.4 分**(15.9 → 17.3),整局多拿 **1.8 分**。
**资源是「主牌作为毙牌机会」本身**,该建的是它的**保留价**,不是又一条按墩生效的规则。

落地 `ruffVoidBehind: 20`;今天全部改动对固定参照 v0.7.0 从 −0.35 抬到 **−0.05 ±0.37**。

> **2026-08-19 补**:这个「打平」是尺子看不见,不是没效果。用 v0.7.10 的配对口径
> 重跑同样两份 build(8400 副)得 **+0.56 ±0.16(t=3.57)**。详见 `NOTES/measurement.md` §1.2。

## v0.7.4(2026-08-16,测试版)

`oppSpendCeil` 从「毙高一档」的门槛接到主判据:`pSurvive` 里毙牌被盖毙那一支,
能压我的牌只数**他肯花的那一档**,替掉 0.90 / 0.50 两个手调常数。

「逼他花掉一张大牌」这层消耗收益对最优对手为零 —— 在新模型里这是可证的
(`ceil` 的定义就是他从不超付)。但那不等于这一手不该打:他不花,这一墩连分就是我的。

**两把尺子第一次给出相反答案**:自对弈消融各条 +0.01~+0.15;
对固定参照 v0.7.0 四批 2800 副合并 **−0.23 ±0.18**。量具已验证(跨文件自比 0.00、全关 0.00),
负号分散在四条上,单条拆不开。详见 [`NOTES/measurement.md`](NOTES/measurement.md)。

## v0.7.3(2026-08-16,测试版)

**门槛从「他有没有」换成「他肯不肯」。** v0.7.1 那版按持有分布定门槛,实测 −0.92 ±0.15;
插桩查明:断门那家主牌密度高,能过门槛的只剩大小王 —— 178 次触发里 85% 打的是王,
而 78% 的墩台面是 0 分,等于掏一张王去赢一墩空气。

`oppSpendCeil` 把门槛改成他自己的比价:收益 = 台面分 × 阶梯权重 + 牌权那一截;
代价 = `trumpHold × phaseK` +(若它是全场最大)收官护底。
王的占比 85% → 29%,中间档(副级牌 / 主Q / 主A / 正级牌)出线。
四批 2800 副 **+0.15 ±0.05**。

## v0.7.2(2026-08-16,测试版)

`partnerRescueP` 的口径错位:pTeam 里「队友把它压回来」用的是队友在**本门**的高牌份额 ——
可只要有对手在这门断门,这墩是被一张**主**盖走的,队友那张本门的 A 没用。
按被毙掉的概率把本门那一截折掉(`rescueRuffAware`)。
带分领进已知断门、当时另有门可领:113 次 / 1245 分 → 99 次 / 1050 分,配对三批 +0.02 ±0.15。

另:埋分影子成本(`buryPtShadow`)被证伪 —— 见 [`NOTES/negative-results.md`](NOTES/negative-results.md)。

## v0.7.1(2026-08-15,测试版)

三条问题修复,都与 AI 强弱无关:

* **必打关卡**的「打过」收紧为**坐庄且守住**。旧版坐庄丢庄也算过关 —— 关卡 10 上跳级的根因
* **竖屏发牌手牌遮挡**:同一个 media query 里有条重复的 `#table` 规则,把出牌区最小高度整个盖掉。
  360×640 实测 129.6px → 139.6px
* **甩牌判定加诊断**:全牌型扫描没能复现报障,改为在出牌记录里打印拆解结果

## v0.7.0(2026-08-15,正式版)

**收官蒙特卡洛。** 剩 5 张起,每次决策抽样若干个「符合记牌约束」的世界推演到局末,
按引擎的 `scoreRound` 折成「我方净升几级」排序。那条一直没写对的最后一墩比较式在推演里自动成立。

```
vs v0.6.1   +1.46 ±0.52 分/局
vs v0.5.7   +5.63 ±1.05 分/局(t=5.39, 1600 副)
庄家丢掉最后一墩  41.6% → 24.9%
```

约 460ms/局(扫参和大批量自对弈记得 `egSearch:0`)。

---

*v0.6.1 及更早的完整条目在 [`NOTES/ai-journal.md`](NOTES/ai-journal.md)。*
