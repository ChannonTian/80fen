# 更新日志

## v0.7.1(2026-08-15,仅测试版)—— 需求列表里三条问题修复;毙牌侧两条改动一条留一条否

自测 205/205,场景 15/15。

### 问题修复

**① 必打关卡的「打过」口径收紧为「坐庄且守住」。** 报障是「关卡 10 上又出现跳级」。
引擎的关卡判定本身没错(12 条自测全过),错的是口径:旧版把
「这一局在打这一级」本身当成打过的证据,于是**坐庄丢庄也算过关** ——
在 10 上坐庄被打下来,下一次上台就直接过了 10。现在只有 `!sc.defendersWin`
才记进 `played`。不会卡死:停在关卡上的一方下一局必坐庄,只是可能要在同一级上
反复坐几局直到真守住。弹窗与设置说明同步改成「坐庄守住」。

**② 移动端发牌时手牌遮挡亮牌/打牌区。** 上一轮修的是对的 —— 竖屏给出牌区设了
`minmax(cardH*3+10, 1fr)` 的下限;但**同一个 media query 里、写在它后面**还留着一条
`#table{grid-template-rows:auto minmax(0,1fr) auto auto}`,后写的赢,下限被整个抹掉。
删掉那条残留。360×640 实测:出牌区 129.6px → 139.6px(正好是下限),牌不再溢出到提示行。

**③ 6677 被判甩牌** —— **没能复现,先加了诊断**。把 6677/6688 这类相邻对子在
5 种主况(四门 + 无主)× 13 个级数 × 四门里全扫了一遍,**没有一例**被判成甩牌;
唯一会被判甩牌的是「主门 6677 且级数正好是 6 或 7」(那时 6 是正级牌、7 是低主,
牌序不相邻,按标准牌序是正解),而反馈确认不是那种牌型。
所以这一版加的是**证据**:凡是判成甩牌,出牌记录里直接打印拆解结果
(拆成几组、每组是什么),下次再遇到一眼能看出是哪一组没接上。

### 智能提升

**④「他这门有几张」接上条件概率(`voidCondK`,默认开)。** 跟牌侧一直只有一个
「人均份额」:这门在外 m 张 ÷ 每家占未见牌的比例。但**已知断门是硬信息** ——
他的牌不可能在那几门,手牌被挤进剩下的门里,主门密度按
`未见总数 /(未见总数 − 断门那几门的未见数)` 成比例抬高。
旧版「他会不会毙」算对了(`pVoidLead`),「他毙得多狠」一直按人均。
同时把 `makeHoldRange` 的硬上下界也夹进来(`holdKRange`)——
中盘信息量小,收官才有力,这是 v0.5.9 就记过的。
配对 600 副:**+0.08 ±0.25**,量不到。模型对,但没换来分,如实记账。

**⑤「毙得够高」的候选(`ruffGuardFollow`)—— 实测否掉,默认关。**
报障「已知下一家断门,仍用小主毙牌,被压过」的根因**不是打分标定,是候选集**:
断门毙牌只生成 `minWinFollow`(刚好压过当前最大的那张主),
「毙高一档躲开已知断门的下家」这个选项压根不在候选里,概率再准也选不出来。
量一下这个漏洞有多大(正式版 150 局):断门毙牌 1324 次,其中 123 次被
**事先已知断门**的对手盖毙、而我手上本来就有更大的主 —— 白送 1055 分。

补上候选之后(门槛用次序统计量:在外 m 张主、他约摸 k 张,他手上最大那张的
期望位次是从大到小第 `m/(k+1)` 张 —— 用「在外最大的主」当门槛是错的,那等于
要求我握大王,实测生成 0 次):

| | 可避免的被盖毙 | 配对净收益 | 庄家丢最后一墩 |
|---|---|---|---|
| 关(基线) | 123 次 / 1055 分 | — | 26.3% |
| 开,不设闸 | **92 次 / 830 分** | **−0.51 ±0.29** | **33.7%** |
| 开,只在中盘 + 台面 ≥10 分 | 125 次(等于没发生) | +0.20 ±0.14 | 32.7% |

**修好了症状,但账是亏的**:大主提前花掉,收官没保底手段。加了闸就等于不发生 ——
说明它挣到的那一点全在收官段,而收官段正是它亏钱的地方。
这是「打分器只算一墩」那个上游缺口的**第四次现身**(见 AI-DESIGN §8):
大主的留手价值是按「守最后一墩」标定的,躲一次盖毙换不回来。
代码和开关都留着(`ruffGuard` / `ruffGuardBonus` / `ruffGuardMinPts`),
等中盘也有跨墩推演之后再回来试。

### 量出来了、这一版没动的两条

审计脚本已经能出数,留给下一轮:

* **领出一张「上面还有更大的、我手里又没有」的副色高牌**(需求列表「AA 打完无 K 有 Q
  不该先打 Q」):150 局里 222 次,丢掉 69.4%,送出 1630 分。
* **明知有对手在这门断门仍领这门**:557 次,丢掉 419 次,送出 3885 分。
  (这两个口径都还没扣掉「当时没有别的门可领」的强制情形,是上界不是净损失。)

## v0.7.0(2026-08-15)—— 收官蒙特卡洛

> 2026-08-15 晚:v0.5.8~v0.7.0 一并合入正式版 `index.html`。
> 正式版从 v0.5.7 直升 v0.7.0,整文件同步,只差 title / versionTag 两处标记。

```
vs v0.6.1   +1.46 ±0.52 分/局   (配对差，350 种子 × 交换阵营)
vs v0.5.7   +5.63 ±1.05 分/局   (t=5.39, 1600 副)
庄家丢掉最后一墩  41.6% → 24.9%
```

自测 197/197,场景 15/15,界面零改动。约 460ms/局(四家都在搜;界面里只有三家 AI,
而且本来就有 1~3 秒思考延迟)。

### 为什么是这一步

v0.5.8~v0.6.1 连撞三次「结构改对了反而掉分」——护底封顶、`drawTrumpValue` 的符号、
`bossBase`。三次追到底都是同一件事:**打分器只算一墩**,而护底、吊主、钢板串问的
都是「这一手在往后几墩里还能换来什么」。那几个量纲错误的大常数,
就是模型用来顶替这块缺失的补丁。所以顺序只能是:**先做推演,再回头拆常数。**

从收官做起是因为算力回报率最高:剩 5 张时状态空间已经很小,
而底牌翻倍让最后几墩的单墩价值远高于中盘。

### 怎么做的

**目标函数就是阶梯本身。** 每个样本推演到局末,按引擎的 `scoreRound` 结算,
折成「我方净升几级」。于是那条一直没写对的比较式

```
[底分 × 抠底倍数 × P(守住) + 当前分 − 目标分]  vs  [本墩得分 × P(拿下) + 当前分 − 目标分]
```

在这里是**自动成立**的:抠底倍数由推演里最后一墩实际出几张决定(不再是常数 2,
也不再是 `reserveUnit` 的估计),守住概率由样本频率给出,
目标分由 `scoreRound` 的 0/40/80/120/160/200 台阶给出。
`pointWeight` 那个高斯包络的近似,在收官段直接被真值替掉了。

**但纯阶梯是阶跃函数,台阶内部是瞎的。** 两个候选差 5 分,多数样本落在同一级台阶,
u 完全相同。所以掺一点连续项:`egPointsEps × 本队得分`,
一级台阶 40 分、`0.008×40 = 0.32` 级 —— 阶梯仍然主导,台阶内部有梯度可循。
实测这一小截值 **+0.32 分/局**(eps 0 → +0.89,eps 0.008 → +1.21)。

**抽样受记牌约束。** 未见牌按「每家手牌数相同 + 硬缺门 + `maxHoldIn` 上界」
分配到三家和底牌,「能去的地方最少」的牌先安排,按剩余空位加权;
分不动就重抽,抽不满 `egMinSamples` 就整个退回启发式 —— 约束太紧时样本不可信,
宁可不搜。

**推演策略必须便宜。** 用不起 `aiChooseFollow`(每次都要重建 108 张的记牌)。
rollout 只用 `minWinFollow` / `buildFollow` 这一层构造式候选:它们只要 hand/lead/trump,
而且天然合法。一个教训:第一版在 rollout 里调了 `makeMemory` 去判钢板,
**那一处占了搜索开销的大半**(679ms/局 → 去掉后 270ms)。
推演策略便宜比精确重要,样本量能买回来的东西比单样本质量多。

**融合方式是排序,不是相加。** 搜索的目标函数是「净升几级」,和分制不同量纲。
只用它排序,而且要比启发式选中的那个高出 `egMargin` 才改判 ——
样本噪声区里仍然听启发式的。理由字符串会带上「收官推演:…(60样本,净升 x.xx 级)」,
教练模式直接能看到。

### 扫参

```
配对差(相对 eg 关闭,350 种子 × 交换阵营):
  30 样本 / 5 张 / eps 0.008  → +1.21 ±0.52
  30 样本 / 5 张 / eps 0      → +0.89 ±0.49
  60 样本 / 5 张 / eps 0.008  → +1.46 ±0.52   ← 取这一档
  60 样本 / 6 张 / eps 0.008  → +1.17 ±0.62   更贵却更差:状态空间大了样本就不够用
```

### 下一步:回头拆那三个常数

搜索只覆盖最后 5 墩。中盘那三个「量纲错误的大常数」还在原地顶着 ——
但现在有了一件以前没有的东西:**收官段有了真值。** 可以拿它当标尺去校准中盘的启发式
(比如用推演算出的「守住最后一墩的真实概率」去校 `reserveHold`,
用推演的真实抠底倍数去校 `kittyMult`),而不是继续在自对弈的总分上盲扫。
§8 那条「拆之前先问它在代偿什么」现在有了下半句:**用推演给出的真值去回答那个问题。**

## v0.6.1(2026-08-15,仅测试版)—— 剩下两条重构做了,两条都没挣到分;为什么值得记下来

行为上**默认与 v0.6.0 完全一致**(两个新开关都默认关)。自测 197/197,场景 15/15。
这一版的产出是两条**否定结论**和它们背后的原因 —— 按 §6 的规矩,
「试过、掉分、于是不上」比「没试过」有价值得多,前提是把为什么写清楚。

### ① 钢板领出统一到期望分口径:能做到打平,那个常数没能消失

`scoreLeadCore` 的钢板支一直是 `bossBase 34 + size×8 + ev×1.4 − fut×k` ——
一个**比真实 stake 还大的标定常数**。v0.5.8 想给护底 flat 项封顶在它保护的那笔分上,
一封就输给它;v0.6.0 把它从 40 扫到 34 挣了 0.41 分,但那只是把它调小。
这一版把钢板支也拆成 `p·Gain − (1−p)·Loss + Tempo − Future`,
`ruffP > 0.25` 那条特例罚分也一并删掉(`leadWinP` 里 `pSurvive` 的
`pVoidLead × 0.8 × 0.85` 算的就是被毙的概率,不需要再补一刀)。

扫参(配对差,相对 v0.6.0,400 种子 × 交换阵营):

```
bossShapeBonus  6 → −1.14 ±0.28   (丢最后一墩 25.3%)
               14 → −0.28 ±0.62   (28.3%)
               22 → −0.04 ±0.69   (27.3%)
   22 + futLeadCashDamp 0.15 + leadTempoWeight 1.4 → 0.00 ±0.76   (29.0%)
```

**统一之后要靠一个 22 分的常数才能回到原位 —— 那不过是 `bossBase` 换了个名字。**
所以结论不是「这个常数是历史包袱」,而是**它扛着 EV 模型没表达出来的真东西**。
最可能是「保住牌权,把整串钢板依次兑现」这件事的复利:`leadTempo` 的贴现和只算了一层
(`tempoValue` 按价值降序乘 `tempoDecay`),而实际上赢下这一墩之后我还会再赢下一墩、
再下一墩 —— 真正的解法是把 Tempo 做成多墩推演,不是把常数搬家。

默认关。想在线上 A/B:`leadEV2:1, bossShapeBonus:22` 这一档是打平的,
而且「庄家丢最后一墩」比默认档好 2 个百分点(27.3% vs 29.3%)。

### ② 「不要主动领主牌」的先验显式化:−0.19 ±0.80

v0.6.0 查出来那条先验一直藏在 `drawTrumpValue` 的符号偏差里(`edge` 恒为负 ≈ −18.8)。
这一版把它摊开写成 `trumpLeadPrior`:

> 领一轮主牌逼四家各出一张。张数上两边打平,但**我方那两张本来留着毙牌和护底**,
> 对手那两张往往是他们最小的(他们没有义务吐大牌)。所以这一轮我方净亏一张主的期权。
> 代价按「队友手上还有主的概率」加权;主牌占优时按 `dt` 线性退到 0
> (那种情况下把双方一起清空恰恰是目的);再乘 `phaseK` ——
> 收官底里没分时攥着主牌毫无意义,该兑现就兑现。

三条豁免都是有道理的,自测里「主力雄厚时领出主牌且不带大怪」和
「底无分:收官王对该兑现」两条正是靠后两条豁免才没被压掉。
但配对对照 **−0.19 ±0.80**,没挣到分(丢最后一墩 29.3% → 28.8%,略好)。

**它不是错的,是没标定完。** 显式化之后强度、形状、豁免条件三样都得重新标定,
而藏在符号里的那一版是三样一起长出来的。代码和判据留着 ——
它比藏在符号里清楚得多,下一次连 `leadWeakTrump` 一起动时从这里接。

### 这一版真正学到的

三次了:**结构上明显更对的写法,实测都打不过那个「错」的旧写法。**
护底封顶、`drawTrumpValue` 的一家比两家、钢板支的常数 —— 每一次的原因都一样:
那个数值在**代偿模型里缺失的一整块东西**,而不是在表达它字面上写的那件事。

于是有一条方法论上的收获,已记进 AI-DESIGN §8:

> **拆掉一个「量纲错误」的常数之前,先问它在代偿什么。**
> 如果答不上来,就不要拆 —— 拆完为了回到原位而重新引入的那个常数,
> 会告诉你答案,只是要多花一轮扫参。

三处答案都指向同一个缺口:**打分器只算一墩**。护底、吊主、钢板串,
本质上都是「这一手在往后几墩里还能换来什么」,而 `Future` 是静态估值、
`Tempo` 只贴现一层。这正是 §7 第一行「无跨墩规划」那条 ——
它不是一条并列的短板,是上面三条的共同上游。

## v0.6.0(2026-08-15,仅测试版)—— 领出侧的牌权概率改问「本队」;最后一墩的四个部件补齐;三个标定常数重扫

```
vs v0.5.9   +0.79 ±0.88 分/局   庄家丢最后一墩 35.2% → 30.5%
vs v0.5.7   +5.07 ±0.89 分/局   庄家丢最后一墩 42.5% → 25.8%   (t=5.68, 2200 副)
```

自测 197/197,场景库 15/15,界面与样式零改动。

这一版做的是上一版列出的那两条重构。**结构性修正本身几乎都是零收益**
(A 段 +0.17、B 段 −0.03),真正的分数来自重构之后才有可能做的**重标定**。
这本身就是结论:那些常数一直在补偿结构上的错,不把错先修掉,常数就调不动。

### A. 领出侧的牌权概率:从「我这张活不活得下来」改成「本队赢不赢得下这墩」

`leadWinP` 一直是 `pSurvive(这一手, 两个对手)`,于是**队友把它压过去**和
**对手把它压过去**被算成同一件事 —— 都叫「没活下来」。可调王的**目的**就是让队友接过去。
这就是「调王没人接」的真正病根,而且它在**领出侧**,不在跟牌侧:
跟牌侧的接牌机器 v0.5.8 就已经就位了,是领出侧根本不肯发这张牌。

```
pTeam = P(两个对手都压不过) + P(有对手压过) × P(队友再压回来)
```

`partnerRescueP` 和跟牌侧的 `pPartnerTakes` 同一套坛子口径:在外压得住我这一手的牌里,
多大比例按期望落在队友手上;副牌再加一条「队友可能是**毙**回来的」。
再收一道 `leadRescueDamp = 0.7` —— 队友要压过的是**对手打出的那张**,不是我这张。

同时**关掉 `tiaoWangValue` 里的 `drawOut`**。那条算的是「逼对手消耗主牌」,
是**吊主**的逻辑,而且要用**大主**才成立:领出一张小主,两个对手各跟一张他们最小的,
大主一张不掉。现在吊主(要大主,走 `drawTrumpValue`)和调王(要队友,走 `pTeam`)分家了。

实测(400 局,小主领出的将牌墩):

| | 队友接走 | 领出方输掉 |
|---|---|---|
| v0.5.9(旧 `pSurvive` + `drawOut`) | 26.0% | 58.9% |
| v0.6.0(`pTeam`,关 `drawOut`) | **30.4%** | 57.2% |

理由字符串的判据也跟着改了:以前是 `tw > leadWeakTrump/2`,而 tw 的大头是 `drawOut` ——
关掉之后「调主」这个理由会整个消失,可**行为本身反而更对了**。现在判据是
「这张压不住场,但本队仍有相当把握拿下 —— 也就是队友救回来那一截占大头」。

### B. 最后一墩:四个部件补齐

上一版列的四条,全做了,全部带开关:

1. **抠底倍数不再是常数 2**(`kittyMultDyn`)。引擎口径是 `2 × 最后一墩每人出牌数`,
   AI 侧一直写死 2。按 `reserveUnit` 的张数估 —— 它就是我打算用来守最后一墩的那一手。
   意义不只是估得准:**这件事是可控的**,留一个钢板**对子**守最后一墩,抠底从 ×2 变 ×4,
   而旧口径里「留一对」和「留一张」拿到的护底价值完全相同。
2. **阶梯目标的「当前总分」含那笔待翻倍的底分**(`ladderKitty`)。
   `roundScore` 只统计打完的墩,`def` 里从来没有底牌 —— 而实测 **56.9% 的局,
   底分×倍数单独就足以跨过 80 线**。超过一半的局面里,`pointWeight` 在错误的位置上
   找「哪条线最敏感」。现在按期望折进 `def`(收官前用先验 0.5,进了收官视野
   用护底牌撑到最后一墩的概率去偏)。
3. **两只手臂用同一把尺子**(`guardPw`)。`scorePlay` 是
   `ev*(X.pw) + tempo − futureValue(...)` —— ev 乘了阶梯权重,护底项没乘。
   它们比的是同一种东西(把局面推过哪条线),该用同一个权重。
4. 剩下那条「`pointWeight` 是高斯包络而不是跨线概率」没做 —— 见下面「还没做」。

B 段整体配对差 **−0.03 ±1.08**,也就是三条结构修正合起来是中性的。

### C. 重标定:`bossBase` 40 → 34

非钢板那一支在 v0.5.8 换成真实期望分之后,两支的量纲第一次可比,40 就显得偏高了。
坐标扫参(配对差,400 种子 × 交换阵营):

```
bossBase   46 → −0.83 ±0.36     40 → 基准     34 → +0.41 ±0.38     28 → +0.04     22 → +0.30
```

取 34。**这一处是本版唯一挣到分的改动** —— 而它之所以能挣到,是因为 A、B 两段先把
结构摆正了。

### 两条重要的否定结论

**① 护底封顶仍然不成立,`bossBase` 不是它的拦路虎。**
v0.5.8 的推断是「封顶 1.0 打不过 `bossBase 56`,等重标定之后再试」。现在 `bossBase` 降到 34,
再扫一遍封顶:

```
endKittyCap  1.0 → −1.51 ±0.48  (丢最后一墩 40.5%)
             1.5 → −0.91 ±0.39  (36.0%)
             2.5 → −0.35 ±0.29  (32.3%)
             0(不封顶) → 基准    (28.3%)
```

**推断被推翻了。** 不封顶的护底不是在补偿 `bossBase`,它自己就是对的。
`endKittyWeight` 也重扫过:3.5 → −0.11、5.0 → 基准、6.5 → +0.00(丢最后一墩 27.3%),
5.0 附近是平的,留着。

**② `drawTrumpValue` 那个「一家比两家」的偏差,修了反而更差 —— 连重标定也救不回来。**

```
drawTrumpVsOne=1 + unit 3        → −0.74 ±0.61   丢最后一墩 35.3%
                 + unit 2        → −0.89 ±0.61   35.5%
                 + unit 3 + cap 18 → −0.83 ±0.62   35.3%
不修(默认)                        → 基准          28.3%
```

三档都是负的,而且「庄家丢最后一墩」一律从 28% 跳到 35%。原因想清楚了:
`edge` 恒为负,实际起的作用是**一条强烈反对主动领主牌的先验**。
在一个底牌会翻倍的搭档游戏里,攥住主牌(留着毙、留着守最后一墩)比吊主更值钱,
所以这条先验是对的。**式子的符号约定错了,但它产生的行为是对的。**
正确的做法不是把比较改对,而是把这条先验**显式写出来** —— 记进 AI-DESIGN §7,留给下一版。

### 还没做

* `pointWeight` 改成真正的跨线概率(现在是乘在边际收益上的高斯包络)。
  这一条要连 `pPartnerPrior` 一起动 —— 那两个数已知失准,而整个跟牌 EV 都围着它标定过。
* 钢板领出那一支也拆成 `p·Gain − (1−p)·Loss + Tempo − Future`,
  让 `bossBase` 这个常数彻底消失。34 只是把它调小了,没有让它变成「真实的分」。
* 把「不要主动领主牌」这条先验从 `drawTrumpValue` 的符号偏差里拿出来,写成显式的一项。

## v0.5.9(2026-08-15,仅测试版)—— 调王的诊断与三次失败的尝试

**配对对照 1800 副:+0.01 ±0.96 分/局(等于没动)。** 自测 197/197,场景 15/15。
这一版**代码层面几乎没改行为**(只留下 `tiaoUseExp` 一条口径修正),
真正的产出是把「调王为什么还是不对」查清楚了,以及三条走不通的路径记在案。

### 调王的病根:一个函数里写了两件不同的事

沪语里的「调王」是**用小将牌寻求队友支持接过牌权,或让度牌权给对手领出**。
而 `tiaoWangValue` 的主项 `drawOut` 算的是「逼对手消耗主牌」—— 那是**吊主**,是另一回事,
而且它要用**大主**才成立:领出一张小主,两个对手各跟一张他们**最小的**主,大主一张不掉。
我用一张小主换他们两张小主,没有任何优势被兑现。`tiaoDrawUnit = 7` 的收益大半是虚的。

三条沿旧思路的尝试,两条被实测否掉:

| 尝试 | 结果 |
|---|---|
| ① `tiaoUseExp` 用**期望**张数代替硬下界 | **保留**。旧版 `holdRange(p,'T').lo` 只有别的门都被演绎压低后才为正,也就是牌快打完时;插桩 400 局,调主 **95.1% 发生在 end 阶段**,而设计文档写的是「调主是中盘战术」。改完 mid 占比 9.8% → 14.3%,症状指标基本不动 |
| ② `tiaoEdgeVsOne` 修「一家比两家」的偏差 | **否掉**。诊断没错(`drawTrumpValue` 拿我**一家**的主牌数去减**两个对手合计**的,插桩 edge 均值开局 −18.7 / 中盘 −18.8 / 收官 −6.4,`tiaoNoEdge` 因此永久生效),但把闸门打开只是让调主从 174 次涨到 475 次,**末家用分牌赢反而从 4.5% 涨到 8.3%**,配对对照 +0.11 ±0.96 |
| ③ `tiaoUnlock` 把收益接到「解锁我方副牌钢板」 | **否掉**。想法对(吊主保护的正是那些随时会被毙的副牌钢板),但量太小:插桩均值 0.05,开与不开的调主次数 557 vs 559 —— 一次选择都没改变。按 §6 的规矩,形同虚设的参数不该带着上线 |

顺带记一条:`drawTrumpVsOne`(把同一个偏差修进**打分项**)配对对照
**−0.85 ±0.97 分/局,庄家丢最后一墩 26.2% → 37.8%**。
`drawTrumpUnit 6` / `drawTrumpCap 30` 是围着那个偏差标定出来的 —— **数值在补偿偏差,不能只修一半。**

**结论:调主打得更频繁 ≠ 打得更对。** 四个开关都留着,默认只开 ①。

### 下一步该怎么改(方案已定,未实施)

病根在 `leadWinP`:它用 `pSurvive(这一手, 两个对手)` 算「我这张活不活得下来」,
于是**队友把它压过去和对手把它压过去被算成同一件事**。可对调王来说,
队友接过去正是**目的**,不是失败。领出侧的牌权概率应该是

```
pTeam = P(两个对手都压不过) + P(有对手压过) × P(队友再压回来)
```

用它去乘 `leadTempo`、去收 `leadWeakTrump×(1−p)` 的罚分,「小主换牌权」的价值自然就出来了 ——
判据变成**队友有没有更大的主**,而不是「能逼对手吐几张」。到那时 `drawOut` 整项删掉,
让吊主(要大主)和调王(要队友)彻底分家。跟牌侧的接牌机器已经就位(v0.5.8 的 `takeOverEndTrump`),
缺的只是领出侧愿不愿意发这张牌。

### 最后一墩的口径:现在只做到一半

「留守最后一墩 vs 拿下当前一墩」这个比较,四个部件里现在只有两个是对的:

| 应该有 | 现状 |
|---|---|
| 底分 × **抠底倍数** | `AIP.kittyMult` 是**常数 2**(按最后一墩单张算)。引擎的 `RULES.kittyMultiplier = 2×最后一墩出牌数` 是对的,AI 侧没跟上。实测 **12.0%** 的局最后一墩不是单张。而且这件事**部分可控**:第二末墩拿到牌权后领一对,抠底就翻两番 —— AI 完全没有这个概念 |
| × **守住概率** | 有。`reserveHold(u,X)` 算的就是它,但只以「最好那手减次好那手」的**边际**形式进 `futureValue` |
| + 当前总分 − **目标分** | `pointWeight` 的阶梯(0/40/80/120/160/200)在,但 ① 它是一个**乘在边际收益上的高斯包络**,不是「跨不跨得过线」的直接比较;② `roundScore` 只统计**已完成的墩**,`def` 里**不含那笔待翻倍的底分**。实测 **56.9%** 的局,底分×倍数单独就足以跨过 80 线 —— 也就是说超过一半的局,AI 的阶梯目标算的是一个缺了大头的分数 |
| 两边**同一把尺子** | **不是**。`scorePlay` 里 `ev` 乘了 `X.pw`,而 `futureValue`(护底项在里面)**没有乘** —— 比较的两只手臂量纲不同 |

**这件事的规模:17.4% 的局,最后一墩的归属直接改变了落在哪一级阶梯上。**
四条里前三条建议一起做(单独动 `kittyMult` 会和护底的标定打架),
并且要连 `bossBase` 一起重标定 —— 那正是 v0.5.8 里封顶失败卡住的地方。

## v0.5.8(2026-08-15,仅测试版)—— 九条实战反馈的根因修复

一次处理掉九条反馈,思路是**先找根因再动手**:九条反馈不是九个 bug,是 4 个根因
加一个流程缺口。之所以前几版「按下一个冒出另一个」,是因为改动都挂在
`scorePlay` / `scoreLeadCore` 外面当特例分支,每个特例自带一套触发合取式,
而**合取式总是不完整** —— 补上正例的同时放进了反例。

**同桌配对对照(1500 种子 × 交换阵营 = 3000 副,引擎统一,只把 `aiDiscard` /
`aiChooseLead` / `aiChooseFollow` 三个函数按座位分派到新旧两版):**

```
新版净胜 +4.31 分/局   (SE 0.76, t=5.68)
新版坐庄丢掉最后一墩 28.0%   旧版 40.5%
```

自测 197 项全过。新增回归场景库 `ai-scenarios.js`,15 条断言:
新版 15/15,v0.5.7 与正式版 v0.5.2 各 5/15。

> 那个 40.5% 比之前单版自对弈量到的 34.4% 高,是因为**对面换成了新版**:
> 新版抢最后一墩抢得更凶。两个数字在同一个配对实验里,可比。

| 反馈 | 根因 | 开关 |
|---|---|---|
| ① 底分>10 却提前没了保底手段 | 护底项是阶跃函数 + 主牌排序把大小王当便宜牌 | `endNearGamma` `endHorizon` `trumpNoPremium` |
| ② 将牌墩无人接、调王没人理 | 调主与接调主写在互斥的阶段里 | `takeOverEndTrump` |
| ③ 断门有分却不毙 | 「断门必毙」只写了队友暂大那一半 | `ruffOppRule` |
| ④ 有一对J 却先出一对8 | 非钢板领出没有 EV 模型 | `leadEV` |
| ⑤ 打2 造反自己对门 | 抢庄红利没在「已亮的是队友」时抵消 | `grabPartnerKeep` |
| ⑥ 小牌探路送分送牌权 | 同 ④ | `leadEV` |
| ⑦ 毙牌不用实惠牌、消耗大牌 | `controlPremium` 误加在主牌上 | `trumpNoPremium` `trumpTopPremium` |
| ⑧ 队友已必赢、末家还毙牌 | 「断门必毙」漏了 `!isLast` | (直接修,无开关) |
| ⑨ 毙牌定大不选主分牌 | 「最省的吃法」没有「必赢」这个分支 | `cashPointW` |

所有改动都带 `AIP` 开关、默认全开,可单组件消融。逐条配对消融(800 副,共同种子逐局相减):

| 关掉这一条 | 净胜变化 | 庄家丢最后一墩 |
|---|---|---|
| `cashPointW` ⑨ 必赢时兑现主分牌 | **−1.89 ±0.47**(t=−4.1) | 28.8% → 29.8% |
| `ruffOppRule` ③ 对手暂大时的断门必毙 | −0.63 ±0.38 | → 29.3% |
| `trumpNoPremium`+`trumpTopPremium` ⑦① 主牌排序 | −0.53 ±0.40 | → **31.3%** |
| `leadEV` ④⑥ 非钢板领出的 EV 口径 | −0.41 ±0.90 | → 30.8% |
| `dumpTrumpDiscount` ⑧ 贴分避开主牌 | −0.30 ±0.26 | → 29.5% |
| `takeOverEndTrump` ② 收官接将牌墩 | −0.04 ±0.07 | → 28.5% |
| `grabPartnerKeep` ⑤ 不反自己对门 | 0.00 | 不变 |

⑤ 那个 0.00 不是「这条没用」,是**自对弈量不到它**:`simulateRound` 的亮主被简化成
一轮制明牌比强,根本不会走到「队友已亮、我要不要反」这个分支。它的验收在场景库里。
⑦ 那一行同理:净胜只挣 0.5 分,但「庄家丢最后一墩」从 28.8% 掉到 31.3% ——
它真正修的是护底手段在中盘被烧掉这件事,那笔账要到收官才结。

---

### ⑦① `controlPremium` 加在主牌上,把大小王当成了「便宜牌」

**单点影响面最大的一处,正式版 v0.5.2 同样有。**

`cheapSort` / `cheapKey` 的排序键是 `globalIdx + (分牌?8:0) + controlPremium(c)`,
而 `controlPremium` 按**自然点数**判 A/K/Q。注释里写得很清楚,这个溢价是为副花色设计的
(「A/K 虽然不是分牌却是撒牌的本钱」)—— 但主牌也走同一条键。主♠打2 时:

| 牌 | globalIdx | +分 | +溢价 | cheapSort 键 |
|---|---|---|---|---|
| 主J | 108 | 0 | 0 | 108 |
| 副常主 ♣2 | 112 | 0 | 0 | **112** |
| 主Q | 109 | 0 | +4 | **113** |
| 小王 | 114 | 0 | 0 | **114** |
| 大王 | 115 | 0 | 0 | **115** |
| 主K | 110 | +8 | +12 | **130** |
| 主A | 111 | 0 | +30 | **141** |

也就是 AI 认为大小王比主K/主A 更便宜。实测断门毙牌:手持「主A+大王」出大王、
「主K+小王」出小王、「主Q+副常主」出副常主 —— 反馈 ⑦ 的字面复现。
它同时是反馈 ① 的一半:中盘每一次断门,AI 都在优先烧掉自己最后的护底牌。

修法两条。**(a) 主牌不吃 A/K/Q 溢价。** **(b) 换成梯子顶端的控制溢价**
(大王 14 / 小王 10 / 正常主 6 / 副常主 4):`globalIdx` 只是序号,大小王只比主K 高
4~5 个序号,可控制价值差着量级 —— 这正是 v4 把 `trumpHold` 从绝对刻度改成相对刻度的
同一个道理。没有 (b),分牌那个 +8 就足以让 AI 拿小王去换一张主K。

`cheapKey` 必须同步改:它同时是**对手模型**的一部分(`makeReads` 用它反推
「他手上没有比 X 更便宜的本门牌」),不改的话 `maxHoldIn` 会一直假设别人先打大王再打主A。

### ⑨ 「最省的吃法」缺少「必赢」这个分支

`minWinFollow` 一律取 `cheapSort` 的第一张,而 `cheapSort` 给分牌一个 +8 的固定回避。
末家毙牌是**稳拿**的:这时候「最省」的正确定义反过来 —— 手上的主分牌该趁机兑现,
留在手里迟早要出,拖到没牌权时更可能白送给对手。实测旧版:
手持「主7/主10/主K」出主7、「主10/主J」出主J,10 分白白留在手里。

`certain` 一路传到 `cheapSort` / `buildFollow` / `ruffThrow` / `blockWinFollow`;
目前只在末家置真(唯一真正确定的位置)。分值折算成牌序用 `cashPointW = 0.8`:
`trumpHoldTop` 14 摊在约 12 级上 ≈ 1.2 分/级,10 分 ≈ 8 级。
**这是本版单条收益最大的一处:+1.89 分/局(t=−4.1)。**

### ⑧ 「断门必毙」这条规则漏了 `!isLast`

v0.5.7 新增的 1c-0 分支意图是对的,实测也修好了它瞄准的那一类。但合取式里少了
`!X.isLast`:我是末家时,「队友暂大」= 队友**已经赢下这墩**,而 `curBoss` 仍可能是 false
(队友出 K、A 还没露面),于是规则照常触发、给毙牌一个 +25 的硬加成 ——
**AI 毙掉自己队友已经拿下的墩。**

400 局:

| 局面 | v0.5.2 | v0.5.7 | v0.5.8 |
|---|---|---|---|
| 队友暂大但非钢板、我断门(非末家)→ 毙 | 0.0% | 86.2% | 86.2% |
| 队友已必赢(我末家)、我断门 → 毙 | 0.0% | **51.5%** | **0%** |
| 对手暂大、我断门有分 → 毙 | 71.8% | 70.4% | 见 ③ |

顺带修 `dumpSort`:注释写着「同样主牌垫后」,但代码里主副之分只是**同分值时**的次序键,
分值优先级更高。于是断门贴分时若手上唯一的分牌是主牌,它会把主牌贴出去
(正式版 400 局里 219 次)。现在主牌上的分打 `dumpTrumpDiscount = 0.5` 再排序:
副色的 5 排在主 5 前面,主 10 仍排在副色 5 前面。

### ③ 同一条规则的另一半:对手暂大时

「断门 + 台面有分 + 我有主」在对手暂大时是同一条规则,当初只写了队友那一半。
另一半只是个普通候选,毙不毙全看 `scorePlay` 和 `futureValue` 打架,三成的机会被放掉。
现在给同等的规则级加成(`ruffOppBonus = −25`,和 `ruffPartnerBonus` 同量级 ——
它们本来就是一条规则的两半)。

> 顺带记一个**我自己踩进去的坑**:诊断时我把「收官 5 张、对手♦K(10分)暂大、
> 我♦断门、手上只有一张大王,AI 不毙」也算成了这条的漏网之鱼。其实那是**正解** ——
> 毙掉之后手里剩四张输牌,最后一墩必丢,底牌那笔 13.7×2 跟着送出去;
> 用 10 分换 27 分是对的。③ 的真正发生地在中盘,不在收官。
> 场景库里现在两个方向都有断言,防止以后又把它当 bug「修」回去。

### ② 调主和「接调主」被写在互斥的阶段里

插桩 400 局:AI 自己领出的调主共 193 次,**92.7% 发生在 `end` 阶段**,mid 只有 7.3%,
open 一次都没有。而 `takeOverScoped` 的第一句是 `if(X.phase==='end') return false`。
**AI 有九成的调主打在队友被代码禁止响应的阶段**,「调王没人接」是必然的,
不是评分偏好问题,是候选压根不生成。

为什么调主都挤在收官?`tiaoWangValue` 的前提是「对手主门持有**下界** > 0」,
而下界来自 `makeHoldRange` 的「手牌数 − 其他门上界之和」—— 这个约束只有在别的门
都被演绎压低之后才为正,也就是牌快打完的时候。「调主是中盘战术」这条设计意图,
和「下界只在收官才有信息量」这条实现约束,本来就对不上。

现在收官阶段对**将牌墩**放开。当初关掉它的理由是「那一档的不接是护底锁造成的」——
那是护底 flat 项的问题,不该由这道闸门代偿。

「接得够高」那一半(反馈里的「末家用10、K直接拿分」)不需要新代码:
`blockWinFollow` 的候选一直在生成,只是在**在外还有十几张主压得住 SA** 的局面里
打不过 `minWinFollow` —— 那种局面下用 SA 去接本来就是坏棋。
大牌已现身、SA 接近钢板时(`pSurvive` 0.99),AI 会正确地选 SA。场景库两个方向都有断言。

### ④⑥ 非钢板领出没有 EV 模型,只有「越小越好」

`scoreLeadCore` 实际上是**两套评分器**:钢板/甩牌走
`leadPointsEV + pWin + tempo + futureValue`,其余全部走一条手写常数式,
唯一的牌力项是**负的一次项**(对子 `20 − top×0.5 …`,单张 `16 − top×0.8 …`)。

实测(手上 ♣J♣J 和 ♣8♣8):**一对J = 6.7,一对8 = 8.2**,差的 1.5 分**全部**来自
`−top×0.5`。打分器里根本没有「一对J 大概率压得住场、还能把对方的一对10 逼出来」
这件事的位置 —— `leadPointsEV` 只在钢板分支被调用,`LEAD_PWIN` 又是按牌型写死的常数
(pair 一律 0.45,不管是一对3 还是一对A)。这也是 `probePenalty` / `tiaoWangValue` /
`feedRuffValue` / `probeSafeBonus` 全变成往同一条式子里加加减减的常数的原因 ——
**「按下一个冒出另一个」在领出侧的发源地。**

改成和跟牌侧同一个式子:`Score = p·Gain − (1−p)·Loss + Tempo − Future`,
`p` 由 `pSurvive` 直接算(跟牌侧一直在用,不是新造的假设)。于是:

* 小牌探路的 `p` 天然低 → 自动降权,`probePenalty` 不再需要;
* 一对J 的 `p` 高于一对8 → 反馈 ④ 自动修好(现在 22.3 vs 20.5),`−top×k` 不再需要;
* `LEAD_PWIN` 那张常数表退休,`leadTempo` 改用算出来的 `p` 加权。

顺带解决了一个老结论的适用范围:「对所有主牌领出一律加正的 edge 奖励反而掉分」
(v4 的实测)成立的前提是,在那条常数式里 edge 是个**跨候选的常数偏移**,把低主领出
也一起抬了上去。换成 EV 口径后它不再是常数 —— 按 `p` 加权,压得住场的主拖拉机拿到
大部分奖励,一张孤零零的小主几乎拿不到。同理 `leadWeakTrump` 那个「白烧一张主」的
罚分改成按**输掉的概率**收 `×(1−p)`。这条修好了自测里
「主力雄厚时领出主牌且不带大怪」那一项(EV 口径下一度选了大王,现在正确选主 55-66 拖拉机)。

`leadEV = 0` 可整块退回旧的三条常数式。

### ⑤ 无庄开局时,抢庄红利没有在「已亮的是队友」时抵消

**打2 = 全游戏第一盘 = 唯一的无庄状态**,所以这条只在打2 出现。
无庄盘里最终亮主者坐庄拿底,而 `grabBonus`(85)只加在候选上、不进 `base`
(`base` 只取 `declHandQuality`),于是从**自己队友**手里把庄抢过来,账面上白赚 85 分;
「队友已亮」的罚分只有 −30,无庄盘门槛又只有 `10−10·prog`(+12)≈ 12~22。

实测(♠2♠2 + 长♠,队友已亮 ♦):

```
无庄开局、队友已亮 → 增量 +91.7 / 门槛 17.2 → 造反自己对门
同一手牌、庄已定   → 增量  −7.3 / 门槛 35.4 → 正确地不动
同一手牌、对手已亮  → 增量 +142.3 / 门槛 17.2 → 正确地造反
```

还有一层:`base` 用的是**我自己**在队友主色下的契合度,队友主色我越短基线越负、
增量越大 —— AI 恰好在「队友有长门而我没有」时最想反队友,正好反了。
对照实验:把 ♦ 换成我也很长,`base` 从 −18.4 变成 +33.5,增量 −12.0,就不反了。

现在队友已亮时红利只保留 `grabPartnerKeep = 0.08`(同队之内谁坐庄确实有差别,
但那是个位数,不是 85)。修完:增量 +13.5 / 门槛 17.2 → 不动;对手已亮的对照组不受影响。

### ①③ 护底项:两条改对了,两条改错了

`futureValue` 里的护底 flat 项旧版是一条**阶跃函数**。实测一张大王的 `futureValue`
随剩余张数(底分≈13):

```
剩 12/9/7 张 → 11.5(护底 0)      ← 前 19 墩护底价值恒为零
剩  6 张 → 34.9(护底 24.1)       ← 视野一开就是断崖
剩  3 张 → 97.7      剩 1 张 → 139.0
```

`near` 上面那段注释说 v4.4 已经改成「gamma<1 的幂 + 天花板锁在它保护的那笔分上」,
**代码里两条都没有**,正式版 v0.5.2 也一样 —— 合并时丢了,或者方案没落地。
补上之后做了配对对照(800 副,共同种子逐局相减):

| 配置 | 净胜 | 庄家丢最后一墩 | 相对当前默认 |
|---|---|---|---|
| cap 2.5 + γ0.5 + floor 0.25 + 视野前移 | 2.67 | 28.8% | — |
| cap 0 + γ0.5 + floor 0.25 + 视野前移 | 3.38 | 23.5% | +0.71 ±0.39 |
| **cap 0 + γ0.5 + floor 0 + 视野前移** | **3.84** | **23.0%** | **+1.18 ±0.40** |
| cap 0 + γ1 + floor 0 + 视野前移 | 3.33 | 25.3% | +0.66 ±0.37 |
| cap 0 + γ0.5 + floor 0 + 视野旧 | 3.13 | 27.8% | +0.46 ±0.48 |
| 护底整块退回旧版 | 2.62 | 30.8% | −0.05 ±0.47 |

结论:

* **凹曲线(`endNearGamma = 0.5`)对,留。** 视野一进入就给到大半,而不是到最后一两墩
  才给满。相对线性 +0.52。
* **视野前移(`endHorizon` 4→5、`endHorizonPerPt` 1/6→0.25、上限 9→12)对,留。**
  底分 13 时从剩 6 张提到剩 8 张起算,接上中盘尾段,消掉 0→24 那个断崖。相对旧视野 +0.72。
* **封顶(`endKittyCap`)错,默认关掉。** 我的量纲论证是「守最后一墩这个期权不可能比
  它保护的那笔分更值钱」—— 数字不同意:封顶之后庄家丢最后一墩从 23.5% 涨到 28.8%,
  净胜掉 0.71。原因是钢板领出那一支的 `bossBase 40 + size×8 = 56` 本身就是个比真实
  stake(≈27)还大的**标定常数**,封顶之后护底就打不过它了。
  `endKittyWeight` 注释里那句「这个量纲错误的大数值,买到的正是收官阶段那条硬约束」
  是对的。等 `bossBase` 重标定之后应该一起再试一次。
* **边际下限(`reserveFloor`)错,默认关掉。** 动机是对的
  (`max(0, p最好 − p次好)` 在大王 0.95 + 小王对 0.90 时只有 0.05,「牌越好护底越弱」),
  但补法不对:下限是个常数,而真正该算的是「两手护底被同时耗光」的联合概率。

四条开关都留着,方便以后连同 `bossBase` 一起重标定。

### 流程:回归场景库 `ai-scenarios.js`

`AIP.overPartner` 的注释早就写到了要害:**自对弈两边是同一个 AI,它从不像人那样
带着意图去调王,所以「队友调了王、我该不该接」这道题根本不会被出出来。**
反馈 ②③⑧ 全是配合类,①④⑤⑦⑨ 是「明显被支配的选择」—— 两类都不会让任何单元测试失败,
也很难在配对自对弈里显出统计显著的差异(⑤ 那一行就是 0.00)。

所以「不再按下一个冒出另一个」靠的不是某次改动,而是**把每条反馈钉成一条回归断言**。
`ai-scenarios.js` 把这九条做成 15 条断言,可以对任意版本跑:

```
node ai-scenarios.js 80fen-test.html     # 改动后 15/15
node ai-scenarios.js index.html          # 正式版 v0.5.2:5/15
```

以后每收到一条反馈,先加一条场景,再动代码 —— 这样新补丁踩掉旧修复时会立刻红。
本版的 ⑧ 就是这么被发现的:v0.5.7 修好了 1c-0 瞄准的那一类,同时把另一类从 0% 推到 51.5%。

## v0.5.7(2026-08-14 测试版 / 2026-08-15 正式版)—— 对子被压的概率按组合算;甩牌漏毙结案

> 2026-08-15:v0.5.3~v0.5.7 一并合入 `index.html`,正式版从 v0.5.2 直升 v0.5.7
> (调王轮三家不接、不埋副 A、断门有分默认就毙、理论最大对子、甩牌不毙的复查、
> 持有区间下界、调主成为独立候选、副色探路降级、对子被压概率按组合算)。
> 整文件同步(逐行比对确认只差 title / versionTag / localStorage 前缀三处测试标记)。
> 引擎自测 197/197。

**把 `pPairAbove` 接进打分**(v0.5.5 欠的那一半;`pBeaterIn`)。

v0.5.5 已经算清了「同点数的两张不一定在同一家手里」,但只把它用在 `isBossPlay`
的布尔阈值上。真正花钱的地方在 `pSurvive` / `pPartnerTakes`:那里问的是
**「他手上有没有压得住我这一手的牌」**,而旧版对单张和对子用了同一个公式。

单张和对子是两个不同的组合问题:

* 单张:从这门在外的 `total` 张里他摸走 `k` 张,只要有一张更大 → `1−(1−higher/total)^k`
* 对子:得**同一个点数的两张都落在他一个人手上** → 指定点数 `k(k−1)/(total(total−1))`,
  在外还剩两张的点数有 `m` 个就是 `1−(1−q)^m`

差多少?这门在外 12 张、4 个点数各剩两张、他约摸 4 张:
**旧版 0.99,实际 0.32。** AI 一直在按「我的对子必被压」打牌 —— 既不敢出对子,
也不敢把对子算成钢板。拖拉机还得连着,再按 `tractorTighten` 收一道。

**自对弈 1300 副配对牌:+1.02 分/局(SE 0.21,t=4.75)。**
四批分别是 +1.58 / +1.30 / −0.03 / +1.13 —— 有一批是平的,合并值以四批为准,
**不要单引 +1.58**。`pairUrn=0` 消融回到 0.00,说明这一版的收益全部来自这一处。
`tractorTighten` 三档结果完全相同:参数是活的(直接取值 0.030 / 0.060 / 0.101),
只是拖拉机被压的概率本来就小到不足以翻转任何一次选择 —— 低杠杆,不是没接上。

这也是 v0.5.5 里那句「正确取法是把 pPairAbove 当连续概率接进打分,而不是放宽布尔
阈值」的兑现:阈值仍是保守的 0.15,收益从打分侧拿。

**副色甩牌漏毙:结案,生成侧没有 bug。**
v0.5.5 记的「8 例生成侧漏掉候选」是我的判据太松 —— 只查了「我有没有主对」,
没查「这手压不压得过当前最大」。别人已经用更大的主对毙过了的局面被误算成 bug。
改用引擎自己的 `minWinFollow` 判定后:250 局 166 次机会,结构上赢不了 75 次,
**生成侧 0 例**,评分侧 91 次里选中 85(93.4%,v0.5.2 时是 83.5%)。

自测 191 → 197 项。

## v0.5.6(2026-08-14,仅测试版)—— 持有区间的下界;调主成为独立候选;副色探路降级

这一版处理的是同一条实战反馈的三层:**「副色小牌探路」不该排在「调王/调主」前面。**

**① 持有区间的下界(`makeHoldRange`)。** 以前只有上界(某家在某门最多还有几张)。
但升级有一条很强的约束被漏掉了:**每家手牌数在任何时刻都相同**。于是

> 下界(座, 门) ≥ 手牌数 − Σ_{其他门} 上界(座, 其他门)

一旦某家在别门露出硬断门,剩下的牌就被挤到这一门来。没有下界就没法回答
「对手手上还有没有主」,而这正是调主该不该打的前提 —— 所以先做它。

**② 调主不再被当成废棋(`tiaoWangValue`)。** 旧版对「压不住场的小主领出」一律扣
`leadWeakTrump`,把两种完全相反的局面混为一谈:对手主已断时那确实是白烧一张主再
把牌权送出去;而对手还必须跟主时,这是用极小的消耗换两样东西 ——

* **逼对手消耗**:按下界算「对手至少还得跟出几张主」,最多记 2 张;
  我方主不占优时只按 `tiaoNoEdge` 折算(那种情况下等于替对手清场)
* **牌权过渡给队友**:按队友主门下界 / 对手主门上界估他接过去的份额

200 局插桩:领出理由里出现「调主」87 次(2.3%)。

**③ 副色探路降到兜底(`probePenalty`)。** 只保留三条豁免:队友在这门 K 领出并赢下
(外面还有 A 却敢这么打 → A 几乎可以断定在他手里,这时候出小牌是**贴分**不是探路,
`partnerAceRead`)、队友这门已断门(送毙,原有 `feedRuffValue` 已覆盖)、这门的分
已经打光。其余一律扣分,让调主之类的候选先出线。

对照正式版 v0.5.2,200 局:

| | v0.5.2 | v0.5.6 |
|---|---|---|
| 领出带「探路」占比 | 24.7% | **17.1%** |
| 探路墩送出去的分 | 5510 | **3850** |
| 断门+队友不稳赢时的毙牌率 | 48.9% | **86.8%** |
| 埋底埋掉副花色 A | 20.2% 的局 | **0** |

**自对弈仍然是中性的**(两批共 600 副配对牌,−0.23 分/局,SE 0.45),这在意料之中,
理由见 v0.5.4 那节:调主的收益要靠队友接得住才能兑现,四家同一套 AI 的自对弈
结构上看不见它。按既定口径,**自对弈在这里只作回归护栏,不作裁判**。
`probePenalty` 扫 0/6/12/24、`tiaoDrawUnit` 扫 0/4/7/12,全部落在噪声内(|t|<1.1),
说明没有站在悬崖边上,取值按判据而非按坡顶。

自测 182 → 191 项。

## v0.5.5(2026-08-14,仅测试版)—— 理论最大对子;甩牌不毙的复查

**「在外还有更大的对子」不是布尔问题(`pPairAbove`)。**

旧版 `unseenPairAbove` 只看「这个点数还有两张没见」,等于按 100% 估「这两张在同一家手里」。
实际上同点数的两张可能分在两家,那就凑不成对:

> 三家各 h 张、未见共 U 张时,两张同点数落在同一家的概率 = 3·h(h−1) / (U(U−1))
> 开局 h=25、U=83 → **0.26**;收官 h=5、U=23 → **0.12**

也就是说 AI 一直在按 100% 估一件实际只有 12~26% 的事,系统性低估自己的对子。
「理论最大对子」跟「本门单张最大牌」是两套账,而且这门越打越薄、剩下的同点两张越不可能还在一家手上。

`isBossPlay` 现在先走严格判据,严格意义上还有更大的对时再看概率是否低于 `pairBossMaxP`。

**阈值取 0.15,而扫参的坡顶在 0.25~0.35 —— 这是故意的。**
样本外 0.30 是 +1.48 分/局(t=2.0),0.25 是 +1.02,0.15 只有 +0.17(SE 0.46)。
不取坡顶的理由:`isBossPlay` 的语义是「钢板」,护底 / 甩牌 / 领出三处都拿它当**确定性**用,
而 0.30 会把「对手有对的概率 26%」、也就是只有 74% 胜率的对子标成钢板。
那是 v0.5.1 `endKittyWeight` 的同款错误 —— 让一个数值去买它语义上不该买的东西。
**那 1.5 分的正确取法是把 `pPairAbove` 当连续概率接进打分,而不是放宽布尔阈值。** 记进下一轮。

**副色甩牌不毙:复查后基本不是 bug。** 250 局插桩,对手甩牌带分、我这门断门、主牌张数够的局面共 154 次:

| | 次数 |
|---|---|
| 结构上根本赢不了(对子/拖拉机要同结构的主对,手上没有) | 51 |
| 毙掉了 | 86 |
| 生成了能赢的候选却没选 | 9 |
| 结构凑得出却没生成候选 | 8 |

去掉规则上不可能的 51 次,**实际毙下 86/103 = 83.5%**。
实战中看到的「不毙」最可能是两种规则约束:对手甩的是对子/拖拉机而手上没有主对(必输),
或者那门还没断完(必须跟牌,轮不到毙)。剩下 17 次(8 生成 + 9 评分)量级小,单独记着。

自测 177 → **182** 项。

## v0.5.4(2026-08-14,仅测试版)—— 不埋副 A;断门有分默认就毙

真人反馈(水平高于 AI 一档)提了四条,先用插桩把四条全量化了一遍(250 局):

| | 实测(修改前) |
|---|---|
| ① 埋底埋掉副花色 A | 手上有副 A 的 218 局里 **42 局(19.3%)埋了** |
| ② 断门 + 队友暂大 + 台面有分 | 398 次机会共 4380 分,**只毙 49.5%**;按规则该毙的那档只有 48.1% |
| ③ 小牌探路 | 占全部领出 **23.5%**,其中 **61.0%** 丢掉牌权(全部领出的对照是 36.9%),送出 5960 分 |
| ④ 调王 | 「小主领出压不住场,吊不出」占领出 15.2%;送毙只占 5.4% |

**这一版修了 ① ②**(判据明确、能直接翻译成代码),③ ④ 是策略层改造,单列。

**① 副花色 A 不再埋。** `faceValue` 里副 A 原来只加 30 分,会被断门收益压过。
判据:A 赢不下一墩,只可能是有人在这门断门并毙掉它;开局各家 25 张、一门 12~13 张,
天然断门概率很低 —— 拿一个几乎确定的墩去换一个「可能」造出来的断门,不划算。
改成硬性阻力 `buryAceBlock: 200`。实测 19.3% → **0%**。

**② 断门 + 台面有分 → 默认就毙,这是规则不是打分。**
放着不毙唯一说得通的理由,是同时确定两件事:队友那张是本门在外最大,且后手对手没人断门。
这两条正好是 `followCtx` 里已经算好的 `curBoss`。**合取不成立就毙。**
旧版这一整组候选被 `if(!partnerWinning)` 挡在门外,连生成都没有。
实测「按规则该毙」那一档 48.1% → **89.4%**(剩下的是加成压不过的极端情形,
比如手上唯一的主就是护底那张 —— 保留成强偏好而非硬覆盖是有意的)。

配对对照 +0.75(SE 0.76),守底指标略退(底≥10 分档 27.4% → 31.4%)—— 毙牌要花主,
这是真实代价,量级小且在噪声内,先接受。

### 关于自对弈台架的局限(这一版最该记住的东西)

反馈里有一条是对台架本身的:**同一个 AI 自己跟自己打,量不出「需要队友/对手够强才能兑现」的策略。**
这条是对的。台架的配对+交换阵营设计能干净地差分掉牌面质量(同一副牌两个变体各打双方),
同队两家也确实是同一版本 —— 这两点没问题。问题在第三点:
自对弈回答的是「对着自己的副本这个改动是否更好」,而调王这类配合型技术,
四家都不会时它换不来任何东西,必然显示为中性。

**之前的错误不是跑了自对弈,是拿自对弈的中性结果去否决有第一性原理支撑的改动。**
往后的口径:判据明确的规则类改动按规则改,自对弈只当**防回归护栏**,不当裁判。

## v0.5.3(2026-08-14,仅测试版)—— 调王轮三家不接:查清了,但只修得动一半

报障:调王轮第三家放任末家用 10/K 拿分拿牌权。依据是将牌门有 50 分(级数牌不是 5/10/K 时),
人均 12.5 分,没人争的将牌墩大概率直接送分。

**先量化。** 250 局插桩,按领出主牌的强度切开(low = 在外一半以上压得住它,即真·调王):

| | 墩数 | 1家 | 2家 | 3家 | 4家 |
|---|---|---|---|---|---|
| **调小主** | 272 | 13.2% | 24.3% | 27.6% | **34.9%** |
| 领大主 | 886 | 64.4% | 10.7% | 11.4% | 13.4% |

真·调王墩里末家赢下 34.9%、拿走 370 分。报障属实,频率也对得上(约 1.1 个/局)。

**再拆打分。** 把「第三家能接却没接」的局面逐项拆开(接过来 − AI 实选):

| 阶段 | 例数 | EV差 | 牌权差 | 机会成本差 | 合计 |
|---|---|---|---|---|---|
| 中盘 | 34 | +3.22 | +0.85 | −5.71 | **−1.64** |
| 收官 | 62 | +4.68 | +0.80 | **−51.50** | −46.02 |

两个完全不同的问题。

**收官那一半是我上一版改出来的,而且我修不动它。**
`endKittyWeight` 在 v0.5.1 被抬到 5.0,底估 16 分时护底加值 = 16×2×0.85×5 ≈ **136 分**,
而整个打分器的典型量级是个位数 —— 它不再是权重,是一条硬约束,收官阶段护底那张牌的
任何用途都被一票否决,包括拦住末家拿走一个带分的将牌墩。量纲上也讲不通:
守最后一墩这个期权不可能比它保护的那笔底分更值钱。

于是把它改回量纲正确的版本(权重 ≤1、封顶在底分那笔 stake 上、爬升曲线用 gamma<1
提前给足、代价按边际算再乘 `reserveHold`)。**实测每局 −2.10 分(SE 0.55, t=−3.8),
庄家丢掉最后一墩 22.4% → 44.0%** —— v0.5.1 的收益全赔回去。
也就是说:那个「错误的」大数值,买到的正是收官阶段的守底纪律。
**结论:数值原样留着,收官阶段这条不动。** 代码里留了 `reserveMarginal` 开关记录这次尝试。

**中盘那一半修了,但要说清楚它的依据不是棋力。**
中盘只差 1.64 分,而且默认根本不生成这个候选(整组吃/毙候选被 `if(!partnerWinning)` 挡着)。
打开之后配对对照是**纯中性**:+0.04(SE 0.11)、+0.40(SE 0.42)、−0.13(SE 0.14),三批都贴着 0。

既然自对弈说它不要钱,就不按棋力定档,按「给人当队友」定档 —— 因为
**自对弈结构上量不了这件事**:两边都是同一个 AI,它从不像人那样带着意图去调王,
所以「队友调了王、我该不该接」这个配合问题在自对弈里根本不会被出题。
`overPartner` 从 +3(罚分)改成 **−4(加成)**,只在**非收官**的将牌墩生效
(`takeOverScope: 4`)。这一档把「三家能接却没接」从 34 例压到 15 例,再往下(−8)只到 13,饱和。

**没解决的**:收官阶段的同类局面(守底纪律优先);断门毙分那一支仍是负的,保持关闭。
真要两头都要,得先把 `certainty` 重标定、并把收官护底从「硬约束」换成一个量纲正确
又同样有效的表达 —— 那是下一轮的事,不是拧一个参数能拧出来的。

自测 177 项全通过,守底指标未回退(底 ≥10 分档 30.0% → 30.0%)。

## v0.5.2(2026-08-09 测试版 / 2026-08-10 正式版)—— 必打关卡只实现了一半

> 2026-08-10:v0.5.1 + v0.5.2 一并合入 `index.html`,正式版从 v0.5.0 直升 v0.5.2。

报障:闲家本级 2,一局抢到 150 分后直接跳到打 3,没有必打 2,设置里必打关卡是开着的。

一行就能复现:

```
E.advanceMatch([2,2], 1, {defendersWin:true, defenderLevelsUp:1}, [2,5,10,13])
→ {levels:[3,2], gateStopped:null}
```

**「必打」有两条判据,旧版只做了第一条。**

1. **不可跳级** —— `clampAtGate` 只拦**跨过**关卡的升级(`g>from && g<to`),这条一直是对的。
2. **必须打过** —— 缺的是这条,而且缺得不显眼。旧注释写着「落点正好等于关卡不算跳过,
   那本来就要在该级打一局」—— 这句话**对庄家成立、对闲家不成立**:
   一局打的是**庄家方**的级数,所以一支队停在 2 上当闲家、上台升一级,
   是从没打过这一关就过去了。

现在过关的条件是本队在该级**坐过庄**。`advanceMatch` 多收一个 `played`
(每队已坐庄打过的最高级数),停在关卡上却没坐过庄就原地不动、只换庄不升级。
不会卡死:升级的一方下一局必坐庄(守住是对家连庄、上台是自己坐庄),下一局就补上这一关。
`played` 由调用方持有,引擎自己不留状态;不传 `played` 时退回只挡跳级的老行为。

**顺带修掉弹窗自相矛盾。** 「上台升 N 级」原来读的是 `scoreRound` 的名义级数,
被关卡拦下时会和下一行的「本次不升级」打架。改成先算整场推进、再按**实际生效**的级数写文案。

规则决定书补了「必打关卡」一节 —— 之前 `RULES.md` 里根本没有这条规则的定义,
两条判据只散落在代码注释里,这也是第二条能缺这么久的原因之一。

自测 168 → **177** 项(新增 9 条:卡住、放行、非关卡级、庄家不卡自己、
`played` 记录、不传 `played` 的兼容、补打一局后过关)。
另在浏览器里跑了 5 个端到端场景(报障局面、补打后、非关卡、庄家守住、关卡关闭)。

## v0.5.1(2026-08-08 测试版 / 2026-08-10 随 v0.5.2 一并进入正式版)—— 护底权重、竖屏出牌区被压塌、悬停态

需求列表「问题修复 / 智能提升」两个区的一轮清理。

**AI:护底手被提前打光(智能提升第 1 条)**

`endKittyWeight` 从 **0.55 → 5.0**。底 12 分、还剩 3 张时,旧值给护底手的加值只有
`12×2×0.5×0.55 ≈ 6.6` 分,压不过「现在就把这张钢板兑现」的当墩收益 —— 于是庄家
四成的局把保底手提前打光,最后一墩连人带底一起送掉。v4 已经把 `PHASE_K.end`
改成随底分走,但**这个折算比例本身**没跟着抬,等于闸门开了、水管没换。

扫参 1.0 → 7.0 是一条先升后平的曲线,4~7 之间是平台,取中间的 5.0:

| endKittyWeight | 净收益(分/局) | 庄家丢掉最后一墩 |
|---|---|---|
| 1.0 | −0.03 | 34.7% |
| 2.5 | +1.15 | 29.3% |
| 3.5 | +2.08 | 25.3% |
| **5.0** | **+2.62** | **21.3%** |
| 7.0 | +2.43 | 19.3% |

**最终对照(300 副配对 / 600 局,样本外种子):+3.02 分/局(SE 0.67, t=4.5),
600 局零违规。** 另一组样本外 300 副:+2.17(t=3.6)。

「丢掉最后一墩」按埋底分分层看才有意义 —— 收益全在「底里有分」那一档,
而它占了 71% 的局(庄家平均埋 12 分):

| 埋底分 | 局数 | 基线 | v0.5.1 |
|---|---|---|---|
| **≥10 分** | 213 | **51.2%** | **20.2%** |
| 1–9 分 | 63 | 47.6% | 27.0% |
| 0 分 | 24 | 37.5% | 45.8% |
| 合计 | 300 | 49.3% | 23.7% |

合入正式版前又跑了两批(n=100 / n=200)。**批与批之间的波动不小** ——
四批分别是 +2.17(SE .60)、+3.02(SE .67)、+2.17(SE .86)、+0.34(SE .85),
逆方差池化 **+2.09 分/局(SE 0.36, t=5.8)**。
单批 +3.02 那个数不该单独引用;而「丢掉最后一墩」这个中间指标在四批里都稳定下降
(最新一批 底 ≥10 分:42.3% → 20.8%),说明机制本身是可靠的,波动来自净分这个末端指标本身噪声大。

底 0 分那档反而略差,但 n=24、差 2 局,在噪声里;方向上也是对的 ——
`endKittyWeight` 乘在 `kittyPts` 上,底分为 0 时整项消失,它不会去护一个空底。
这个参数买到的不是「一律死守最后一墩」,是**选择性**:底肥就留手,底瘦就照旧兑现。

**AI:接过队友这一手 —— 实测为负,挂开关关掉(`takeOverScope: 0`)**

需求列表另两条(队友调王没人接、队友副花 K 暂大而我断门不毙,末家一张 A/10 全收走)
指向同一处实现:整组「吃 / 毙」候选被 `if(!partnerWinning)` 挡在门外,队友暂大时**一张都不生成**,
AI 只能在 贴分 / 跟小 / 垫牌 里挑。候选补上了,配对对照却各档都掉分 ——
全开 −1.0、限将牌墩 −0.5、限断门毙分 −0.3,收紧确定性门槛到 0.5 才回到 −0.05。
原因大概率是 `certainty` 系统性低估队友(校准表 模型 0.692 / 实际 0.782),这条闸门开得太频。
代码与消融开关都留着,默认 0;要吃下这块收益得先重标定 `certainty`,而不是在它上面再加一层。

**顺带修掉的一处评分泄漏。** `aiChooseFollow` 的候选去重遇到重复直接丢掉后来的那个 ——
同一手牌被两条理由生成时,先入的那条会顶替掉本该以另一套口径打分的同一手牌。改成留分高的。
单独跑是 +0.05(SE 0.39),行为中性,但没有它上面那组消融数据全是脏的。

**竖屏出牌区被压塌(问题修复第 3 条)**

「发牌时手牌遮挡亮牌区、发完才归位」不是地址栏跳变(v0.4.2 按那个假设改成了 `svh`,没修掉)。
真身是布局:发牌阶段 `#dealZone` 预留两行,加上侧栏按 `0.9fr` 拿走近四成高度,
把 `#table` 的中间行压到 **20px**,而 `.played` 里的牌是定高的(`min-height:var(--cardH)`,70px 上下),
于是整块溢出到提示行和手牌上;发牌一结束预留释放,空间回来就自己好了。
现在竖屏给出牌区一个真正的下限(三张牌高,它是 3 行网格),把 `min-height:0` 收回来让下限能往上传,
侧栏降到 `0.6fr`;机身矮于 700px 时再把牌本身缩一号,免得记录面板被挤没。
375×600 / 375×812 / 1280×800 三档量过,溢出为 0。

**手牌悬停态(问题修复第 2 条)**

`@media (hover:hover) and (pointer:fine)` 挡不住:平板 + 妙控板、带触屏的笔记本、
以及若干安卓浏览器都把自己报成 `hover:fine`,手指点上去照样留下悬停态 ——
就是「取消选择后卡在半抬起」「旁边一张莫名其妙抬起来」。改成由**实际输入设备**决定:
`html.hasHover` 跟着最近一次 `pointerdown` 的 `pointerType` 走,手指一碰就摘掉,鼠标一动就装回来。

**自测** 168 项全通过。其中「领出钢板(大王对)先收」改成了「收官底肥:王对留着守最后一墩」——
那手牌是 王王 + H4 + C7,王对领出去占两墩、剩下两张全是输牌,最后一墩必丢,
底里那笔翻倍的分跟着送出去。旧断言本身就是错的,不是迁就实现。

## v0.5.0(2026-08-05 测试版 / 2026-08-08 正式版)—— AI v4:牌权、收官底分、造反判据

> 2026-08-08:测试版试玩通过,原样合入 `index.html`,正式版从 v0.4.2 直升 v0.5.0。
> 一并带过去的还有引擎导出面的修复(见文末「引擎导出面」),`index.html#test` 恢复可用。


一轮实战复盘 + 200 局插桩自对弈带来的修正。这一轮先量化再动手,三个实测数字定了优先级:

| 指标 | v3.1 实测 | 含义 |
|---|---|---|
| 庄家丢掉最后一墩 | **39%**(78/200 局) | 平均埋底 11.6 分 → 每局白送闲家 **9.3 分**(已含 ×2) |
| 将牌领出中的低将牌 | **37%**(562/1529) | 领出评分把低主当普通废牌「探路」 |
| 将牌墩由领出者赢下 | 80.3% | 「能吃却跟小」只有 3~15% —— 毛病不在跟牌侧,在领出侧 |

第一条单项就大于 v3.1 相对 v3.0 的全部收益(+8.2 分/局)。

**修掉的 bug**
- **`canOverride` 会让人反自己。** 它只比 strength、不看座位,而 `aiDeclDecide` 拿它当唯一的
  合法性闸门。实测:自己亮了♠单张之后,改亮♥对 / 小王对反无将 / 大王对反无将三种全部返回
  `true`,对应的惩罚却只有 −9 分,压不住无将 58 的抢摊红利 —— AI 真的会反自己。
  现在同一个人想加强只有「加固」一条路(同花色单张→一对)。人类玩家那两颗按钮也一起修好了。
- **王对的 Future 被算了两遍。** `jokerPairHold` 和新的 `trumpHold` 都在给「全场最大」计价,
  加起来把王对的留手价值顶到 60+,收官该兑现的时候也兑现不出来。现在 `jokerPairHold` 只记
  **成对溢价**。

**收官与底牌(冲那 9.3 分)**
- `PHASE_K.end` 从常数 0.15 改为**由底分决定**:底分 0 → 0.15(该兑现就兑现),
  底分 20 → ≈1.05(收官反而比中盘更该留控制牌)。旧版偏偏在底牌翻倍最该被在意的时候,
  把留手价值几乎抹平了。
- 「最后一墩争夺」的起算点 `endHorizon` 同样随底分提前(4 → 最多 9 张),
  庄家扣了 20 分进底,从中盘就该开始规划留一手全场最大。
- **护底价值只发给一手**(`reserveUnit`):手上三个钢板单元,留一个守最后一墩就够,
  另外两个该在收官前兑现。旧版发给每一个,于是它们互相锁死、谁也不肯先出。
- 闲家倒推底分不再是「未见分均匀摊到 8 张」。庄家的埋牌行为本身是信息:他绝不埋主,
  所以在外的主牌要从分母里剔掉;他又倾向不埋分,分的密度再打一折。

**牌权与吊主(领出侧重做)**
- **`scoreLeadPlay` 第一次有了 Tempo 项。** v3 的 `tempoValue` 只在跟牌里用,可领出恰恰是
  牌权最该计价的地方。权重 0.35 时实测只改变 **2.1%** 的领出决策 —— 又一个形同虚设的参数;
  提到 1.0、同时把 `bossBase` 从 56 降到 40(原来那 56 里有一截其实就是「拿住牌权」,
  现在改由手上剩余待兑现单元算出来)之后,决策改变率到 6.1%。
- **吊主单独成类。** 判据只有一条:我的主牌能不能耗过两个对手的主牌
  (`drawTrumpValue`,按在外主牌数 × 对手手牌占比估)。奖励**只给非王的主牌钢板** ——
  用王去吊,吊出来的主一样多,却把全场最后的控制权提前花掉了。
  写死的 `score:46 主力雄厚,吊主清场` 那颗特例候选删掉了:常数 46 无条件压过所有其他候选,
  而且根本不看对手还剩几张主。
  > 中间踩了一个坑值得记:一开始给**所有**主牌领出都加正的 edge 奖励,实测单调掉分
  > (unit 0→+2.0、4→0.0、8→−2.8)。因为那是一个跨候选的常数偏移,把低主领出也一起抬上去了,
  > 正是要修的那个毛病。现在非钢板的主牌领出只取 edge 的负半边,再加一个固定折扣。
- **主牌的留手价值改用相对刻度。** 旧版是 `2.5 + ordIdx×0.5`,按牌面序号线性给,
  「全场第三大的主」和「全场第十大的主」差得很近,于是 AI 会为了护住王对先把主 A、主 K 撒光,
  再在没牌权时被迫打王对 —— 「过早打大王对、把自己消耗完」有一半根源在这里。
  现在按**在外还有几张压得住它**算(`trumpHold`)。
- 领出主牌时这一墩能收的分打 0.45 折:主牌是所有人都攥着不放的一门,
  副牌那套「1/3 在队友手里、他会送过来」的估计在主牌上明显偏高。

**造反 / 无将**
- **造反改增量判据。** 旧版是绝对门槛 `V(opt) ≥ θ`,而 `V(opt)` 只回答「以 opt 为主时我这手牌
  好不好」;造反要回答的是「换成这个主,比现在这个主好多少」。旧版从头到尾没有算过现状,
  于是会出现「手握长♠、却因为摸到小王对去反无将」。现在减去 `declHandQuality(现主)` 作基线,
  抢庄红利 / 暴露代价 / 谁已亮主留在增量侧,造反另加一档门槛(打乱一个已成立的主色有摩擦成本)。
- **无将的 Fit 换成牌力占比。** 无将局主牌总共只有 12 张,「期望长度」不是好尺子 ——
  握大王对 + 两张级牌(4/12)和握四张级牌无王(4/12)在张数上完全等价,实战里天差地别。
  改成 大王 1.0 / 小王 0.75 / 级牌 0.4 的加权占比。「无将对闲家更有利」也从只在级数 5/10/K
  时生效的 +0.35,提到 Fit 层的常项。

**队内配合**
- **送毙**(主打队友断门)从兜底分支的一句 `+8` 变成一条算出来的期望收益:
  队友确实缺这门 × 对手不缺这门 × (这墩能收的分 + 牌权留在我方)。两个概率都来自已有的
  `makeVoidProb` / `maxHoldIn` 软推断,不是新造的假设。带开关 `AIP.feedRuff`,便于消融。
- **调王(小主换牌权)本轮没做**,原因写在 AI-DESIGN §7:它需要「对手每门的持有**区间**」
  这一层模型,现在只有上界。按设计文档自己的教训,与其上一个猜出来的参数,不如先欠着。

**实测(交换阵营的配对对照,350 副牌 / 700 局,含亮主阶段)**

| 对照 | 每局净分差 | 胜局占比 |
|---|---|---|
| v4 vs v3.1(全部四个组件) | **+6.44**(SE 1.81, t=3.6) | **53.7%** |
| 只换领出 `aiChooseLead` | +4.25 | 54.3% |
| 只换亮主 `aiDeclDecide` | +1.18 | 51.5% |
| 只换跟牌 `aiChooseFollow` | +0.68 | 50.8% |
| 只换扣底 `aiDiscard` | 0.00 | 50.0%(本轮没动它,这个 0 是台架的自检) |

同批 200 局插桩复测:庄家丢掉最后一墩 39% → **35%**,底分损失 9.3 → **7.5 分/局**;
将牌领出 1529 → 1235 次;开局阶段的主牌消耗占比 41% → **6%**。

引擎自测 150 → **165** 项,全通过。

### v4.1 补丁 —— 跟牌是位置决定的四个不同问题

「将牌墩没人领牌权」这条一开始被误诊成「领出小主是坏棋」。不对:**小主探路是正当的牌权
出让策略** —— 保存实力、收益和概率都不确定时(队友可能没分、自己赢墩概率不高),
把牌权让出去往往是必要的。出牌的目的从来不是「用自己的牌赢下这墩」,而是
「让本队拿到当下与未来的最大收益,以达成局末分数目标」。真正的毛病在后三家,
它们处境完全不同却共用同一套候选:

- **第三家(对家)的托底职责没法表达。** `minWinFollow` 只给**最省的吃法** ——
  压过当前最大就收手。可第三家的职责之一是**无论如何别让末家用一张 10/K 直接把这墩带走**:
  外有 K 就得打 A 以上,外只剩 10 打 J 以上就够。「压过了、但压得不够高」是 v3/v4.0
  根本没法表达的一个选择。新增 `blockWinFollow` 作并列候选,
  并且**只发给「队友已出过牌、身后还有对手」的那一家** —— 我是第二家而队友坐末手时,
  该托底的是队友,烧掉自己的 A 是越位。
  实测第三家该盖没盖的比例 56.9% → **43.9%**;配对对照里值 +0.2~0.3 分/局。
  > 数字小,但要注意 **AI 打 AI 会系统性低估这类错误** —— 双方都不掏那张 K,
  > 错误互相抵消。对面换成会打的人,代价高得多。它修的是**表达能力**,不是权重。

- **第二家「推断队友握绝对大牌就直接贴分」—— 试了,失败了,记为负结果。**
  `pTeamWin` 里那个 0.32 确实表达不了这件事,于是照 `pSurvive` 的口径写了 `pPartnerTakes`。
  实测随采信比例单调掉分:0 → **+8.2**、0.2 → +6.8、0.35 → +5.7、0.6 → +6.3 分/局。
  根因是算错了对象:它回答「队友手上**有没有**压得住的牌」,而决策需要的是
  「他**会不会**打出来并守住」。前者在中盘轻易超过 0.5,于是 AI 系统性地把责任推给队友。
  要做对需要队友持有的**下界**,和「调王」卡在同一个地方(§7 对手建模)。
  机制和开关 `AIP.pPartnerCalc` 留在代码里,默认 0。

### v4.2 补丁 —— 三处返工

**(1) 护底该留哪一手,选错了类。** v4.0 的排序键是 `(主?1e6:0) + top×100 + size`,
在副牌里 `top` 压过 `size`,等于**优先挑副牌单张去护底** —— 恰好是最不可能守住的那一类:
任何一个对手在这门断门,一张主就毙掉了。副牌**对子**要一对主才毙得掉,副牌多张要凑出
同结构的一手主,但多张又多一个前提 —— 得在倒数第二墩就握着牌权,否则领不出来。
真正常规的护底是主牌大小王 / 主色级数牌 / 主牌对子。
现在改成算「能撑到最后一墩的概率」`reserveHold`,毙牌按张数结构折算,副牌再打一个
「还得先有牌权」的折扣。**庄家丢最后一墩 35% → 33%,底分损失 7.5 → 7.1 分/局,+0.5 分/局。**

**(2) 去掉「第二家烧 A 是越位」那道闸门。** v4.1 把 `blockWinFollow` 限定给
「队友已出过牌」的那一家,理由站不住 —— 第二家先出 A / 级数主完全可以是**拿牌权**:
最好的结果是末家贴分或第三家被迫送分,退一步至少换来往后几墩的领出权。
该由打分权衡的事情不该用位置规则砍掉候选。闸门去掉后 168 项自测**一条没破**
(包括「用最省的牌吃(Q 不动 A)」和「队友坐末手时不烧 A」)—— 说明评分本来就会在
那些局面下拒绝烧 A,那道闸门纯属多余。

**(3) `tempoValue` 从直接求和改为贴现和。** 牌权只保证下一墩由我领出:最好的那一手能立刻
兑现,第二好的要等我再拿一次牌权。按价值降序乘 0.8ⁱ,曲线自己收敛,`tempoCap` 那个硬截顶
不再需要把好手压平。

**顺带查清了 v4.1 那条负结果到底错在哪** —— 不是「推断失误被第三家压制」,也不是
「末家有分不掏大牌」,而是两层:

- **成交量。** 采信记牌后贴分频率 23.6% → 32.1%,贴分成功率确实从 37.5% 涨到 46.9%
  (推断是有信号的),但代价是**放弃了 321 个本来能以 ~70% 赢下的墩**,还多送 7.5 分/局。
  贴分的盈亏不对称,47% 远不够本。
- **校准。** 把模型给的 p 和实际频率对了一遍:「对手暂大」模型 0.255 / 实际 **0.183**,
  「队友暂大」0.692 / 0.782,「over/ruff」0.727 / 0.696。
  **那个 0.32 本来就偏高**,不是偏低。可往低调也掉分(0.22→+7.0、0.14→+7.6)。
  结论:**它已经不是概率,是一个被调过的权重** —— `scorePlay` 的 gain/loss 各项都是围着它
  标定的,单独把一个输入改准反而打破了其余各项吸收掉的偏差。要修得整体重标定,
  校准表已经留在 AI-DESIGN §5.1 当目标。

**最终对照(400 副牌 / 800 局):含亮主 +5.67 分/局(SE 1.76, t=3.2)、胜局 53.0%;
不含亮主 +8.73 分/局、胜局 55.4%。** 单组件:领出 +4.46、亮主 +1.22、跟牌 −0.64(噪声内)。
引擎自测 **168** 项全通过,400 局冒烟零违规。

**引擎导出面。** 界面块里另抄了一份过时的 `ENGINE` 名单(缺 `makeMemory`、`aiChooseFollow`、
`bossUnits` 等三十来个),`#test` 一进去就抛错 —— 长期漂移,不是这轮改出来的。
现在引擎块导出唯一一份 `ENGINE`,浏览器 `#test` 与 node 无头测试共用同一份,两边不会再各走各的。

## v0.4.2(2026-08-03)—— 手牌选中态、发牌期布局抖动、新开一局

**问题修复**
- 手牌取消选择后有时卡在"半抬起"、有时连带旁边一张也半抬起:根因是 `:hover`
  给非选中牌一个 -6px 的预览抬起,而触屏没有真正的"移开"事件,点完之后浏览器经常
  把 `:hover` 悬在被点的(有时因为叠牌命中了旁边那张)牌上不放,和 `.sel` 这个真实
  选中态叠在一起,看起来忽高忽低。选中态完全交给 `.sel`,`:hover` 只在
  `(hover:hover) and (pointer:fine)`(真鼠标/触控板)下生效,触屏不装这条规则
- 移动端发牌期间手牌区偶发盖住亮牌/操作区、发完牌才归位:`#app` 用的
  `100dvh` 会跟着地址栏收起/展开实时变,手机上发牌那十几秒正好可能撞上地址栏
  自动收起的那一下,导致 `#app` 高度跳变、内部布局跟着抖一下。改用稳定不变的
  `100svh`(地址栏展开时的最小高度)钉死,不再跟着地址栏收放重排

**功能**
- 设置菜单新增"新开一局(重置整场)":整场级数/庄家全部归零,和"下一局"那种保留
  级数的推进不同,是破坏性操作,用警示色隔开、点击需二次确认

引擎自测仍是 150 项,全部通过(本轮只动界面/CSS,未碰引擎与 AI)。

## v0.4.1(2026-08-03)—— 非 AI 需求清理

按需求页"待行动区"逐条对齐:

**问题修复**
- 移动端手牌区选中牌上半部分被遮挡:根因不是图层顺序,是 `#hand` 同时设了
  `overflow-x:auto` 和 `overflow-y:visible`——浏览器有个反直觉规则,一个轴不是
  `visible` 时另一个轴的 `visible` 会被当成 `auto`,选中牌 `translateY(-14px)`
  抬起的部分因此被当成"溢出"裁掉。这层 `overflow-x:auto` 是上一版加的"横向溢出
  保险",但动态重叠算法本身已经保证塞得下(见 `fitHandRow`),保险没必要冒裁切的
  险,改回 `overflow:visible`
- 设置菜单里选项文案一长(如"沪语版(术语切换)")就把整个面板撑出屏幕:面板此前
  只设了 `min-width`,没有 `max-width` 兜底,改为上下限锁同一个值

**功能**
- 出牌区(牌桌四个方位)多张牌(对子/拖拉机/甩牌)不重叠会被挤到看不清或溢出:
  改用和手牌同一套视觉语言,后一张压前一张只留左半边
- 默认语言改沪语,语言项从状态栏的一个勾选框搬进设置菜单、改成下拉列表(为后续
  加语言预留位置,英语暂缓)
- 设置菜单完成/取消/恢复默认冻结在顶栏(`position:sticky`),不用滚到底才能操作;
  新增真正的"取消"——开菜单时快照一份,点取消整份连同已存的 localStorage 一起
  复原,而不是只清内存(否则关掉重开又会看到取消前改过的值)
- 设置菜单按分区重排序,预期改动频率从高到低:显示与音效、节奏、教练、规则
  (规则是几个人说好怎么打就定下来的房规,最不常动)

**确认已完成、无需再做**
- "亮主有对子时不必须亮对":`declOptions` 在 AI v3 里已经同时生成亮单与亮对两个
  候选,`canOverride` 在无人亮主时对两者都放行,UI 早就会分开出两个按钮

对局记录/出牌记录合并、英语支持留在需求页的"待讨论区",本轮不做。

## v0.4.0(2026-07-31)—— AI v3:三个阶段全部公式化

AI 的三个决策阶段(亮主/造反、扣底、出牌)第一次用同一套量纲写成公式:所有分值都折算成
「分」,期望收益与机会成本可以直接相减。可调参数集中到 `AIP` 对象。详见 `AI-DESIGN.md`。

**亮主/造反(新增 `aiDeclDecide`)**
- 从「按牌型强度掷骰子」改为打分 + 门槛:抢庄红利、手牌契合度、级数 5/10/K、庄家归属、
  谁已亮主、必打关卡、暴露代价七项加权,门槛随已发牌数递减
- 手牌契合度改用超几何期望外推,自带小样本收缩 —— 旧版的线性外推会让 AI 因为
  「第一张就摸到主」而推断出满手主
- 无将不再无脑亮:公式里有一项基础不情愿,要靠将牌数量优势或副牌面质量来抵消

**扣底(`aiDiscard` 重写)**
- 改为贪心逐张选,每选一张重算断门增益(断门是集合性质,一次性排序算不对)
- 新增「牌权保持链」`bossChain`:手握 A、KK 时那几张低牌的断门贡献按 0.55 折价计入
- 分牌不再是一律不埋,而是比较「持有丢分风险」与「扣底丢分风险」(底牌翻倍、
  但只在丢掉最后一墩时兑现),按差值决定
- 无将/少主时断门价值按 `easeEnds` 曲线折扣(中间平缓、两头陡峭)

**出牌(评分模型重写)**
- 从「能不能赢这墩」改为「这一墩净赚多少分 − 这手牌留着的未来价值」
- 候选先定性(跟大/跟小/跟分/垫分/垫牌/毙牌)再算分,新增「垫牌」候选
- 「队友暂大不是稳大」不再是 0.9/0.35 两个常数,而由记牌算出的存活概率 `pSurvive` 给出

**修掉的四个具体毛病**(均已加断言,不会再退化)
- 一局前期直出王对:机会成本项让它在开局/中盘几乎不可能被选中,收官则该兑现就兑现
- 队友暂大就无脑贴分:后手还有对手且队友不是钢板时,改为跟小
- 无分墩把低将牌当废牌垫掉:根因是 `ordIdx` 在各门内独立编号,新增 `globalIdx` 让主牌恒高于副牌
- 副门不会留 A/K:根因是 `cheapSort` 只看「大小 + 是不是分牌」,A 因为不是分牌反被判成最便宜;
  新增 `controlPremium`,手持 A、K、10 被对手领 A 压死时改为出 10、留 A+K

**教练模式**
- 「失误」门槛重标为「这一手大约亏了几分期望分」(高 6 / 中 12 / 低 22);
  领出的候选分差量纲约为跟牌的 4 倍,先归一再比,修掉「对领出唠叨、对跟牌失明」

**三个「参数形同虚设」的 bug** —— 都是机制写了但压根没在起作用
- 【critical】`voidValue`(断门价值)完全无效:断门只在埋掉一门最后一张时兑现,
  对贪心是不可见的台阶,前几张的边际增益全是 0,于是永远走不到那一步。
  实测把它从 10 调到 120 埋底结果一个字不变。改为把整门的断门价值摊到需要埋的那几张上
- 【critical】甩牌无人能拦:引擎一直支持用同张数、同结构的将牌整体压制甩牌,
  但 `minWinFollow` 第一行就是 `if(lead.type==='throw') return null`,AI 从不生成这个候选。
  新增 `ruffThrow` 按甩牌的组件结构去主牌里凑
- 毙牌存活率算错:`pSurvive` 漏了「对手要盖过我的毙牌,他也得先在领出门断门」这个前提,
  算出来 0.03,于是 AI 几乎从不毙牌。补上后回到 0.7 级别

**三处口径错误**
- 无将 + 级数是分牌时的加成方向搞反了:原来按「无将局级牌本来就都是主」减半,
  但级牌在有主局和无将局同样都是主,减半没有依据。真正的差别是稀释度 ——
  无将局主牌总共 12 张、其中 8 张是分,即主牌里三分之二是分;有主局那 8 张被摊薄在 36 张里。
  改为无将 ×1.35、我方是闲家再 +0.35
- `easeEnds` 的分母凭手感写了 11/6 且与文档不符:改为按「再多主牌也换不来更多毙牌机会」的
  饱和点推导,有主 12(人均 9 的约 1.35 倍)、无将 5(人均 3;12 是全场总数,单家拿不到)
- 扣底 PointΔ 的「门长于平均」按 33 张算、基准还写死 6:改为按埋完后的手牌估,
  基准跟主牌占用量走 —— (25 − 主牌数)/3

**三个新机制**
- **阶梯型动态目标**:局末目标不是「攻守 80 分」一根线,而是 0/40/80/120/160/200 一整排
  (引擎结算口径本来就是阶梯)。抢 40 分免被跳 2 级、抢 120 分多升一级,都可能取代
  「够 80」成为现实目标。`pointWeight` 改为对每条临界线放高斯包取上包络
- **从出牌方式反推缺门**:他跟牌时若既没有压制意图、也没有贴分动机,会出本门最便宜的牌 ——
  于是「他出了 X」等于「他没有比 X 更便宜的本门牌」,这是硬演绎不是概率。
  队友暂大时改用贴分序(他出了不带分的牌 ⇒ 本门没有分牌)。实测某例中对手该门持有上界
  从 20 张收到 2 张、断门概率 0.07 → 0.76。只在领出单张时采信(多张受跟牌义务约束)
- **牌权价值**:低主毙牌的决策点是「牌权归我 vs. 给暂大那家」哪个好,模型此前完全没有这一项。
  新增 `tempoValue`,并对对子/拖拉机加易碎溢价(飞机大炮等不起,该早兑现)。
  注意只算打完这一手之后还剩的单元,否则与 `futureValue` 重复计价(第一版就栽在这)

**亮主细化**
- 有对并非必亮对:`declOptions` 现在同时提供亮单与亮对。亮单不暴露、留加固后路但易被反掉;
  亮对难被反但摊牌。信心不足想试一手 → 亮单,手牌够好 → 亮对
- 加固改为走打分:比较此刻「亮对」与「维持单张」的分差,不再是一合法就 90% 加固
- 无庄抢摊红利提高(有主 85 / 无将 58),无将的「基础不情愿」在无庄盘打三折
- 后手加分 `laterPoints` 从四个拍死的常数改为按记牌估:这门在外还剩多少分、谁还没出牌、
  他缺不缺这门。修的是「第三家跟小、末家对手用一张 10 极低代价拿走这墩」算不出来的问题
- 收官底牌:`PHASE_K.end` 的固定折扣偏偏在底牌翻倍最该被在意时把留牌价值抹平。
  庄家直接读底牌,闲家用「未见分 × 8/未见张数」倒推

**验证**
- 引擎自测由 110 项增至 150 项,全部通过;300 局全 AI 自对弈零违规零崩溃
- 新增 v3 vs v2 交换阵营的对照实验与单组件消融
- 800 副牌 × 交换阵营共 1600 局:相对上一版每局多抓 **+8.2 分**(t ≈ 8)、胜局率 **53.0%**;
  相对 v2(v0.3.0)每局多抓 **+10.8 分**、胜局率 52.3%
- 扫参结论:目标函数在合理区间是平的(`voidValue` 62~100 净分差 9.8~9.9),
  唯一有梯度的是「某家占未见牌几分之一」的假设,而正解是按实际手牌数动态算而非常数

## v0.3.0(2026-07-31)

**问题修复**
- 【critical】出牌顺序错乱、一墩挤进 5 张、四家手牌张数不等:根因是赢墩后若玩家抢先出牌,残留的 `setTimeout(step,400)` 会和新触发的 `step()` 重复给同一 AI 排出牌,改为排前清掉上一个定时器、执行时复核墩面是否已被推进
- 手机竖屏完全不可用:`#msg` 提示语原本嵌在牌桌中心的十字网格正中格,竖屏下三行文字挤爆格子、和四家牌位互相重叠;改为把 `#msg` 移出 `#center`,变成 `#table` 自己独立的一行
- 手机横屏按钮区域离得很空:发牌互动区常年占两行高度(即使出牌阶段是空的);改为仅在发牌/亮主阶段生效
- 手牌一多就换行、换行后从牌桌中心区里扣高度:手牌区改为按当前张数与容器宽度动态计算重叠量,牌越多贴得越紧但永远单行,不再挤占牌桌

**规则**
- 新增"必打关卡"(不可跳级)规则:跳级区间内撞上关卡就停在最近一级,可在设置中配置关卡级数(默认 2/5/10/K)与开关

**界面**
- 底部按钮改为三区布局(发牌互动区 / 出牌主操作 / 庄家区),按钮统一尺寸,不坐庄或底牌未扣时庄家区按钮直接隐藏
- 出牌历史/复盘:墩序数独占一行,不再和四家的牌挤在一起
- 牌桌中心新增"当前谁大"提示(我方绿/对方橙红)及台面分数
- 记牌表口径澄清("别家还剩"不含自己手牌)、直接给出"该门最大牌能否吃"、补充剩余分牌明细与已确定断门的家
- 亮主等待由 1.8 秒延长到默认 10 秒(可调),等待期间可点"不亮"立即跳过

引擎自测由 101 项增至 110 项,全部通过。

## v0.2.0(2026-07-25)

**问题修复**
- 出牌区/亮主牌反复闪烁:`render()` 每次都重建 DOM 导致动画重放,改为内容未变不重建
- 级数将牌(副常主/无主常主)散排:同级牌加花色次级排序,归堆显示
- 完全造反后未真正重新发牌:改为走完整重发流程,并加重发次数上限防死循环

**规则调整**
- 低分/少主触发的行为改为可选(默认关闭):关闭 / 仅重新发牌(不抢庄) / 完全造反(重发+双方抢庄)
- 抽出 `dealerAfterDecl()` 明确"庄定盘恒不换庄",修掉此前庄定盘偶发被错误换庄的问题(根因:旧版低分造反判定每局无条件触发,与王对造反同时出现造成误判为换庄)

**功能**
- 新增设置菜单(开始界面 + 游戏内状态栏),规则与节奏参数持久化
- AI 出牌思考时间可调(默认 1–3 秒随机)
- 每墩留桌时间可调(默认 3 秒),可切换为手动点击进入下一墩
- 庄家可随时查看自己扣的底牌
- 局末复盘可查看本局四家初始手牌(局中锁定,防止透视)

**界面**
- 改为左右布局(牌桌居左、历史/笔记/教练居右),整页不再出现滚动条
- 牌面数字、状态栏、面板文字整体放大(牌宽改用 CSS 变量驱动缩放)

引擎自测由 96 项增至 101 项,全部通过。

## v0.1.0(2026-07-16)

首次发布。发牌抢亮、无主/造反、多局连打升级、教练模式(提示/失误反馈/复盘/记牌训练)、沪语术语切换。开局改为手动点击"开始游戏"。局末新增"复盘本局"入口;落地 AI v2(缺门推断、埋底优化等启发式改进)。
