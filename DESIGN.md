# 80分 启发式 AI —— 设计笔记

> **这一页只放现在成立的东西。** 逐版的推演过程在 [`NOTES/ai-journal.md`](NOTES/ai-journal.md),
> 被否掉的尝试在 [`NOTES/negative-results.md`](NOTES/negative-results.md),
> 量具与方法论在 [`NOTES/measurement.md`](NOTES/measurement.md),
> 参数登记表在 [`SWITCHES.md`](SWITCHES.md)。规则本身见 [`RULES.md`](RULES.md)。

> 一份文档说清三件事:AI 怎么想、每个决策的公式长什么样、公式落在代码的哪个函数里。
> 所有函数名都可以在 `index.html` 第一个 `<script>` 块里搜到。

## 0. 架构:三层单向依赖

```
界面(UI) ──调用──► AI ──调用──► 引擎(纯函数)
```

引擎只回答「什么是合法的」(`classify` / `isLegalFollow` / `resolveTrick` / `checkThrow`);
AI 只回答「合法之中选哪个」;界面只负责画和收输入。

AI 绝不自己重新实现规则 —— 所有候选都经 `isLegalFollow` 验证,不合法直接作废回退。
这保证 AI 永远不会出违规牌,也让规则改动(如参数开关)自动被 AI 遵守。

不做搜索(不穷举对手手牌),而是模仿人的决策结构。三个阶段共用同一条链:

```
信息收集 → 候选生成 → 打分比较 → 择优执行(每一步带理由)
```

「每一步带理由」不是日志装饰,而是教练模式的接口:教练 = 把同一套 AI 跑在玩家座位上,
展示它的首选和理由,再对比玩家实际出牌。因此 AI 与教练共享上下文构造器
(`followCtx` / `leadCtx`)和同一套评分函数,不可能出现「AI 这么打、教练说不该这么打」。

## 1. 三个阶段,链条相似而决策方式不同

| 阶段 | 时机 | 决策 | 入口函数 |
|---|---|---|---|
| 发牌 | 每发到一张牌 | 亮主 / 造反 / 不动 | `aiDeclDecide` |
| 整理 | 亮主确认、庄家拿到底牌后 | 从 33 张里扣 8 张 | `aiDiscard` |
| 出牌 | 每一墩 | 领出 / 跟牌 | `aiChooseLead` / `aiChooseFollow` |

贯穿全局的两个原则:

**目标是局末净分,不是赢下每一墩。** 己方够 80,或守住对手到不了 80。所有打分都服务这个
目标 —— 贴分、跟小、抢墩的取舍都是「这一墩的分值 vs. 这张牌留着的未来价值」的权衡。

**一切分值折算成「分」。** 5/10/K 的那个分。这样期望收益与机会成本能直接相减,
而不是靠一堆不同量纲的魔法数互相打架。可调参数集中在 `AIP` 对象里。

---

## 2. 信息基础:记牌与推断

**记牌(`makeMemory`)。** 已见 = 自己手牌 + 全部公开出牌 + (庄家还记得底牌)。
用「每种牌全场只有 2 张」倒推出**在外未见牌**的字典 `mem.unseen`。

**钢板(`isBossPlay`)。** 本门在外已无更大。

> **重要原则:`isBossPlay` 只是一个信号,不是硬门槛。** 本门在外已无更大 ≠ 出了必赢
> —— 对手可能在这门断门,用主牌敲脱。

**缺门推断(`makeVoids`)。** 从出牌历史找「谁在哪门没跟牌」。这是 v2 最大的杠杆,
喂给三处:领出时钢板遇对手缺门降权;贴分确定性按「我之后还有没有对手、他缺不缺这门」修正
(即「毙上家放下家」的量化);甩牌安全判断。

**缺门推断(软)—— 从出牌方式反推(`makeReads` / `maxHoldIn` / `makeVoidProb`)。**
硬缺门只在别人「没跟本门」时才看得出来,信息利用得太浅。更强的一层是**硬演绎**:

> 一个理智的人跟牌时,若既没有压制意图、也没有贴分动机,会出本门最便宜的牌。
> 于是「他出了 X」就等于「他手上没有比 X 更便宜的本门牌」。

注意这不是概率推断,是演绎。关键在于**用哪把尺子排序**取决于他当时的处境:

| 他那队当时 | 他的最优出法 | 用的序 | 演绎出的下界 |
|---|---|---|---|
| 不是暂大 | 出最小的废牌 | `cheapKey`(= `globalIdx` + 分牌罚 + 控门溢价) | 他没有比这更便宜的 |
| 暂大 | 贴最大的分 | `dumpKey`(= −分×100 + `globalIdx`) | 他既没有分牌,也没有更小的废牌 |

两条下界是「且」的关系,越多轮次上界收得越紧。再与记牌一交叉(`maxHoldIn`),
就得到他在这门**最多还能有几张**;上界为 0 即断门,否则按他手牌占未见牌的比例衰减:

```
maxHold = |{ 未见牌 c : c 属于该门, cheapKey(c) ≥ 下界₁, dumpKey(c) ≥ 下界₂ }|
pVoid   = maxHold=0 ? 1 : (1 − 他手牌数/未见牌总数)^maxHold
```

实测:东领 A、西(东的队友)跟了张不带分的 Q —— 队友暂大却没贴分,说明他这门既没有分牌、
也没有比 Q 更小的废牌。西这门的持有上界从 **20 张掉到 2 张**,断门概率从 0.07 抬到 **0.76**。

两条必须挂在门口的限制:

- **只在领出单张时采信。** 多张牌的跟法受「有对必对 / 拖拉机必跟」的义务约束,
  他出什么未必是他想出什么,演绎会失真。
- **它假设对手用和 AI 同一套排序。** 遇到刻意骗牌的人(故意出高牌制造断门假象)会被带沟里。

> 补一句实测与直觉的分歧:若我恰好握着 K、K、10、10,直觉会说西的断门概率接近 1。
> 但严格演绎排不掉「另一张 Q」和「另一张 A」—— 西握着 A 时打 Q 依然是正确的贴分顺序
> (A 压不过已经打出的 A,而 Q 更小)。所以上界是 2 而不是 0,0.76 是能站住的那个数。

**存活概率(`pSurvive`)—— v3 新增,替换掉 v2 的魔法系数。**
一手牌摆上台面后,活到墩末不被指定对手压掉的概率:

```
pSurvive(cl, S) = Π_{s∈S} ( 1 − pBeat(s) )

           ┌ pVoidLead(s) · pHas · (台面有分 ? 0.90 : 0.50)   我这手是毙牌
pBeat(s) = │ (台面有分 ? 0.80 : 0.35)·[在外有主]              已知他缺领出门
           └ (1−pVoidLead)·pHas + pVoidLead·pRuff            其余

pHas       = 1 − (1 − higher/total)^(total/denom)    在外压得住我的牌的密度
denom      = 未见牌总数 / 我的手牌数                 他占未见牌的比例(动态,见下)
pVoidLead  = 由上面的软推断给出                      他在**领出那一门**断门的概率
pRuff      = (台面有分 ? 0.80 : 0.35) · [在外还有主牌]
```

`higher` / `total` 由 `unseenBeats` 从 `mem.unseen` 数出来:在外这门还剩几张、
其中几张压得住我(对子/拖拉机要求成对才算)。

**「我这手是毙牌」那一支是 v3.1 补的,它此前是个真 bug。** 旧版把我打出的主牌
直接拿去和「在外所有更大的主牌」比,算出的存活率极低,于是 AI 几乎从不毙牌 ——
漏掉的前提是:**对手要盖过我的毙牌,他也得先在领出的那一门断门**。补上这个前提之后,
毙牌的存活率从 0.03 级别回到 0.7 级别。

**`denom` 不是常数。** 开局未见 3×25+8 = 83 张、某个对手占 25/83 ≈ 1/3.3;
收官未见 3×5+8 = 23 张、他占 5/23 ≈ 1/4.6。所以按实际手牌数动态算,而不是拍一个 3。

于是「队友暂大不是稳大」不再是一句口号,而是一个从记牌算出来的数:
队友领 A 且这门未见 A 已出完 → `certainty ≈ 0.95`,该贴;
队友领 9 且在外一堆大牌 → `certainty ≈ 0.05`,不该贴。

---

## 3. 阶段一:亮主 / 造反

随发牌进度,信息量增大、概率范围缩小。但不同因素的重要性,决定了亮/造反的**时机** ——
每得到一张牌做一次决策:在规则允许的情况下,亮 vs. 不亮。

### 3.1 打分公式

`scoreDeclOption(D, opt)`,D 是决策上下文,opt 是一个候选(某花色单张/一对,或王对→无将):

**v4 起,亮主与造反用的是两个不同的判据。** 首亮问「以这个花色为主,我这手牌够不够好」;
造反问的是另一件事 ——「换成这个主,比**现在这个主**好多少」。v3 两者共用一个绝对门槛,
从头到尾没有算过现状,于是会出现「手握长♠、却因为摸到小王对去反无将」这种没有逻辑的行为。

```
首亮 ⇔ V(opt)                      ≥ θ(v)
造反 ⇔ V(opt) − Q(现在的主色)      ≥ θ(v) + 12
```

`Q(·) = declHandQuality(·)` 是 V 里**只与主色有关**的那一段(Fit + Level)。
抢庄红利、暴露代价、谁已亮主这些本来就是造反的边际成本收益,留在增量侧不减。
造反那额外的一档门槛,对应「打乱一个已经成立的主色本身有摩擦成本」。

```
V(opt) =   Grab              无庄开局的抢庄红利(有主 85 / 无将 58)
       + 60·Fit               手牌与该主色的契合度   ∈[−1,1]
       + 25·Level             级数为 5/10/K 的加成
       + 20·Dealer            庄家归属修正
       + 30·Rebel             造反动力(谁已亮主)
       + 18·Gate              必打关卡的扰乱价值
       − 12·Reveal            暴露手牌信息的代价
       + Single               有对却只亮单张时的净得失(见 3.3)

亮 ⇔ V ≥ θ(v),   θ(v) = ┌ 34 − 22·(v/25)   有庄盘
                        └ 10 − 10·(v/25)   无庄盘
```

v = 已见手牌张数。θ 随 v 递减 —— 早期观望还有信息价值,越到后面「再等等」越不成立
(`declThreshold`)。

### 3.2 各项的来历

**Grab(是否无庄开局)。** 无庄时(全游戏第一局,或完全造反后的无庄状态),亮主 = 坐庄
= 拿底 8 张 + 自己定主色,优势极大,实战中通常都是抢摊、抢反。红利给足 **85**,
但刻意不给到能压过「这门实在太烂」的程度:`Fit = −1` 时净分转负,烂到底的花色仍然不抢。
有庄时,抢摊不是默认策略,`Grab = 0`。

**无将的抢摊红利低一档(58)但不为零。** 无庄盘坐庄的价值有一半来自长将牌压场,
而无将局主牌总共才 12 张,谁也压不了场 —— 所以低一档。但红利仍在,不该被排除;
与之配套,下面那条「基础不情愿」在无庄盘要打三折(见 Fit)。

**Fit(手牌契合度)。** 关键是**别用线性外推**。已见 v 张里有 seen 张会变成主牌,
外推最终 25 张手牌里的期望主牌数用超几何期望(`projectLen`):

```
L̂ = seen + (25 − v) · (total − seen) / (108 − v)
```

`total` = 若以此为主,全场主牌总数:有主局 36 张(该花色 26 + 三门常主 6 + 四王 4),
无将局只有 12 张(8 张级牌 + 4 王)。这条式子天然带收缩:v 小则结果贴近先验均值
(第一张就摸到主 ≠ 25 张全是主),v 大则贴近实际观测。

```
有主:  Fit = clamp((L̂ − 9) / 5, −1, 1)                 人均 36/4 = 9 张
无将:  Fit = 1.2·NTShare − 0.6 + 0.6·SideQ − 0.5·(有庄 ? 1 : 0.3) ± 0.25·(我是闲家)
```

**无将不用长度,用牌力占比(v4 改)。** 无将局主牌**总共只有 12 张**,这时候「期望长度」
不是好尺子 —— 关键是这 12 张里最大的那几张在不在我手上。握大王对 + 两张级牌(4/12)
和握四张级牌无王(4/12)在张数上完全等价,实战里天差地别。所以

```
NTShare = Σ w(c) / (2·1.0 + 2·0.75 + 8·0.4)     w: 大王 1.0、小王 0.75、级牌 0.4
```

「无将对闲家更有利」这条(见下面 Level 那段的论证)v3 只体现在级数是 5/10/K 时的 `+0.35`,
v4 提到 Fit 层作常项:闲家 `+0.25`、庄家方 `−0.15`。

无将是特殊的一种:亮/造反即暴露自己一对王容易被针对诱出,且无将局将牌极少、
可用将牌毙掉的打法非常受限,导致各门内手牌质量影响很大 —— 所以不会无脑亮无将,
公式里那个 **−0.5 的基础不情愿**就是这个意思。只有将牌比对手有数量优势、
或副花色各门牌面够大(较多稳赢的大牌、对子)时才划算,由 `sideQuality` 度量
(A 记 1、K 记 0.6、对子记 0.8,除以门长归一)。

**Level(级数 5/10/K)。** 那 8 张级牌本身就是 40 分(打 5)或 80 分(打 10 / K)。
主将还能获得牌面优势(主 5 > 副 5,一墩中比大小可直接拿分),所以抢亮的赢面变大,
权重甚至高过庄家归属。

> **这里 v3.0 算错过一次,值得记下来。** 当初写的是「无将 ×0.5,因为无将局级牌本来就都是主、
> 边际更小」—— 这个推理没有对照基准:级牌(常主)在**有主局和无将局里同样都是主牌**,
> 跟谁是主花色无关,所以减半没有依据。
>
> 真正的差别在**稀释度**。无将局主牌总共 12 张,级数是分牌时其中 8 张是分,
> 即**主牌里三分之二是分**;有主局那同样 8 张被摊薄在 36 张主牌里,谁主花色长谁通吃。
> 而且无将时常主压死一切副牌,每张级数分牌 ≈ 一墩稳赢外加 5~10 分。
>
> 对**闲家**更是如此,三条叠加:主牌人均 3 张、大致均分,庄家那台长将牌碾压机被拆掉了,
> 分牌回到可争夺状态;常主的稳赢属性让闲家能实打实地把分收进来;庄家没了长主也更难
> 保住最后一墩,抠底反而对闲家开了口子。
>
> 所以系数是 **有主 ×1.0、无将 ×1.35、我方是闲家再 +0.35**,不减反增。

**Dealer(谁是庄家)。**

- 自己坐庄:`+0.5` —— 主要根据自身手牌亮主,亮了直接定主色。
- 队友坐庄:`−0.7`,但 `Fit > 0.6` 时豁免 —— 除非一门有极大的明显优势,
  通常观察等待队友是否会亮主。
- 对手坐庄:`+0.3` —— 主要根据自身手牌优势亮主。

**Rebel(谁已亮主,只影响造反)。**

- 队友已亮:`−1.0` —— 无需打破队友的优势。极端情形(总将牌太少 + 副牌也很差)
  才会考虑,概率很低。
- 对手已亮:`+0.4 + 0.6·(v/25)` —— 造反动力变大,且**对手亮主越晚,造反动力越大**
  (推测对手已确认其手牌优势,值得打乱)。
- 自己已亮:`−0.3` —— 劣势是暴露更多手牌信息给对手,且前面的决策本应已较优。

**Gate(必打关卡)。** 规则开启时轻微影响策略:对手坐庄、且对手当前级数正好卡在关卡上
(无跳级可能),则预期损失受限、扰乱对手可能变成占优策略,`+18`。表现为:发牌前段更可能
提前亮主,后段有造反选项时更可能造反(后者由 θ 的递减自然实现)。

**Reveal(暴露代价)。** 王对造反 `×1.0`(等于把一对王亮给对手针对),一对常主 `×0.4`,
单张 `×0.2`。

### 3.3 亮单 vs. 亮对,以及加固

**有对并非必亮对。** 这是两个不同的选择,都要交给打分(`declOptions` 现在两个都给):

| | 好处 | 代价 |
|---|---|---|
| 亮单 | 不暴露这一对;保留「加固」这条后路 | 别家一对常主就能反掉 |
| 亮对 | 除大小王对外无人能反 | 把一对牌明明白白摊给对手看 |

实战里,对该门作主信心不足却想试一手、或扰乱对手的动机大过手牌本身好坏时,会选亮单。
公式照抄这个直觉:

```
Single = 8·(1 − Fit⁺)                                   「留后路」只在心里没底时才值钱
       − pPairOverride · 45 · (无庄 ? 1.5 : 1) · Fit⁺    被反掉的代价随信心放大
```

`Fit⁺ = max(0, Fit)`。`pPairOverride` 是别家凑出一对常主的粗略概率:某一门的两张级牌
都落进同一家 25 张手里 ≈ (25/108)(24/107) ≈ 0.052,两个对手约 0.104,对「两张都还没见过」
的每一门独立累计。无庄盘乘 1.5,因为被反掉时连庄位和那 8 张底一起丢。

于是自然形成:**信心足 → 亮对锁死;信心不足 → 亮单试探**。

**加固**同样走打分,不再是「一合法就 90% 加固」:用同一把尺子比较此刻的「亮对」与
「维持单张」,只有前者更高才加固 —— 也就是「当初信心不足先试一手,现在牌拿多了,
信心是否已经够把它锁死」。

---

## 4. 阶段二:扣底

庄家在发牌结束、亮主确认后拿到 8 张底牌,手中 33 张全部是合法扣底对象,
需要按扣底适合度评分选择。

### 4.1 打分公式

```
Bury(c) = − Face(c) + Void(c) + PointΔ(c)
```

断门是**集合性质**(埋掉一门的最后一张才算断),所以不是一次性排序,而是**贪心逐张选**:
每选一张就重算剩余各门的断门增益,共 8 轮(`aiDiscard`)。

> **贪心遇上台阶函数,是 v3.0 里一个隐蔽但致命的 bug。** 当初 `Void(c)` 写成
> 「埋掉这张之后该门断门概率的提升」,而断门概率只在**最后一张**被埋时才从 0 跳到 1。
> 于是对一门 4 张的花色,前 3 张的边际增益全是 0 —— 贪心根本看不见这个台阶,
> 永远走不到第 4 张。实测把 `voidValue` 从 10 调到 120,埋底结果**一个字都不变**,
> 这个参数形同虚设。
>
> 修法是把整门的断门价值**摊到需要埋的那几张上**,让梯度露出来:
> 只要预算够(该门张数 ≤ 剩余埋牌数),每张就分到 `voidValue·discount / 门长`。
> 修完之后同一手牌在 `voidValue=68` 时会把 4 张的方块整门埋掉造缺,`voidValue=10` 时则不会。

### 4.2 Face —— 牌面价值(`faceValue`)

留在手里能赢墩拿分的能力,越大越不该埋。出牌阶段牌面价值小的(小单张、小对子)被扣价值大;
反之将牌、连对大对子、副花色单张 A 最不可能被扣。

```
Face(c) = ┌ 400 + 6·ordIdx        主牌(硬性阻力,绝不埋主)
          └ 2.2·ordIdx
            + 18   若成对          别拆对
            + 30   若为 A          控门
            + 14   若为 K,再 +10 若同门还握 A    ——「A + K 一起撒」的本钱
```

### 4.3 Void —— 断门贡献(`voidProb` / `bossChain`)

断门带来策略优势:不再局限于跟进领出花色,而有更多、因此更高概率有更优的选择
(打破「本门最大」预期的毙牌赢墩吃分、或在已知稳赢墩中从其他门抽分牌垫分)。
通过扣底获得天绝、或在牌权易手前出牌达成断门,是庄家特权。

```
budget = 还要埋几张,  len = 该门现有张数,  ch = bossChain(该门)

          ┌ 68 · voidDiscount / len            len ≤ budget      整门埋掉 = 天绝
Void(c) = │ 0.55 · 68 · voidDiscount /(len−ch)  len−ch ≤ budget  埋到只剩稳赢牌
          └ 0                                   否则             这门这轮断不掉
```

`ch = bossChain(...)`:该门从最大往下能连着稳赢的张数。有 A 则 A 稳赢;A 出完 K 变最大,
于是 K 也算进链条,以此类推。这条链决定了「牌权易手之前我还能撒掉几张」。

「牌权易手前」指:庄家默认开局领出,并可由确定(或极高概率)赢下一墩的方式出牌保留领出牌权,
直到稳赢概率不再极近 1。例如手上一门 A、KK 和另两张低价值牌,扣掉那两张后可开局撒 AKK
几乎稳赢且该门即断 —— 那两张的断门贡献折价记 **0.55**(`AIP.voidChainCredit`)。

**voidDiscount —— 无将/少主折扣。** 无将局思路与普通将牌局不同:将牌量少、毙牌选择极为有限,
「能否断门」和「牌面价值」都会变化。毙牌选择少 ⇒ 各门内牌面价值变高(缺少更大的将牌压制,
单张 J 赢墩拿分的概率相对变大)⇒ 扣底价值相对降低;同时断门带来的优势变少。

```
voidDiscount = easeEnds( nTrump / (有主 ? 12 : 5) )

easeEnds(t) = 0.5 − arcsin(1 − 2t) / π
```

**分母是怎么定的。** 它是「再多的主牌也换不来更多毙牌机会」的饱和点,取人均主牌数的
约 1.35 倍:有主局主牌 36 张、人均 9 → 饱和在 **12**;无将局主牌**总共**才 12 张、
人均 3 → 单家饱和在 **5**(12 是全场总数,单家不可能拿到,拿它当分母是错的)。
v3.0 里写的是 11 / 6,凭手感,而且和文档里描述的曲线对不上 —— 现在两边都以这个推导为准。

`easeEnds` 单调递增、g(0)=0、g(0.5)=0.5、g(1)=1,且**中间平缓、两头陡峭** ——
将牌从 0→3 张和从 7→10 张时收益变化剧烈,中间区间(4~6 张)钝感。这正是笔记里
要的那条曲线,而不是常见的 logistic(那条恰好相反:中间陡、两头平)。

### 4.4 PointΔ —— 分牌丢分风险对比

候选为分牌时,评估**扣底**和**持有**两种状态的丢分风险对比。底牌分数翻倍,
因此风险非对等:底牌由赢下最后一墩的队伍以 `分值 × 最后墩出牌数 × 2` 拿下。

```
riskHold = pts · pHold
riskBury = pts · 2 · (1 − pLast) · gateDamp
PointΔ   = riskHold − riskBury             正 = 埋更划算

pHold = clamp(0.50 − 0.035·(门长ₑ − 基准), 0.15, 0.65)
```

**「门长于平均」要按埋完之后的手牌算,不是按现在这 33 张** —— 这一点 v3.0 只是撞对了一半
(贪心循环里门长会随已选的牌缩水,但前几张仍用 33 张时的长度,而基准是写死的 6)。现在:

```
门长ₑ = 该门现有张数 − 这门还会被埋走的那一份(按副牌占比摊)
基准  = (25 − nTrump) / 3        埋完剩 25 张,其中 nTrump 张是主,副牌人均门长
```

- **持有丢分风险低**,通常是一门手牌多于平均 —— 不易在无牌权时被迫出分,
  能自己选择出分时机。故 `pHold` 随门长递减。
- **扣底丢分风险低**,通常是将牌明显多于平均且牌面好、能保留绝对大牌到最后
  (如握大/小王且不易被诱出);或极少情况下一门副花色极长、能在局末掌握牌权独大撒牌。
  这由 `pLast = pWinLastTrick(hand, trump)` 度量:主牌厚度走 `easeEnds`,
  大王对 `+0.22`、小王对 `+0.10`(能压到最后的才敢把分埋进底)。
- **gateDamp —— 必打关卡。** 规则开启且对手赢下这局后会回到必打级(无跳级可能)时,
  即使小概率输掉最后一墩、底牌分数翻倍,实际损失也有上限,故 `gateDamp = 0.6`,
  分牌扣底价值相应提高。

> 反向的一种打法笔记里也提到:若赢下最后一墩的概率本就不高、而手牌整体牌面差导致持有丢分
> 概率高且分牌总额大,则可能转而采取「尽可能保住最后一墩」的策略,让整局变成
> 「平时可以输墩但闲家没什么分可拿、决战最后一墩」。当前实现只体现为 `pLast` 低时
> 分牌倾向留手,尚未做成显式的策略切换 —— 见第 7 节短板。

---

## 5. 阶段三:出牌

### 5.1 从「赢这墩」改成「净赚多少分」

出牌的择优评分不能只以「是否本队赢下这墩」为目的,而要以「为本队保住/抢到足够分数的
可能贡献」为基准。这是 v3 相对 v2 最本质的改动。

**先定性,再算分。** 合法牌先按策略性质归类,各带一个价值系数 `CAT_K`:

| 类别 | 含义 | 候选来源 | 系数 |
|---|---|---|---|
| 毙牌 `ruff` | 断门时用主牌抢墩 | `minWinFollow` | 1.06 |
| 跟大 `over` | 本门压过 | `minWinFollow` | 1.00 |
| 跟分 / 贴分 `dump` | 送分给己方,或躲不掉的分先出 | `buildFollow('dump')` | 1.00 |
| 跟小 `cheap` | 保留实力 | `buildFollow('cheap')` | 1.00 |
| 垫牌 `discard` | 断门时垫废牌,不送分也不拆主 | `buildFollow('discard')` | 1.00 |

**再算分(`scorePlay`)。** 计算的价值是这一墩的期望收益减去机会成本:

```
Score(x) = CAT_K[cat] · [ p·Gain − (1−p)·Loss ] · pw
         + Tempo(x)
         − Future(x)

p     = pTeamWin(x)  本队最终赢下这墩的概率
Gain  = 台面分 + 我出的分 + laterPoints(赢)
Loss  = 台面分 + 我出的分 + laterPoints(输)
pw    = 阶梯比分权重(见下)
Tempo = 牌权价值(见 5.3)
```

候选里还多了一条**毙掉甩牌**:引擎的 `resolveTrick` / `structMatches` 一直允许用同张数、
同结构的将牌整体压制一手甩牌,但 v3.0 的 `minWinFollow` 第一行就是
`if(lead.type==='throw') return null;` —— AI 从不生成这个候选,于是甩牌抢分几乎无人拦得住。
`ruffThrow` 补上了:按甩牌的组件结构(几个对、几个单)去手上的主牌里凑,先满足最难凑的。

### 5.1b 跟牌是**位置**决定的四个不同问题(v4.1)

「将牌墩没人领牌权」这条观察,一开始被误诊成「领出小主是坏棋」。不是的 ——
**小主探路是一种正当的牌权出让策略**:保存实力、收益和概率都不确定时(队友可能没分、
自己赢墩概率不高),把牌权让出去往往是必要的。出牌的目的从来不是「用自己的牌赢下这墩」,
而是「让本队拿到当下与未来的最大收益,以达成局末的分数目标」。

真正的毛病在后三家 —— 它们的处境完全不同,却共用同一套候选:

| 位置 | 他面对的问题 | v3/v4.0 的表达能力 |
|---|---|---|
| **第二家(下家)** | 出大牌抢牌权 —— 最好的结果是末家贴分或第三家被迫送分,**退一步至少换来往后几墩的领出权**;实力不足则跟小;或推断出末手的队友握绝对大牌时直接贴分,让中间两家去比大小 | `pTeamWin` 在这里是**一个常数 0.32**;牌权项 `tempoValue` 是个近似常数的状态估值,权重 0.35 时只改变 2.5% 的跟牌决策 |
| **第三家(对家)** | 接管牌权解队友的围,并且**无论如何不能让末家用一张分牌直接拿墩**:外有 K 就打 A 以上,外只剩 10 就打 J 以上 | `minWinFollow` 只生成**最省的吃法** —— 「压过了但压得不够高」是它根本没法表达的选择 |
| **第四家(上家)** | 信息最全,按台面选 | `isLast` → `p=1`,这一支本来就对 |

> **一条走过的弯路:别用位置规则去砍候选。** `blockWinFollow` 最初写成「只发给队友已经出过牌
> 的那一家」,理由是「第二家烧 A 是越位」。这个理由站不住 —— 第二家先出 A / 级数主
> 完全可以是**拿牌权**,该由打分权衡的事情不该用硬规则替它决定。
> 闸门去掉之后 168 项自测**一条没破**,包括「用最省的牌吃(Q 不动 A)」和
> 「队友坐末手时不烧 A」—— 说明评分本来就会在那些局面下拒绝烧 A,那道闸门纯属多余。

### 5.1c `blockWinFollow` —— 第三家的托底

```
在外这门最大的分牌 topPt = max{ ordIdx(c) : c 未见, c 属于本门, cardPoints(c) > 0 }
若 topPt > 当前最大 → 额外生成一个候选:本门里最便宜的、压得过 topPt 的牌
```

只要不是末手就都生成,两个候选并列,由 `scorePlay` 去权衡
「多花一张大牌 vs. 少送一墩分 vs. 拿到牌权」—— `laterPoints(输)`、`pSurvive`、`Tempo`
本来就能算这笔账,缺的自始至终只是**候选**。

实测 200 局:第三家该盖没盖的比例从 **56.9% 降到 43.9%**,送出去的分从 0.8 降到 0.6 分/局。
配对对照里它值 **+0.2~0.3 分/局** —— 看着小,但要注意 **AI 打 AI 会系统性低估这类错误**:
双方都不掏那张 K,错误互相抵消。对面换成会打的人,代价要高得多。
这也是为什么它即使在噪声范围内也该留下:它修的是**表达能力**,不是权重。

**p 的三种情形(`pTeamWin`)。**

```
我压过当前最大  且我是末手  →  1
我压过当前最大  否则        →  pSurvive(我这手, 后手对手)     ← 见 §2
我没压过  队友暂大          →  certainty = pSurvive(队友那手, 后手对手)
我没压过  对手暂大          →  0.32(队友还没出) / 0.02(队友已出)   ← 见下面那条负结果
```

#### 那个 0.32 —— 一条彻底的负结果,以及它揭出来的东西

诊断的出发点是对的:一个拍死的常数表达不了「我推断队友握着这门的绝对大牌,所以该直接贴分,
让中间两家去比大小」。于是照 `pSurvive` 的口径写了 `pPartnerTakes`(队友在这门大约几张、
其中几张压得住当前最大、加上他可能断门来毙,再乘他身后还有没有对手)。

**实测掉分,而且随采信比例单调:** 0 → **+8.7**、0.2 → +6.8、0.35 → +5.7、0.6 → +6.3 分/局。

先查了行为,再查了校准,两步都有结果。

**行为侧(200 局,「对手暂大 + 队友未出」共约 3900 次):**

| | 采信 0(先验 0.32) | 采信 1(均值 0.41) |
|---|---|---|
| 选择贴分 | 23.6% | **32.1%** |
| 贴分后本队真的赢下 | 37.5% | **46.9%** |
| 贴错送给对手的分 | 26.7 分/局 | **34.2 分/局** |
| 选择去争 | 1055 次,赢下 69.1% | **734 次**,赢下 72.3% |

推断本身是**有信号的** —— 贴分成功率从 37.5% 涨到 46.9%,方向没错。
但它换来的是**放弃了 321 个本来能以 ~70% 赢下的墩**,还多送出去 7.5 分/局。
所以失败不在「推断失误被第三家压制」,也不在「末家有分不掏大牌」,而在**成交量**:
贴分的盈亏是不对称的(贴对了只是省下手里的负债,贴错了连墩带分一起送),
47% 的成功率远不够本。

**校准侧 —— 这一步才问到根子上。** 把模型给出的 p 和实际赢下的频率对一遍:

| 候选类别 | 样本 | 模型给的 p | 实际赢下 | 偏差 |
|---|---|---|---|---|
| 对手暂大(不去争) | 3440 | 0.255 | **0.183** | −0.072 |
| 队友暂大 | 2604 | 0.692 | 0.782 | +0.089 |
| over / ruff | 1814 | 0.727 | 0.696 | −0.031 |

**0.32 作为概率本来就偏高**,不是偏低 —— 我不去争的时候,本队实际只有 18% 能把这墩夺回来。
`pPartnerTakes` 把它推到 0.41,是往错误方向走了两倍。

那把它调低呢?**也掉分:** 0.32 → +8.7、0.22 → +7.0、0.14 → +7.6 分/局。

> **结论:它已经不是一个概率,是一个被调过的权重。**
> `scorePlay` 里的 `gain` / `loss`(`laterPoints`、我出的分、`futureValue` 的分牌负债项)
> 全都是围着这个 0.32 标定出来的。单独把这一个输入改准,等于打破了其余各项吸收掉的偏差 ——
> **在一个各项互相补偿的模型里,提高单个输入的准确度不一定提高整体准确度。**
> 要真正修好,得把跟牌侧的 EV 整体重标定(用上面那张校准表当目标),
> 而不是换掉一个输入。机制与开关(`AIP.pPartnerCalc`)留在代码里,默认 0。

**后手加分(`laterPoints`)。** 后面还没出牌的人会往这墩里加多少分。
v3.0 这里是四个拍死的常数,于是「第三家跟小、末家对手用一张 10 极低代价拿走这墩」
这种亏永远算不出来 —— 常数 3.0 不知道那门在外还剩多少分、末家掏不掏得出一张 10。
改为按记牌估:

```
share = 该门在外剩余分 / 3                    每家在这门大约握着多少分

laterPoints(赢) = min(10, 0.85·share)·[队友未出] + Σ min(3, 0.25·share)
laterPoints(输) = min(3, 0.15·share)·[队友未出] + Σ min(10, 0.85·share·(已知缺门?1.3:1))
```

这门的分被抽干了就趋近 0,这门还压着一堆分就顶到 10 —— 第三家该不该拼,由这个数说话。

**比分权重 `pw` —— 阶梯型动态目标(`roundScore` / `pointWeight`)。**

局末目标**不是「攻守 80 分」这一根线**,而是一排阶梯,且随局面动态改变现实目标。
阶梯的来历就是引擎的结算口径(`scoreRound` / `advanceMatch`):

| 闲家最终分 | 结果 |
|---|---|
| 0 | 庄家升 3 级 |
| < 40 | 庄家升 2 级 |
| < 80 | 庄家升 1 级 |
| ≥ 80 | 闲家上台(升 0 级) |
| ≥ 120 / 160 / 200 | 闲家再多升 1 / 2 / 3 级 |

所以真正的临界点是 **0 / 40 / 80 / 120 / 160 / 200 一整排**。抢到 40 分免被跳 2 级、
或抢 120 分多升一级,都可能在某个局面下取代「够 80」成为现实目标;庄家反之亦然。

于是「此刻一分值多少」= 它把局面推过**某条**临界线的边际概率。对每条线放一个高斯包,
取上包络 —— 离哪条线近就为哪条线拼:

```
pw = 1 + 0.5·( max over L ∈ {0,40,80,120,160,200} of  exp(−(gapL − live/2)² / 2σ²) − 0.6 )

gapL = L − def   (跳过已越过的、和本局无论如何够不着的)
σ    = max(8, 0.30·live)
```

关键是 **`pw` 只乘期望收益、不乘机会成本** —— 于是它调的正是「现在兑现 vs. 留着压制」
这架天平,而不是给所有候选做一次无意义的等比缩放。

> v3.0 只在 80 那一根线上放了一个高斯包,难怪实测收益在噪声内 —— 它把另外五个临界点
> 全忽略了。这是当时「净分涨了、胜局率没涨」的主要原因之一。

### 5.2 Future —— 机会成本(`futureValue`)

这是修「直出王对」类问题的核心:一手牌留到以后打的期望价值,按局面阶段折算。

```
Future(x) = [ Σ 各牌的留手价值 ] · PHASE_K[phase]

phase   = open(手牌≥17) / mid(≥8) / end
PHASE_K = 1.0 / 0.7 / 0.15                收官时留着已无未来可言

王对        7(庄家方再 +5)—— 只是**成对溢价**    小王对 ×0.7
任何主牌     trumpHold(c) = 2.5 + 14·(1 − 在外压得住它的张数 / 在外主牌总数)
副 A         6 + 0.26·(该门在外剩余分)   以后还能捞这门的分
副 K         2 + 0.10·(该门在外剩余分),同门握 A 再 +8
```

**主牌的留手价值必须用相对刻度(v4 改)。** v3 写的是 `2.5 + ordIdx × 0.5`(级牌固定 7、
单王固定 8/6),按牌面序号线性给。问题是 `ordIdx` 是绝对刻度:一对主 A(ordIdx≈24)
算出来 14.5、王对 18 —— 「全场第三大的主」和「全场第十大的主」在这条线性式子里差得很近。
于是 AI 会为了护住王对,先把主 A、主 K 撒光,再在没牌权时被迫打王对。
**「过早打大王对、把自己消耗完」有一半根源在这里。**

正确的刻度是相对刻度:全场最大的那张,不管 ordIdx 多少,留手价值都该顶格;
在外还有一半主牌压得住它,价值就该腰斩。`unseenBeats` 现成可用。

> **配套必须做的一件事:别和 `jokerPairHold` 重复计价。** 改完之后每张王已经从 `trumpHold`
> 拿到 16.5 了,`jokerPairHold` 若还按 18 记「全场最大的压制力」,王对的 Future 会顶到 60+,
> 收官该兑现的时候也兑现不出来。所以 `jokerPairHold` 降到 7,只表示「成对」这件事本身的
> 额外价值(能压对子、能整体吃拖拉机)。

**收官与底牌 —— v4 重做,这是单点最值钱的一条。**

实测(200 局自对弈插桩):**庄家 39% 的局丢掉最后一墩**,平均埋底 11.6 分,
每局白送闲家 **9.3 分**(已含 ×2)。这比 v3.1 相对 v3.0 的全部收益(+8.2)还大。

v3 已经识别出 `PHASE_K.end = 0.15` 那个硬伤(偏偏在底牌翻倍最该被在意的时候,
把留牌价值几乎抹平了),但补丁只是加了一条 flat 项,触发条件是
「end 阶段 **且** 剩 ≤4 张 **且** 这一手是钢板」三条同时成立 —— 等到那时候,
该留的控制牌早就打光了。v4 从三个方向一起改:

**(a) 收官的折扣由底分决定,不再是常数。**

```
PHASE_K.end = clamp(0.15 + 0.045·底分, 0.15, 1.1)
```

底分 0 → 0.15(该兑现就兑现,v3 的行为在这里是对的);
底分 20 → ≈1.05(收官反而比中盘更该留控制牌)。

**(b)「最后一墩争夺」的起算点也随底分提前。**

```
endHorizon = clamp(4 + 底分/6, 4, 9)
```

庄家扣了 20 分进底,从中盘就该开始规划留一手全场最大,而不是等到剩 4 张。

**(c) 护底价值只发给一手(`reserveUnit`)。** 守住最后一墩只需要**一手**牌。
手上三个钢板单元,留一个就够,另外两个该在收官前兑现。v3 把这份价值发给**每一个**
钢板单元,于是它们互相锁死、谁也不肯先出,最后被对手用小牌一张张耗光。
这一项不乘 `PHASE_K` —— 它恰恰只在收官视野内成立。

**挑哪一手,不能只看牌面大小(v4.2 改)。**「钢板」只说明本门在外已无更大,
不说明它保得住 —— 护底靠的是**别人毙不掉**:

| 单元 | 能撑到最后一墩吗 |
|---|---|
| 副牌单张 | **最不可能**。任何一个对手在这门断门,一张主就毙掉了 |
| 副牌对子 | 好一档:毙它需要**一对**主牌,张数结构得对得上 |
| 副牌多张 | 更难毙(要凑出同结构的一手主),但另有前提 —— 必须在**倒数第二墩**就握着牌权,否则领不出来,优势等于零 |
| 主牌大小王 / 主色级数牌 / 主牌对子 | 常规的护底选择 |

```
reserveHold(u) = ┌ 0.95·(1 − 在外压得住它的主牌数 / 在外主牌总数)        主牌
                 └ (1 − pOppVoid·q)² · 0.6                              副牌
q = 单张 0.85 / 对子 0.45 / 三张以上 0.25      毙牌要张数结构对得上,组合越大越难凑
0.6 = 副牌护底的折扣,表示「它还要求先拿到倒数第二墩的牌权」(粗版,见 §7)
```

> v4.0 那版排序键是 `(主?1e6:0) + top×100 + size`,在副牌里 `top` 压过 `size` ——
> 等于**优先挑副牌单张去护底**,恰好挑中了最不可能守住的那一类。
> 改完之后:庄家丢掉最后一墩 35% → **33%**,底分损失 7.5 → **7.1 分/局**,
> 配对对照 +0.5 分/局。

**底分怎么知道。** 庄家直接读 `buriedKnown`;闲家倒推,但**不能均摊**——
均摊等于假设庄家随机埋底。庄家的埋牌行为本身就是信息:

- 他**绝不埋主**(`faceValue` 里 `buryTrumpBlock = 400`)⇒ 在外的主牌要从分母里剔掉,
  否则系统性低估底分;
- 他**倾向不埋分**(`PointΔ` 多数为负)⇒ 分的密度再打一折(`kittyPointBias`)。

```
底分估计 ≈ (未见的副牌分) × 8 / (未见的副牌张数) × 0.8
```

**为什么「一局前期直出一对大王」是坏棋。** 它的当墩 `Gain` 往往只有 0(没人跟分给你),
而 `Future` 高达 18~30 —— 直出即丧失全场最大牌对后续墩、尤其是最后墩(含可能翻倍的底分)
的压制可能,也未必比拆成两张单张出能拿到更多分。所以直出王对、或将牌直出本门最大,
都决不是稳定最优的打法。`PHASE_K` 让这个压制价值随手牌减少而衰减 —— 到收官该兑现就兑现。

### 5.3 Tempo —— 牌权价值(`tempoValue` / `bossUnits`)

低主毙牌真正的决策点不是「这墩值几分」,而是**牌权归我 vs. 牌权给暂大的那家,哪个好**。
牌权归我 = 下一手能立刻兑现手上的优势;牌权给对手 = 他去兑现他的,我还可能被逼贴分。
v3.0 的模型里完全没有这一项 —— 收益只算分。

```
Tempo(x) = [ p·tempoValue(打完 x 之后还剩的手牌) − (1−p)·oppTempo ] · 0.35

unit_val   = min(10, 0.33·该门在外剩余分)·min(1, 0.6·张数) + 1.2·张数
             组合(对子/拖拉机)再 ×(1 + 0.18·(张数−1))
tempoValue = Σᵢ unit_valᵢ · 0.8ⁱ        按 val 降序,i 从 0 起
```

**为什么是贴现和而不是直接相加(v4.2 改)。** 牌权只保证**下一墩**由我领出:
最好的那一手能立刻兑现,第二好的要等我再拿一次牌权,越往后越不确定 ——
「下一轮权重最高,往后递减;后面那些能不能兑现,取决于我还留不留得住牌权」。
这条曲线自己会收敛,不再需要 v3 那个 `tempoCap = 24` 的硬截顶把好手一律压平
(现在 40 只当兜底)。

> 跟牌侧的权重仍是 0.35,不是不想提 —— 是**实测提了就掉分**:
> 0.35 → +8.7、0.7 → +5.6、1.1 → +5.5 分/局。这和领出侧正好相反(领出侧提到 1.0 才有效),
> 因为领出的 `bossBase` 里本来就藏着一截牌权价值可以让出来,跟牌侧没有这样一个常数。
> 跟牌侧要再往前走,得先解决上面 §5.1 那张校准表 —— 各项都是围着现在这套系数标定的。

最后那条**易碎溢价**对应你说的「优势手随墩数推移更可能被拆」:飞机大炮、连对等不起,
带分的更该早点兑现;单张无所谓。

> **一个必须避开的坑:别和 `Future` 重复计价。** 第一版把手上所有钢板单元都算进 tempo,
> 但 `Future` 里已经给同一张副 A 记过「留着还能捞这门的分」了,同一份价值被算了两遍,
> 实测反而掉分。改成**只算打完这一手之后还剩的单元**(把本次要出的牌排除掉)才对:
> `Future` 管「花掉这张的代价」,`Tempo` 管「拿到牌权后能兑现那些**没花掉**的牌」。

### 5.4 排序层的两处修正

这两处不在打分函数里,而在候选构造的排序里 —— 但正是它们造成了笔记里记的两个具体毛病。

**(a) `globalIdx` —— 主牌恒高于任何副牌。**

```
globalIdx(c) = effSuit(c)==='T' ? 100 + ordIdx(c) : ordIdx(c)
```

`ordIdx` 在各门内独立编号,主花色的 3 和副花色的 3 数值相近,排序时被当成等价废牌 ——
于是 AI 会在无分墩最后一手、明明有低价值副花色非分牌时打出非分将牌。同样不是分牌时,
将牌价值大概率不论牌面都高于副花色牌。(唯一例外:能预测对手撒牌时,
「留副花单张防撒牌」可能高于留主牌 —— 尚未实现,见第 7 节。)

**(b) `controlPremium` —— A/K 的控门溢价。**

```
controlPremium(c) = A:30  K:12  Q:4  其余 0
```

旧版 `cheapSort` 只看「牌面大小 + 是不是分牌」,于是手持一门 A、K、10 而该门先由对手领出
一张 A 时,A 因为**不是分牌**反而被判成「最便宜」丢掉。正确打法是出 10(或 5)、留 A + K:
同等分值下,牌面价值(通过赢墩拿下预期收益的概率)更大,获得牌权时可以撒 A + K。
`controlPremium` 同时进 `cheapSort` 与 `discardSort`。

> 注意这不影响「出 A 有风险」的判断:出 A 时若对手为扣底庄家、该门只出过之前那一张 A,
> 则仍有断门可能 = 撒牌风险。但那是**出 A 的时机**问题,不改变**留 K** 的选择。

**(b′) 这个溢价只对副牌成立(v0.5.8)。**

上面这段推导从头到尾说的都是副花色。而 `cheapSort` 是**全门共用**的一把尺子,
主牌也照吃 A/K/Q 溢价 —— 于是主♠打2 时的「便宜序」被彻底打乱:

```
主J 108  <  副常主 112  <  主Q 113  <  小王 114  <  大王 115  <  主K 130  <  主A 141
```

也就是 AI 认为**大小王比主K/主A 更便宜**。实测断门毙牌:手持「主A+大王」出大王、
「主K+小王」出小王、「主Q+副常主」出副常主。这条 bug 有两笔账:
一笔是眼前的「毙牌消耗大牌」,另一笔要到收官才结 —— 中盘每一次断门都在
优先烧掉自己最后的护底牌,于是「底里超过10分却提前没了保底手段」。
配对消融:关掉这条修正,净胜 −0.53 分/局,而**庄家丢最后一墩从 28.8% 涨到 31.3%**。

主牌那一侧需要的不是「A/K/Q 溢价」,而是**梯子顶端的控制溢价**
(大王 14 / 小王 10 / 正常主 6 / 副常主 4)。理由和 §5.2 把 `trumpHold` 从绝对刻度
改成相对刻度完全一样:`globalIdx` 只是序号,大小王只比主K 高 4~5 个序号,
可控制价值差着量级。没有这一截,分牌那个 +8 就足以让 AI 拿小王去换一张主K。

**`cheapKey` 必须同步改**:它同时是**对手模型**的一部分(§2 的 `makeReads` 用它反推
「他手上没有比 X 更便宜的本门牌」)。不改的话 `maxHoldIn` 会一直假设别人先打大王再打主A。

**(c) 「最省」在稳拿这一墩时是反过来的(v0.5.8)。**

`cheapSort` 给分牌一个 +8 的固定回避,这在「还可能被压」时是对的。但末家毙牌是稳拿的,
这时候正确的定义反过来:手上的主分牌该趁机兑现,留在手里迟早要出,
拖到没牌权时更可能白送给对手。旧版实测:手持「主7/主10/主K」出主7、「主10/主J」出主J。

`certain` 一路传到 `cheapSort` / `buildFollow` / `ruffThrow` / `blockWinFollow`,
目前只在末家置真(唯一真正确定的位置)。分值折算成牌序用 `cashPointW = 0.8`
(`trumpHoldTop` 14 摊在约 12 级上 ≈ 1.2 分/级,10 分 ≈ 8 级)。
**这是 v0.5.8 单条收益最大的一处:配对消融 +1.89 分/局(t=4.1)。**

### 5.5 领出(`aiChooseLead` / `scoreLeadPlay`)

领出的候选是手牌分解出的全部组件(`decompose`)加上甩牌候选。评分仍是「这墩能收的分 −
机会成本」,只是赢墩概率对领出而言由「是不是钢板」主导:

```
稳赢的一手:  Score = 56 + 8·张数 + 1.4·pw·leadPointsEV − Future·f
             f    = 1.0   主牌钢板        出掉是真的丧失压制力
                  = 0.35  副牌钢板        它的「未来价值」本来就是「以后领出去收分」,
                                          现在领出就是在兑现它,不该重复扣一遍
             再  − 26      主牌钢板且未到收官(收官只 −4)
                 + 16      庄家方主牌 ≥8 张且不含大怪(吊主清场)
                 − 35      对手在这门缺门(虽是钢板,可能被毙)
```

主牌钢板那 −26 是个**统一的基础折扣**,「这张有多不可替代」交给 `Future` 去表达
(王对 18~30,普通主牌 2.5~8)。早先的写法是按牌型分别硬编码 −22/−26,结果把
「用普通主牌清主」这种好棋也一并否掉了 —— 王对不该领出,一对主 A 该不该领出是另一回事。

### 5.5b 领出侧的牌权与吊主(v4)

实测把「用将牌消耗对手没有实际收益、不考虑牌权」这条观察定位到了两个具体地方 ——
而且**都不在跟牌侧**:将牌墩 80.3% 由领出者赢下,「能吃却跟小」只占 3~15%。

**(a) `scoreLeadPlay` 此前完全没有 Tempo 项。** `tempoValue` / `bossUnits` 只在跟牌里用。
可领出恰恰是牌权最该计价的地方:领出赢下 = 保住牌权继续兑现,领出输掉 = 交给对手。

```
Tempo_lead = ( pWinLead·tempoValue(打完这手后还剩的单元) − (1−pWinLead)·oppTempo ) · w
pWinLead:  主牌钢板 0.97 / 副牌钢板 0.95·(1−pRuff) / 拖拉机 0.60 / 对子 0.45 / 单张 0.25
```

> **权重 0.35 时它形同虚设 —— 实测只改变 2.1% 的领出决策。** 原因是 `tempoValue` 是一个
> **状态**估值,各候选之间只差自己那一个单元,近似是常数偏移,只靠 `pWinLead` 拉开差距。
> 真正的修法不是硬调大权重,而是认清 **`bossBase = 56` 里本来就有一截是「拿住牌权」**:
> 把它降到 40,把那一截交给算出来的 Tempo(权重提到 1.0)。决策改变率随之到 6.1%。
> 这是第四次踩「参数形同虚设」这个坑,教训见 §6。

**(b) 吊主单独成类。** v3 的兜底分支里,主牌只比副牌低一个常数 6,而低主的 `Future`
才 2.5~5 —— 实测 **1529 次将牌领出里 562 次(37%)是低将牌**,理由字符串全是「小牌探路」。
一次这样的领出 = 白烧一张主 + 主动交出牌权,收益为零。

吊主的判据只有一条:**我的主牌能不能耗过两个对手手上的主牌**。

```
对手预估剩余主牌 = 在外主牌总数 × min(1, 两个对手的手牌数 / 未见牌总数)
drawTrump        = clamp( 6 · (我的主牌数 − 对手预估), −30, +30 )
```

两条限制缺一不可:

- **正的奖励只给非王的主牌钢板。** 用王去吊,吊出来的主一样多,却把全场最后的控制权
  提前花掉了(「大怪留后手」)。
- **非钢板的主牌领出只取负半边**,再加一个固定折扣。压不住场的主牌吊不出对手的大主 ——
  他用最小的主跟掉就完了。

> **这里的中间版本掉分,值得记。** 第一版对**所有**主牌领出都加正的 edge 奖励,
> 实测单调掉分:`drawTrumpUnit` 0 → +2.0、4 → 0.0、8 → −2.8。
> 因为那是一个**跨候选的常数偏移**,把低主领出也一起抬了上去 —— 正是要修的那个毛病。
> 符号对了、粒度错了,照样是负收益。
>
> 同时删掉的还有 v3 硬塞在 `aiChooseLead` 里的那颗 `score:46 主力雄厚,吊主清场` 候选:
> 常数 46 无条件压过所有其他候选,而且根本不看对手还剩几张主。

**(c) 送毙 —— 主打队友的断门。** v3 里全部的队内配合只有兜底分支的一句
`+8 队友缺这门,给他机会`,在一堆 20~60 分的候选里等于没有。改成算出来的期望收益:

```
feedRuff = w · pPartnerVoid · (1 − pOppVoid) · ( min(12, 该门在外分/3) + oppTempo )
```

两个概率都来自已有的 `makeVoidProb` / `maxHoldIn` 软推断,不是新造的假设;
还有一条闸门是「在外还得有主牌可毙」。带开关 `AIP.feedRuff`,设 0 即整条关闭,便于消融。

**leadPointsEV —— 领出一手稳赢副牌能收多少分的期望。** 笔记里那条「副 A 预期收益」的
简化落地:

```
rem = 该门在外还剩的分(unseenPointsIn)
leadPointsEV = rem·0.33·(1 − pPartnerVoid)·min(1, 0.8·张数)     队友送的
             + rem·0.33·0.35·min(1, 0.8·张数)                    对手被迫漏的
```

在外这门的分大约 1/3 在队友手里,队友不缺门才送得出来;对手只在被迫时漏。
`pPartnerVoid` 由 `makeVoids` 给出(已知缺门 0.9,否则 0.12)。
v4 补一条:**领出主牌时这一墩能收的分要打 0.45 折**。主牌是所有人都攥着不放的一门 ——
队友不会把主 K 贴上来(他要留着毙牌),对手也只跟最小的主。副牌那套「1/3 在队友手里、
他会送过来」的估计在主牌上明显偏高,不打折的话「领出一张大怪」会因为主门在外分多
而拿到虚高的收益。

其余分支:拖拉机难跟压一手;**分对慎出** —— 无保护的分对领出降权(散牌强对碰大对的概率
惊人);小牌探路,队友缺这门且不带分时加分(给他毙牌的机会)。
**主动甩牌**只在整门 ≥2 组件全为钢板且对手不缺此门时才出,引擎 `checkThrow` 仍最终把关。

### 5.5c 非钢板领出:从「越小越好」改成期望分(v0.5.8)

上一节末尾那句「其余分支」正是问题所在。`scoreLeadCore` 实际上是**两套评分器**:

* **钢板 / 甩牌** → `leadPointsEV` + `pWin` + `Tempo` + `Future`,一套真的期望分模型;
* **其余全部** → 一条手写常数式,唯一的牌力项是**负的一次项**:

```
对子   20 − top×0.5 − pts×4 − …
单张   16 − top×0.8 − pts×6 − …
```

于是手上 ♣J♣J 和 ♣8♣8 时,打分是 **6.7 vs 8.2** —— 差的 1.5 分**全部**来自 `−top×0.5`。
打分器里根本没有「一对J 大概率压得住场、还能把对方的一对10 逼出来」这件事的位置:
`leadPointsEV` 只在钢板分支被调用,`LEAD_PWIN` 又是按**牌型**写死的常数
(pair 一律 0.45,不管是一对3 还是一对A)。

这条常数式还是「按下一个冒出另一个」在领出侧的发源地:`probePenalty`、`tiaoWangValue`、
`feedRuffValue`、`probeSafeBonus` 全变成往同一条式子里加加减减的常数,彼此没有共同量纲,
每加一条都要重新平衡前面几条。

改成和跟牌侧同一个式子:

```
Score = p·Gain − (1−p)·Loss + Tempo − Future
p     = pSurvive(这一手, 两个对手)          ← 跟牌侧一直在用,不是新造的假设
Gain  = 我出的分 + leadPointsEV(队友送的 + 对手漏的)
Loss  = 我出的分 + 赢的那个对手顺手掏出的分 + 队友被迫漏的分
```

`p` 一算出来,几个补丁自动退休:小牌探路的 `p` 天然低 → 自动降权,`probePenalty` 不再需要;
一对J 的 `p` 高于一对8 → 那条反馈自动修好(现在 22.3 vs 20.5),`−top×k` 不再需要;
`LEAD_PWIN` 那张常数表也不再需要,`leadTempo` 改用算出来的 `p` 加权。

顺带划清了一个老结论的适用范围。§5.5b 记的「对所有主牌领出一律加正的 edge 奖励反而掉分
(unit 0→+2.0、4→0.0、8→−2.8)」成立的前提,是在那条常数式里 edge 是一个
**跨候选的常数偏移**,把低主领出也一起抬了上去。换成 EV 口径后它不再是常数 ——
按 `p` 加权,压得住场的主拖拉机拿到大部分奖励,一张孤零零的小主几乎拿不到。
同理 `leadWeakTrump` 那个「白烧一张主」的罚分,本来就该按**输掉的概率**收,即 `×(1−p)`。

保留的是 `tiaoWangValue` 和 `feedRuffValue`:它们表达的是 EV 模型看不见的东西
(逼对手消耗主牌、把牌权交给队友去毙),不是这条式子的替代品。
`AIP.leadEV = 0` 可整块退回旧的三条常数式。

> 附带修好的一个 latent bug:`leadCtx` 里 `remainingOpp` 硬编码成 `[1,3]` ——
> 座位 1 或 3 领出时,「剩余对手」里装的是自己和队友。领出侧原先没有函数读它,
> 所以一直没暴露;`leadWinP` 要用 `pSurvive`,必须先修好。

---

## 6. 验证方法

三层验证,都能 node 无头跑(提取 `<script>` 块 eval 后调 `runTests`):

1. **规则单元测试**(~110 条)—— 牌型判定、跟牌合法性、甩牌罚出、结算与升级、关卡钳制。
2. **场景测试** —— 构造牌面断言 AI 行为。v3 新增的 40 条正是笔记里记的毛病:
   开局握王对不直出 / 收官该兑现;无分墩缺门不拿低主去毙,台面有 20 分则要毙;
   A,K,10 被对手领 A 压死时出 10 留 A+K;队友暂大(非钢板)且后有对手时不贴 10,
   队友钢板且我末手则贴;扣底不埋主、短门整门埋;`projectLen` 小样本收缩;
   `globalIdx` 主牌恒高于副牌;`easeEnds` 两头比中间陡;亮主门槛与造反动力排序;
   带分甩牌会被将牌毙且结构对得上;从出牌方式反推后持有上界大幅收紧;
   有对时亮单/亮对按信心切换;断门价值调低就不再造缺(证明这个参数真的在起作用)。
   引擎自测由 110 项增至 150 项。
3. **对照实验** ——
   - 同副牌,启发式防守 vs 随机防守,断言平均抓分显著更高。
   - **v3 vs v2 直接对打** —— 判断改动到底有没有用的唯一诚实指标。
     单看「vs 随机基线」会被基线的愚蠢放大,数字好看但没有意义。
     做法:同一批牌局跑两遍并**交换阵营**(消除座位与庄家偏差),引擎统一用新的
     (纯规则,两版一致),只把 `aiDiscard` / `aiChooseLead` / `aiChooseFollow` 按座位分派。
     还可以只换其中一个函数,做**单组件消融** —— v3 开发过程中正是靠它抓到
     「领出评分把钢板基础分砍太狠」这个回归的。
   - 300 局全 AI 自对弈冒烟,保证零违规零崩溃。
4. **回归场景库 `test/ai-scenarios.js`(v0.5.8 新增)** —— 每一条实战反馈钉成一条断言,
   可以对**任意版本的 html** 跑:`node test/ai-scenarios.js 80fen-test.html`。

   这一层不是第 2 层的重复。第 2 层的场景是开发时想到的题;这一层的场景**全部来自
   真人反馈**,而且专挑自对弈量不到的两类:

   * **配合类** —— `AIP.overPartner` 的注释早就写到了要害:自对弈两边是同一个 AI,
     它从不像人那样带着意图去调王,所以「队友调了王、我该不该接」这道题
     **根本不会被出出来**。
   * **被支配的选择** —— 比如「毙牌用大王而不用主A」。它不会让任何断言失败,
     在配对自对弈里也常常淹没在噪声里(v0.5.8 里主牌排序那一条只挣 0.53 分/局,
     远小于单条 SE),但它是实战里一眼可见的错。

   还有第三类:**改动本身被自对弈量不到**。v0.5.8 的「不反自己对门」消融值是 0.00 ——
   不是这条没用,是 `simulateRound` 的亮主被简化成一轮制明牌比强,
   根本不会走到「队友已亮、我要不要反」这个分支。

   规矩:**每收到一条反馈,先加一条场景,再动代码。** 这样新补丁踩掉旧修复时会立刻红。
   v0.5.8 的第 ⑧ 条就是这么被发现的 —— v0.5.7 修好了 1c-0 瞄准的那一类,
   同时把「末家、队友已必赢却毙牌」从 0% 推到了 51.5%。

   一个反面教训值得记下来:我一度把「收官 5 张、对手♦K(10分)暂大、我♦断门、
   手上只有一张大王,AI 不毙」也当成 bug —— 其实那是**正解**(毙掉之后手里剩四张输牌,
   最后一墩必丢,底牌那笔 13.7×2 跟着送出去;用 10 分换 27 分是对的)。
   所以场景库里凡是「该做 X」的断言,**都要配一条「不该做 X」的对照组**,
   否则下一轮就会有人把正解当 bug「修」掉。
   - **单组件消融**:只换 `aiDiscard` / `aiChooseLead` / `aiChooseFollow` 其中之一,
     看各自值多少分。v3.0 开发中靠它抓到「领出评分把钢板基础分砍太狠」的回归。

### 实测结果

交换阵营的配对对照:同一批牌局跑两遍并交换阵营(消除座位与庄家偏差),引擎统一
(纯规则两版一致),只把 `aiDiscard` / `aiChooseLead` / `aiChooseFollow` 按座位分派。

| 对照 | 每局净分差 | 胜局占比 | 样本 |
|---|---|---|---|
| **v4 vs v3.1**(含亮主阶段) | **+6.4 分**(SE 1.8, t=3.6) | **53.7%** | 350 副牌 / 700 局 |
| v3.1 vs **v3.0** | **+8.2 分** | **53.0%** | 800 副牌 / 1600 局 |
| v3.1 vs **v2(v0.3.0)** | **+10.8 分** | **52.3%** | 800 副牌 / 1600 局 |
| 启发式防守 vs 随机防守 | 129 vs 73 分 | — | 129 局 |

**v4 的单组件消融**(200 副牌 / 400 局各跑一次):

| 只换这一个 | 每局净分差 | 胜局占比 |
|---|---|---|
| `aiChooseLead`(领出) | **+4.25** | 54.3% |
| `aiDeclDecide`(亮主/造反) | +1.18 | 51.5% |
| `aiChooseFollow`(跟牌) | +0.68 | 50.8% |
| `aiDiscard`(扣底) | 0.00 | 50.0% |

最后一行是台架的**自检**:v4 根本没动 `aiDiscard`,所以它必须精确等于 0。
两版完全相同时净分差 0.00、胜局 50.0%,是跑任何对照前的第一件事。

**先量化再动手。** v4 这一轮的优先级不是拍出来的,是 200 局插桩自对弈量出来的:

| 指标 | v3.1 | v4 |
|---|---|---|
| 庄家丢掉最后一墩 | 39%,底分损失 9.3 分/局 | 35%,**7.5 分/局** |
| 将牌领出次数(200 局) | 1529 | **1235** |
| 开局阶段的主牌消耗占比 | 41% | **6%** |

配对样本方差 ≈ 185,SE ≈ 0.5,净分差的 t ≈ 8 —— 稳稳显著。胜局率 53.0% 对应 SE 1.25%,
约 2.4 个标准差,也站得住。

**最值得说的是胜局率终于跟上来了。** v3.0 净分 +2.9 却胜局率 49.3%(与 50% 无异),
差在两处:一是评分只认 80 那一根线,二是好几个机制其实压根没在起作用。

### 四个「参数形同虚设」的教训

调参之前,先确认这个参数**真的在影响决策**。这一轮抓到三处,每一处都白白躺了一个版本:

1. **`voidValue` 完全没用。** 断门价值只在埋掉一门最后一张时兑现,对贪心是不可见的台阶。
   把它从 10 调到 120,埋底结果一个字不变。摊分之后才真正生效,单这一项贡献约 4 分。
2. **毙牌存活率算错。** `pSurvive` 漏了「对手要盖过我的毙牌,他也得先在领出门断门」,
   算出来 0.03,于是 AI 几乎从不毙牌。
3. **`AIP.dumpOpp` 根本不存在。** 参数化那次编辑丢在一个超时的命令里,代码中还是字面量,
   扫参三档结果**完全一致** —— 这个「完全一致」本身就该是警报。
4. **(v4)领出的 Tempo 项按 0.35 加上去,只改变 2.1% 的领出决策。** 根因不是权重小,
   是 `tempoValue` 是个**状态**估值:各候选之间只差自己那一个单元,近似常数偏移。
   修法也不是硬调大,是认清 `bossBase = 56` 里本来就有一截是「拿住牌权」,
   把那一截让出来交给算出来的 Tempo。
   **验收方式值得固化下来:先量「这个参数改变了百分之几的决策」,再量净分。**
   决策改变率接近 0 时,净分差无论正负都只是噪声。

扫参本身的结论仍是老样子:**目标函数在合理区间里是平的**。`voidValue` 从 62 到 100,
净分差都在 9.8~9.9;`bossBase` 从 44 到 72,在 3.1~4.5。所以取区间低端或中值,
不追那 0.1 分 —— 那是噪声不是信号。唯一有清晰梯度的是 `pSurvive` 里
「某家占未见牌的几分之一」这个假设,而它的正解不是某个常数,是按实际手牌数动态算(见 §2)。

### 5.6 护底的折算比例(v4.3)

v4 把收官口径改成由底分驱动(`PHASE_K.end`、`endHorizon` 都随底分走),
但**折算比例 `endKittyWeight` 本身没跟着抬**,还是 0.55 —— 闸门开了、水管没换。
底 12 分、还剩 3 张时,护底手拿到的加值只有 `12×2×0.5×0.55 ≈ 6.6` 分,
压不过「现在就把这张钢板兑现」的当墩收益,于是庄家仍有四成的局把保底手提前打光。

扫参 1.0→7.0 是先升后平的曲线,4~7 是平台,取 5.0。样本外 300 副配对
**+2.17 分/局(SE 0.60, t=3.6)**;另一组样本外 300 副 +3.02(t=4.5)。

「丢掉最后一墩」要按埋底分分层看,否则会被读成无差别提升:
底 ≥10 分(213/300 局)**51.2% → 20.2%**,底 1–9 分 47.6% → 27.0%,
底 0 分 37.5% → 45.8%(n=24,噪声)。收益全在底里有分那一档 ——
`endKittyWeight` 乘在 `kittyPts` 上,底分为 0 时整项消失,
所以它买到的是**选择性**(底肥留手、底瘦照旧兑现),不是一律死守最后一墩。

这条印证了 §6 那个教训的**反面**:目标函数在合理区间里通常是平的,
但如果一个参数的量纲本身错了(这里是把一笔翻倍的底分折成了半成),
曲线就不是平的,是一条真有梯度的坡。扫参扫出坡来,说明找对了地方。

### 5.7 队友「暂大」时接不接过来(v4.3,实测为负)

整组「吃 / 毙」候选原本被 `if(!partnerWinning)` 挡在门外 —— 队友暂大时一张都不生成,
AI 只能在 贴分 / 跟小 / 垫牌 里挑。对应两类实战亏损:队友调王没人接、
队友副花 K 只是暂大而我断门却不毙,末家一张 A/10 全收走。

候选补上了(`takeOverScoped`),配对对照却各档都掉分:全开 −1.0、限将牌墩 −0.5、
限断门毙分 −0.3,把确定性门槛收紧到 0.5 才回到 −0.05。默认 `takeOverScope: 0` 关掉。

诊断:`certainty` 系统性低估队友(§5.1 校准表 模型 0.692 / 实际 0.782),
门槛写 0.85 等于几乎每墩都开。要吃下这块收益得先重标定 `certainty`,
而不是在一个已知偏低的量上再加一层闸门 —— 和 `pPartnerCalc` 那次是同一个坑。

## 7. 已知短板与实现方案(按痛感排序)

| 短板 | 现状 | 实现方案 |
|---|---|---|
| ~~**无跨墩规划**~~ v0.7.0 做了收官段 | 收官(≤5 张)改为蒙特卡洛推演,目标函数直接是引擎的 `scoreRound` 折成「我方净升几级」——抠底倍数、守住概率、阶梯目标三样都变成真值,不再靠 `kittyMult` / `reserveHold` / `pointWeight` 去近似。**中盘仍是静态估值** | 下一步不是把搜索往中盘推(状态空间不够小,实测 6 张起搜反而更差),而是**拿收官段的真值去校准中盘的启发式**:用推演算出的「守住最后一墩的真实概率」校 `reserveHold`、用推演里最后一墩的真实张数校 `kittyMult`。这是 §8 那条方法论的下半句 |
| **对手建模只到缺门级** | 不推断大牌在谁手上;`pSurvive` 假设各家均分 | 维护每家×每门的**持有区间**而非单个概率:下界由「必须跟牌却没跟」给出,上界由 §2 的演绎给出。已有 `maxHoldIn` 是上界的一半,补下界后可做区间传播 |
| **防撒牌意识缺失** | 「留副花单张防对手撒牌」这条例外没进排序 | 在 `discardSort` 里加一项:若某门**对手可能整门独大**(我方该门只剩单张、且在外大牌集中),这张单张的垫牌代价上调。判据可复用 `bossUnits` 反过来算「对手侧的钢板」 |
| ~~**调王(小主换牌权)没做**~~ v4.7 做了,但**打在了队友无法响应的阶段**(v0.5.8 部分解决) | `tiaoWangValue` 的前提是「对手主门持有**下界** > 0」,而下界来自「手牌数 − 其他门上界之和」,只有别的门都被演绎压低之后才为正 —— 也就是牌快打完的时候。插桩 400 局:调主领出 193 次,**92.7% 在 `end` 阶段**,mid 只有 7.3%。而 `takeOverScoped` 当时对 `end` 阶段一律关闭 | v0.5.8 先把闸门对将牌墩放开(`takeOverEndTrump`),治了「没人接」。**根子还在**:「调主是中盘战术」这条设计意图,和「下界只在收官才有信息量」这条信息约束对不上。要让调主真的回到中盘,得有一个**不依赖下界**的判据 —— 比如从亮主/扣底行为反推对手的主门长度先验,而不是等硬演绎 |
| **「不要主动领主牌」这条先验藏在一个符号错误里** | `drawTrumpValue` 拿我**一家**的主牌数去减**两个对手合计**的,`edge` 因此恒为负(插桩均值:开局 −18.7 / 中盘 −18.8 / 收官 −6.4)。v0.6.0 把比较改对并重标定 `drawTrumpUnit`(3 / 2 / 加低 cap 三档),**每一档都掉分**(−0.74 / −0.89 / −0.83),庄家丢最后一墩一律从 28% 跳到 35%。也就是说这个负偏差实际扮演的是**一条强烈反对主动领主牌的先验**,而在一个底牌会翻倍的搭档游戏里那条先验是对的 —— 攥住主牌(留着毙、留着守最后一墩)比吊主更值钱 | 不要「把比较改对」。把这条先验**显式写出来**:领出主牌的默认代价 = 失去一次毙牌机会 + 削弱护底能力,两者都可以从 `trumpHold` / `reserveHold` 算出来,不需要靠 `edge` 的符号去代表。改完之后 `drawTrumpValue` 才可以回到正确的口径 |
| ~~**领出侧的牌权概率把「队友压过去」当成失败**~~ v0.6.0 已做 | `leadWinP` 用 `pSurvive(这一手, 两个对手)`,只问「我这张活不活得下来」。对**调王**来说队友接过去正是目的,不是失败 —— 于是「小主换牌权」永远算不出正值,只能靠 `tiaoWangValue` 里那个虚的 `drawOut`(逼对手吐主)去凑 | 改成 `pTeam = P(两对手都压不过) + P(有对手压过) × P(队友再压回来)`,用它乘 `leadTempo`、收 `leadWeakTrump×(1−p)`。然后把 `drawOut` 整项删掉,让**吊主(要大主)**和**调王(要队友)**分家。跟牌侧的接牌机器 v0.5.8 已就位,缺的只是领出侧愿不愿意发这张牌 |
| ~~**最后一墩的比较只做到一半**~~ v0.6.0 做了四分之三 | 应该是 `[底分×抠底倍数×P(守住) + 当前分 − 目标分]` vs `[本墩得分×P(拿下) + 当前分 − 目标分]`。现状:`AIP.kittyMult` 是常数 2(实测 12.0% 的局最后一墩不是单张,而且这件事**部分可控** —— 第二末墩领一对就翻两番);`roundScore.def` **不含那笔待翻倍的底分**(实测 56.9% 的局,底分×倍数单独就够跨过 80 线);`pointWeight` 是乘在边际收益上的高斯包络,不是跨线概率;`scorePlay` 里 `ev` 乘了 `pw` 而 `futureValue` 没乘,两只手臂量纲不同。**规模:17.4% 的局,最后一墩的归属直接改变落在哪一级阶梯上** | 四条一起做:`kittyMult` 由 `reserveUnit` 的张数推出;`def` 加上「若守住最后一墩」的底分投影;阶梯改成直接比两条路径的跨线概率;`futureValue` 与 `ev` 用同一个 `pw`。必须连 `bossBase` 一起重标定 —— v0.5.8 的护底封顶就是卡在这里 |
| **`bossBase` 是个比真实收益还大的标定常数** | 钢板领出那一支的基础分 `40 + size×8 = 56` 不是任何真实的分,它比典型的底牌 stake(≈27)还大。后果是**任何按真实量纲标定的项都打不过它** —— v0.5.8 想给护底 flat 项封顶在 stake 上,一封就输给它,净胜掉 0.71 分/局 | 把钢板领出也拆成 `p·Gain − (1−p)·Loss + Tempo − Future`,和 v0.5.8 的非钢板分支(§5.5c)统一。这是一次真正的重标定,得连 `bossSize` / `leadTrumpPenalty` / `endKittyWeight` 一起动,建议单独一版 |
| **`reserveMarginal` 的边际是按单点概率差算的** | `max(0, p(最好) − p(次好))`:手上有大王(0.95)+ 小王对(0.90)时系数只有 0.05,护底几乎归零 —— **牌越好护底越弱**。v0.5.8 试过加常数下限,方向为负(−0.47) | 真正该算的是「两手护底**被同时耗光**」的联合概率,而不是两个边际概率相减。需要 `reserveHold` 支持「给定第一手已出,第二手还能撑到最后的概率」 |
| **收官策略仍不是显式切换** | v4 已把收官口径改为由底分驱动(`PHASE_K.end`、`endHorizon`、`reserveUnit` 都随底分走),但仍是连续调节,没有模式开关 | 剩下的那一半:`pLast < 0.25` 且手上分牌多时,`pw` 的阶梯目标改为只认最后一墩的底分,中盘墩的 `Gain` 权重下调 |
| **无信号牌 / 配合约定** | 队友之间不传递信息 | 代价最大收益最不确定的一条。要先定义一套约定(如首攻花色暗示长短门),再让 `makeReads` 按约定解码。建议放在最后做,且要能一键关闭 |
| **抽样没有用软推断加权(v0.7.0 遗留)** | `sampleWorld` 只用了硬约束(张数、硬缺门、`maxHoldIn` 上界),世界在硬约束内是**均匀**抽的;`pVoidOf` / `fCheap` / `fDump` 这些软概率一点没进去。底牌同样被当成均匀分布,而庄家埋底是有策略的(`aiDiscard` 就是那个策略,`kittyPointsEst` 已经用过这条反推底分——同一个洞察只补了一处) | 按 Kermit 的做法用推断概率偏置世界生成(实测值 22%)。原料我们全有,是文献支持最强、成本最低的一处 |
| **推演里的其他三家用了我的记牌** | `rolloutLead` 判钢板时传的是搜索者的 `mem`,等于在每个抽样世界里把我的信息发给了对手和队友。后果是系统性高估对手应对质量、低估我方收益 | rollout 里每家只用「自己手牌 + 公开出牌史」重建极简 mem,或干脆退化成不判钢板的更笨策略(§10.2:样本量比单样本质量值钱) |
| **队友按期望建模,而不是按 max** | `pSurvive` 对队友和对手用同一套坛子公式。但队友是合作的(会在**他的**信息集下做对我方最好的事),对手是对抗的 —— 全用期望等于把队友当随机变量。这正是「队友暂大算不算稳」那串校准偏差(模型 0.692 / 实际 0.782)的结构来源 | cooperative vs adversarial determinization:队友侧取 max、对手侧取 min。修完之后 `pPartnerPrior` 那两个「已知失准却不能动」的常数有希望自然成立 |
| **局末效用是级数差,不是整场胜率差** | `levelUtility` 算本局净升几级。但真正的目标是打过 A 赢整场,且有必打关卡 —— 关卡上「升 2 级」和「升 1 级」可能等价,「只换庄不升级」也是一种结果 | $U=W(\text{levels}_{after})-W(\text{levels}_{before})$,$W$ 为整场胜率表。可以先用一张粗糙的胜率表打底 |
| **对手不理性时推断会反噬** | §2 的演绎假设对手用同一套排序 | 给每家维护一个**信任度**:若某次演绎被后续出牌证伪(推断他缺门、他却跟出来了),下调该家信任度,`pVoid` 按信任度向先验收缩 |

**共同上游:缺的不是「多算几墩」,是「把一份稀缺资源分配到整局」(v0.7.5 修正)。**

v0.5.8~v0.6.1 三次「结构改对了反而掉分」(护底封顶、`drawTrumpValue` 的符号、`bossBase`)
当时归因为「打分器只算一墩」。v0.7.5 用定点反事实回放把它问得更准了:

在「我断门毙牌、后手还有已知断门的对手」这个决策点上分叉,两个版本 × 1200 局 × 850+ 个点 ——
**不毙、垫一张废牌** 相对现状 **+1.32 ±0.55 / +2.11 ±0.58**。但把它做成规则(甚至硬规则),
聚合**仍然是零**。两个分层说明了为什么:

* 优势随「本局第几次遇到」衰减:**+0.88 / +0.17 / −0.41**
* 优势只在手上有钢板可守时存在:0 手 **−0.05**,1 手 **+1.63**

拆到底:省下这一张,后面靠毙牌多抢 1.4 分(15.9 → 17.3),整局多拿 1.8 分;
最后一墩归属几乎不动(49.1% → 51.4%)。

**所以每一次都亏是真的,但这些亏损不独立 —— 它们提取的是同一份资源:
「主牌作为毙牌机会」。第一次修好买到了它,第二次修好已经没有东西可买。**
一条按墩生效的规则,表达不了「我还需要留几张」。

这解释了为什么:每一条让 AI 更敢/更不敢在中盘动牌的改动,都在「庄家丢最后一墩」上付账
(到 v0.7.5 已是第五次);以及那几个量纲错误的大常数为什么怎么拆都掉分 ——
它们顶替的正是这份资源的**保留价**。

**下一步(方案已定,判据和量具都就位):给毙牌建保留价,而不是给这一墩定价。**

```
reserve(X) = E[未来还剩的毙牌机会里,最好的那一次值多少分]
毙的代价    = max(0, reserve − 本墩台面分)
```

三个输入都能从记牌算,不需要搜索:还剩几次机会(主牌数 × 断门结构 × 剩余墩数)、
每次机会的分布(`unseenPointsIn` 除以该门剩余张数)、取最好的那一次(次序统计量,
和 `oppSpendCeil` 同一个工具)。**收官护底是这组机会里最贵的那一次(底分 ×2),
是特例而不是另一件事** —— 如果这条成立,`endKittyWeight` / `reserveUnit` / `reserveMarginal`
会被同一个式子吸收掉。分三步做,每步用 `test/cf-ruff.js` 验收(见 `NOTES/measurement.md`)。

**在这条建出来之前,不要再去拆那几个常数,也不要再加按墩生效的毙牌规则。**

前两条是杠杆最大的。收官搜索尤其划算:剩 6 张时状态空间已经很小,
而底牌翻倍让最后几墩的单墩价值远高于中盘 —— 花在这里的算力回报率最高。
参考 [ShengJi+ 深度强化学习](https://www2.eecs.berkeley.edu/Pubs/TechRpts/2023/EECS-2023-127.html)
的评测框架、[rbtying/shengji](https://github.com/rbtying/shengji) 的规则细节。

## 8. 经验教训

- **拆掉一个「量纲错误」的常数之前,先问它在代偿什么。** v0.5.8~v0.6.1 连撞三次:
  护底 flat 项封顶(峰值 133 vs 它保护的 27 分)、`drawTrumpValue` 的「一家比两家」
  (`edge` 恒为负 ≈ −18.8)、钢板领出的 `bossBase`(比真实 stake 还大)——
  三处**结构上明显更对的写法,实测全部打不过那个「错」的旧写法**,
  而且连重标定也救不回来。原因每次都一样:那个数值在**代偿模型里缺失的一整块东西**,
  不是在表达它字面上写的那件事。
  · 护底不封顶 = 收官阶段那条硬约束;
  · `edge` 恒为负 = 一条反对主动领主牌的先验(在底牌翻倍的搭档游戏里是对的);
  · `bossBase` = 「保住牌权把整串钢板依次兑现」的复利。
  **判据:如果答不上「它在代偿什么」,就先别拆。**(v0.7.0 补上下半句:
  **用推演给出的真值去回答那个问题** —— 收官段现在有真值了,
  可以拿它当标尺去校准中盘的启发式,而不是继续在自对弈总分上盲扫。) 拆完为了回到原位而重新引入的
  那个常数会告诉你答案(v0.6.1 的 `bossShapeBonus 22` 就是 `bossBase` 换了个名字),
  只是要多花一轮扫参。三处答案指向同一个缺口 —— 见 §7 第一行。
- **一次性偏离 ≠ 策略性偏离(v0.7.5)。** 定点反事实回放测的是「这一次改、其余照旧」;
  把它变成规则是「每次都改」。实测两者能差一个数量级:单次 +1.32~+2.11,做成规则后聚合 ≈ 0。
  拿到一个漂亮的单次收益,先问「做成策略之后,第二次、第三次还值这么多吗」——
  **一定要分层看**(按第几次遇到、按手牌结构),平均值会骗人。
- **诊断之前先数触发次数,并且比对两个分布(v0.7.3)。** 一条规则触发 9 次,
  分数打平不说明任何事。更要紧的是问:**规则触发的场合,和报障发生的场合是同一批吗?**
  「毙得够高」78% 触发在 0 分墩,而缺陷平均发生在 8.6 分的墩上 —— 两个分布压根不重合,
  所以它既修不好问题,又白花牌。
- **动机对不代表方向对(v0.7.1~v0.7.5)。** 整整四个版本在调「毙多高」,
  而正确答案是「别毙」。判据清楚不等于方向清楚 —— 方向要用反事实量,不能靠推。
- **构造式候选生成远比枚举可靠且够用。** 跟牌的合法组合数是指数级的,不能穷举;
  按意图构造少量候选(最省的吃法 / 贴分 / 跟小 / 垫牌)就够了。
- **「理由先行」的设计让教练模式几乎零成本。** 每个候选自带一句中文理由,选中后存入局面。
- **权重手写意味着可解释、可调**,但也意味着每个怪异行为都对应一条可指认的评分规则 ——
  调 AI = 调几个数字,而不是黑盒。v3 把这些数字集中进 `AIP`。
- **量纲统一比系数精确更重要。** v2 里 `30 + 台面分×1.5` 和 `certainty×(20+分×4)`
  根本不可比,导致调一处坏一处;v3 全部折算成「分」之后,取舍才第一次讲得清道理。
- **别用线性外推。** 发牌阶段「已见 3 张里 2 张是黑桃 ⇒ 最终 25 张里 17 张黑桃」
  这种推断会让 AI 在第一张牌就发疯。超几何期望自带收缩,一行式子解决。

## 9. 价值的抽象:`P × S` 的适用边界(2026-08-15 讨论)

这一节记的是一次纯理论讨论,起点是一条很自然的抽象:

> **预期得分手段价值 = 预期得分概率 × 预期得分分值**

结论:骨架对,但有四处会让它**系统性给出错误排序**——而且这四处恰好一一对应了
v0.5.8~v0.6.1 里那三次「结构改对了反而掉分」。它不是一条需要微调的公式,
是一条需要知道自己边界在哪的公式。

### 9.1 一个算例,以及它暴露的东西

题目:第一墩,庄家领出一张副色 A,这一墩期望收多少分?

**赢墩概率。** 单张 A 只会输给毙牌(第二张 A 压不过先出的 A,平局归先手),
所以只需要算天然断门。我持 1 张、底牌 0 张时,这门在外 23 张、未见 75 张:

$$P(\text{某一家断门})=\prod_{i=0}^{24}\frac{52-i}{75-i}\approx 9.1\times10^{-6}$$

两个对手合计 ≈ **0.0018%**。（对自己的门长很敏感:持 6 埋 2 时在外只剩 16 张,
单家断门升到 ≈0.05%。）**结论是"约等于 1",但这条公式只对钢板单张成立** ——
对子要走「同点两张必须落在同一家」的坛子问题(见 §2 的 `pPairAbove`),
非钢板还要算「有更大的牌**且他愿意打出来**」。三种情形不能写成一条。

**队友贴分。** 这门 24 张里有 2 张 10、2 张 K、2 张 5(级数牌不是 5/10/K 时)。
队友持 25/75:

* $P(\text{有 10 或 K})=1-\prod_{i=0}^{3}\frac{50-i}{75-i}=0.810$
* $P(\text{无 10K 但有 5})=0.190\times\big(1-\tfrac{46}{71}\cdot\tfrac{45}{70}\big)=0.190\times0.584=0.111$

跟一张牌只出一张,所以是**取大**而不是求和:
$E=0.810\times10+0.111\times5=\mathbf{8.66}$ 分。

**但 8.66 仍然偏高,因为「有」≠「贴」。** 这就引出了下一节。

### 9.2 意愿系数不是参数,是队友在他自己信息集上跑同一个计算的结果

站在队友的角度,分值 $p$ 的那张牌只有两条路。用净分差记账
(见 §9.3 第①条),设 $w$ 为它以后赢墩的概率、$g$ 为那一墩额外收的分、$\tau$ 为牌权价值:

$$\text{贴} \iff p > \frac{g(2w-1)+w\tau}{2(1-w)}$$

边界正确($w=0$ 一律贴,$w=1$ 一律留)。带入 $g\approx6,\tau\approx6$ 反解临界 $w$:

| 牌 | 分值 | 留下来的临界赢墩率 |
|---|---|---|
| 10 / K | 10 | $w>0.684$ |
| 5 | 5 | $w>0.571$ |

**贴分的判据不是「这张牌有没有用」,是「它赢墩的概率超不超过约三分之二」。**
反直觉的一条:5 的留存门槛**更低**,因为贴掉它只换来 5 分。

于是"意愿"变成一个可算的概率,拆三层:

**A —— 兑现优先级**
$$A(c)=1-P(\text{队友同时握有这门在外所有比 }c\text{ 大的牌})$$
10:上面压着一堆,$A\approx1$。K(我刚打掉一张 A):
$A(K)=1-P(\text{队友持有另一张 A})=1-\tfrac{25}{75}=\tfrac23$。

**这就是那个 0.6 的来历,而关键在于它会跳:**

| 另一张 A 的去向 | $A(K)$ | 队友该怎么做 |
|---|---|---|
| 未知(开局) | 2/3 | 多半贴 |
| 已被打出 | → 0 | 留着,他的 K 是钢板 |
| 已知在对手手里 | → 1 | 一定贴,迟早被吃 |
| **我自己埋进底牌了** | 我知道是 0,**他不知道,仍按 2/3 行动** | ← 信息不对称 |

最后一行是要害:**领出方必须用队友的信息集去估,不能用自己的。**
意愿本质上是一个**二阶信念**量。

**B —— 结构完整性**
$$P(\text{他的 10 成对}\mid\text{他有 10})=\frac{(25/75)(24/74)}{1-(50/75)(49/74)}=\frac{0.108}{0.559}\approx19.4\%$$
拆 10 对几乎无代价,拆拖拉机不然。**但这一层藏着一条战术:$B$ 依赖于我领出的牌型。**
我领单 A 他出一张;我领**一对** A 他必须出一对——那 19.4% 从扣分项变成加分项,
他一次贴 20 分。**领对 A 的收分期望不是领单 A 的两倍,是更高。**

**C —— 安全感**:他对「这墩确实是我方的」的把握。第一墩 ≈0.98,
但**随这门被打过几轮而崩塌**——它是唯一单调衰减的因子,
解释了"同一张 A,第一墩领和第三墩领,收分完全不同"。

合起来第一墩:$10\to0.93$、$K\to0.62$、$5\to0.93$,修正后 ≈ **7.0 分**。
意愿这一层值 1.6 分,比算术误差更大。

**对手那一侧不该用这套机器。** 队友贴分是**决策**,对手漏分是**约束**:
$$P(\text{被迫出分})=P(\text{他这门值得出的合法牌全是分牌})$$
第一墩可以手算出恰好为 0(他持 7~8 张而全门只有 6 张分牌)。
这是纯超几何量,不需要任何系数。**给对手安一个他没有的自由度,是原抽象的结构错误。**

### 9.3 `P × S` 的四个系统性偏差

**① 记账漏了一半:分是守恒的。**
$P\times S$ 只算"我方收到多少"。可 200 分守恒,我不拿的就是对手拿。
后果很具体:**同样值 10 分的「我拿一个 10」和「拦住对手拿一个 10」,
在这个公式里前者 10 分、后者 0 分。** 所有防守性手段——毙掉带分的墩、
盖住末家的分牌——被系统性低估。这正是「断门有分却不毙」「接得不够高」
两条实战反馈在公式层面的根。
(一个例外要注明:**抠底让守恒在结算层被打破**,有效总分可达 $200+kp(m-1)$,
所以份额记账在收官段不成立。)

**② 期望分不是期望效用,而这个游戏的效用是阶梯的。**
结算是 0/40/80/120/160/200 一排台阶。"50% 拿 40 分"和"100% 拿 20 分"
期望相同,但在"还差 35 分上台"和"已经 85 分"两个局面里最优选择相反。
**风险偏好不是外加参数,是由当前分数距哪条线多远内生决定的。**
正确写法是 $V=E[U(\text{局末总分})]$。
> 桥牌里这件事是成熟共识:同一手牌在 matchpoints(近似线性)和 IMPs(非线性压缩)
> 下最优打法不同,**没有人用「期望墩数」做决策,都用期望计分**。

**③ 它是 Bellman 递归的一阶截断,而"牌权折扣"是二阶项的粗糙代理。**
$$V(s)=\max_a\Big[\underbrace{E[\text{本墩净分}]}_{P\times S}+P(\text{我方得权})V(s'_{\text{我领}})+P(\text{对手得权})V(s'_{\text{他领}})\Big]$$
一个乘性折扣系数只能近似第二项的一层。而这个游戏的牌权复利很长
(赢下这墩→兑现第二张钢板→再赢…),护底更是跨十几墩。
**这一条正好解释了那三堵墙**:护底不封顶、`edge` 恒为负、`bossBase` 比真实 stake 大——
三个"量纲错误"的大常数,每一个都是一阶公式用来**顶替缺失递归项**的补丁。
所以三次"改对"都掉分:改对的是字面语义,砍掉的是它代偿的东西。
收官蒙特卡洛之所以立刻 +1.46,就是因为它不做截断。

**④ 最根本的表达力缺陷:$P\times S$ 无法表达「故意输」。**
一手牌可以**输掉这一墩却仍然有正价值**——送毙、调王、逼出对手大牌、造断门、
把牌权让给队友。$P\times S$ 在 $P$ 小的时候一律给低分。
代码里靠 `tiaoWangValue` / `feedRuffValue` / `probeSafeBonus` 这些特例项去补,
**正是因为主式子表达不了**。这也是"补丁堆"在领出侧的成因(§5.5c)。

### 9.4 建议的改写

$$\Delta V(a)=\underbrace{E[U(\text{净分差})]}_{\text{守恒记账 + 阶梯效用}}
+\underbrace{\sum_{s'}P(s'\mid a)\,V(s')}_{\text{递归,不是折扣系数}}
-\underbrace{C_{\text{结构}}(a)}_{\text{拆对/断门/失去毙牌权}}$$

关键是**它是差分**:$V(\text{现在打})-V(\text{留着以后打})$。
领一张副 A 收 7 分听起来不错,但如果这张 A 留到第三墩还能收 6 分,
"现在打"的真实价值只有 1 分——差别全在牌权和这门的分被打光的速度上。
第三项也别省:领 A 会**帮对手造断门**,把「断门→毙牌」的时间表提前,这是负项。

三层里只有第一层能写成闭式,第二层只能截断或抽样,第三层基本是启发式。
**所以任何"漂亮的闭式"必然在某处夹带代偿项——用之前先问它在替谁站岗。**

---

## 10. 外部研究对照(2026-08-15 检索)

### 10.1 直接研究这个游戏的:两篇,都是黑盒,都没有数学分解

* **ShengJi+**(Berkeley EECS-2023-127)——Deep Monte Carlo 显著优于 DQN
  (leveling rate 69.5% vs 30.5%),对随机基线 97.6%,120 万局自对弈。
  状态用 54×2 one-hot 按级数/主色动态编码,出牌按 pattern 逐个选,
  最多 3 个 pattern。**明确建模了抠底翻倍**(注意他们的规则是 $2^{\text{张数}}$,
  我们上海规则是 $2\times\text{张数}$,从三张起分叉)。
  **没有任何显式的手牌/单墩价值公式**,全靠 Q 函数拟合。
* **CS230 Tractor**(Stanford)——DQN,388 维状态,对随机 91%、对手写策略 65%。
  **直接跳过了扣底阶段。**

也就是说:**在"可解释的启发式分解"这条路上,这个游戏没有现成公式可抄。**
但下面每一个子问题,在桥牌/Skat 里都有成熟答案。

### 10.2 PIMC 的理论边界 —— 我们 v0.7.0 的依据,也是它的失效预报

Long, Sturtevant, Buro, Bowling(AAAI 2010)给出了 PIMC 的两类固有错误和三个预测指标:

* **Strategy fusion** —— 在同一信息集的不同世界里用了**不同**策略,
  而现实中必须对所有世界承诺**同一手牌**。
* **Non-locality** —— 对手用私有信息把局面引向 PIMC 预测不到的区域;
  局部最优的着法全局次优。

三个指标:**leaf correlation**(兄弟叶子同收益的概率,高则 PIMC 好)、
**bias**(偏向某方的程度,极端则好)、**disambiguation factor**(信息集收缩速度,高则好)。
实测 Skat/Hearts 的 lc 在 0.8~1.0、df ≈ 0.6 → **落在 PIMC 安全区**,
比均衡策略只差约 0.1 分/局;而 Poker(df=0, lc=0.5, bias=0.5)完全不适用。

**对 80分 的意义:我们的 df 只会更高**——每墩四张牌翻开,而且**必须跟门**,
一跟就暴露门长;lc 也高(同一副牌的多数走法归到同一级台阶)。
**80分 在 PIMC 的安全区里,可能比 Skat 还安全。** 这解释了收官抽样一上来就 +1.46。

**但它同时预报了失效点:strategy fusion 在「必须对所有世界出同一张牌」时最严重
——正是贴不贴分这类决策。** §9.2 那整段讨论,恰好落在 PIMC 理论已知的最大弱点上。

> 一个我们**无意中做对**的细节:经典 PIMC 是"每个世界各求最优着法,然后投票",
> 那才是 strategy fusion 的最坏形式。我们的 `endgameSearch` 是
> **先固定候选、再对所有世界求平均**($\max_c E_w$ 而不是 $E_w\max_c$),
> 已经避开了最坏形式。这一点值得写死在注释里,别在以后"优化"成投票制。

### 10.3 αµ —— 修 strategy fusion 的正解,而且可以增量做

Cazenave & Ventos 的 αµ:不返回单个值,返回**世界向量的 Pareto front**;
Max 节点保留所有非支配向量,Min 节点取对手 Pareto front 的乘积;
`M` 参数只数**我方**的着数。优化后 M=3 / 52 张约 3 秒每着(未优化 55 秒)。
在 3NT 上 αµ(3) 对 αµ(1) 在结果不同的牌里赢 55~62%,7NT 上 67.9%。

**关键:αµ(1) 就等于 PIMC。** 也就是我们现在的做法是 M=1,
往上做是**连续的**,不是推倒重来。这是收官段下一档最明确的路径。
**它不修 non-locality**——论文明说假设对手是完全信息的,不做推断。

### 10.4 Kermit 的推断方法 —— 直接对应我们的 `makeReads`

Buro & Long(IJCAI 2009)面对的问题和我们一模一样:确定性策略会让
$P(\text{move}\mid\text{world})\in\{0,1\}$,推断极脆。两条解法:

* **离线训练取代运行时计算**:在 2200 万局**人类**牌局上直接估 $P(f_i\mid\text{move})$;
* **特征分解**:$P(w\mid\text{move})=P(w)\prod_i P(f_i\mid\text{move})/P(f_i)$,特征独立近似。

推断值 **+217 分 / 1600 局(996 vs 779),约 22%**。
而且他们发现**用人类牌局训练比用双明手求解器训练更好**——
完全信息下的最优防守会低估庄家赢面,因为实战对手看不见那些弱点。

**对我们最直接的一条**:`makeReads` 注释里挂着的那句
「局限:它假设对手用和 AI 同一套最便宜排序」,文献里的标准解法不是去建模具体对手,
**而是换成一个人群的统计**。后续 Rebstock/Solinas/Buro/Sturtevant(CoG 2019)
的 Policy Based Inference 把这条又推进了一步。

### 10.5 桥牌的手牌评估传统 —— 印证 §9.3 的两条

* **Milton Work 点数 / Losing Trick Count / Law of Total Tricks** 是最接近
  "每张牌一个数、相加"的东西,而它们的**已知失败模式**正是分布性牌型(长套、短门)——
  所以才要 LTC 之类的补丁,以及各种"调整"。
  **"每张牌一个数再相加"这条路在桥牌里被试了一百年,结论是它只能当粗筛。**
* **matchpoints vs IMPs**:同一手牌在两种计分下最优打法不同,
  是"阶梯效用"在桥牌里的成熟对应物(见 §9.3 第②条)。


### 10.6 回看整个抽象:还漏了什么

对照文献重新过一遍 §9 的三层式子,下面这些是**讨论到现在仍然没有位置**的东西。
按"能不能马上做"排,不按重要性。

**(a) 推演里的其他三家,用的是我的记牌。** `rolloutLead` 判钢板时传的是搜索者的 `mem` ——
等于在每个抽样世界里,把我的信息发给了对手和队友。这是纯粹的实现缺陷,
理论上讲叫「给了 rollout 策略它不该有的信息集」,后果是**系统性高估对手的应对质量、
低估我方的收益**。修法很便宜:rollout 里每家只用「自己手牌 + 公开出牌史」重建一个极简 mem,
或者干脆退化成不判钢板的更笨策略(§10.2 的教训:样本量比单样本质量值钱)。

**(b) 抽样只用了硬约束,没用软推断。** `sampleWorld` 用了张数、硬缺门、`maxHoldIn` 上界,
但 `pVoidOf` / `fCheap` / `fDump` 这些**软概率**一点没进去,世界仍是在硬约束内均匀抽的。
Kermit 的关键一步正是"用推断出的概率去偏置世界生成",量化过值 **22%**。
这是文献支持最强、我们又已经有全部原料的一处。

**(c) 底牌在抽样里被当成均匀分布。** 可庄家埋底是有策略的——不埋主、倾向不埋分,
`aiDiscard` 就是那个策略。我们在 `kittyPointsEst` 里已经用这条反推过底分,
**但 `sampleWorld` 里没有**:同一个洞察,只补了一处。闲家抽样时应该按埋底策略重加权。

**(d) 目标函数少了一层:局末效用应该是整场胜率的差分。**
`levelUtility` 算的是本局净升几级。但真正的目标是打过 A 赢下整场,而且有**必打关卡**——
在关卡上"升 2 级"和"升 1 级"可能等价(被钳制),"只换庄不升级"也是一种结果。
正确写法是 $U=W(\text{levels}_{\text{after}})-W(\text{levels}_{\text{before}})$,
$W$ 是整场胜率。在 A 级和关卡上,两者差别很大。

**(e) 队友该按 max 建模,对手该按 min 建模,我们全用了期望。**
`pSurvive` 对队友和对手用同一套坛子。但队友是**合作**的
(他会在**他的**信息集下做对我方最好的事),对手是**对抗**的。
文献里这叫 cooperative vs adversarial determinization。全用期望等于把队友当成随机变量,
这正是「队友暂大到底算不算稳」那一串校准偏差(模型 0.692 / 实际 0.782)的结构性来源——
**我们一直在用错误的算子去拟合一个用对算子就自然成立的量。**

**(f) 我方两人的信息不对称,完全没建模。** 庄家知道底牌,队友不知道,
而且庄家**知道**队友不知道。§9.2 那个"我埋掉了另一张 A,队友却仍按 2/3 行动"就是这个结构。
现在 `isDecl` 只用来加了个王对溢价,这块结构一点没用上。

**(g) 只有「从对手出牌反推」,没有「我这样出牌会被反推出什么」。**
`makeReads` 是单向的。信息隐藏、示弱、诱敌,一律无法表达。
这属于 non-locality 的一部分,αµ 也不修(§10.3),是明确的已知缺口。

**(h) 意愿的不动点是双向的,而我们只做了单向。**
§9.2 推的是"我估队友的意愿",但队友同时也在估我的意愿——
我领 A 的价值取决于**队友是否认为我领 A 是在要分**。没有约定就没有信号,
这就是 §7 里「无信号牌 / 配合约定」那条,现在它有了一个理论位置:
**它是意愿不动点的另一半。**

**(i) 有些价值不属于任何单一手牌,属于计划。**
"先用小牌逼出对手的 A,再用 K 收分"是两手联动,第一手单独看是负的。
一阶公式加一层 tempo 贴现都表达不了。αµ 的 `M` 参数数的正是这个
(连续几个**我方**决策),所以 §10.3 那条路径顺带也解决这一条。

**(j) 评估的统计功效。** 我们一直在用 SE ±0.3~1.0 分/局 判断效应量常在 0.5 以下的改动,
这一整轮里至少有三次结论卡在"方向对但不显著"。
标准的方差缩减手段我们一件没做:**用同一批牌局的完全信息解(双明手)当控制变量**,
能把 SE 砍掉一大截;Kermit 用的是 1600 局锦标赛制加交换阵营(交换阵营我们做了)。
**这一条不改棋力,但它决定我们能不能看见棋力的变化** —— 优先级应该比看起来高。

---

## 附:版本沿革

- **v0.7.0(2026-08-15,仅测试版)** 收官蒙特卡洛。剩 ≤5 张时改为抽样推演:
  未见牌按「各家张数相同 + 硬缺门 + `maxHoldIn` 上界」分配,60 个样本,
  便宜的构造式 rollout 推到局末,按 `scoreRound` 折成「我方净升几级」并掺一点分数项
  打破台阶内部的平局。**vs v0.6.1 +1.46 ±0.52;vs v0.5.7 +5.63 ±1.05,
  庄家丢掉最后一墩 41.6% → 24.9%。** 一个教训:第一版在 rollout 里调 `makeMemory`
  判钢板,那一处就占了搜索开销的大半(679 → 270 ms/局)——
  推演策略便宜比精确重要。
- **v0.6.1(2026-08-15,仅测试版)** 把 §7 剩下的两条重构做完:钢板领出统一到期望分口径
  (让 `bossBase` 退休)、「不要主动领主牌」的先验从 `drawTrumpValue` 的符号偏差里
  显式化。**两条都没挣到分,默认全关**(−1.14~−0.04 / −0.19),代码与开关保留。
  产出是两条否定结论和 §8 那条方法论:统一之后要靠一个 22 分的常数才能回到原位,
  说明它扛着「保住牌权把整串钢板依次兑现」的复利 —— 那是 Tempo 只贴现一层的锅。
- **v0.6.0(2026-08-15,仅测试版)** 领出侧的牌权概率从 `pSurvive`(我这张活不活得下来)
  改成 `pTeam`(本队赢不赢得下这墩)—— 这是「调王没人接」的真正病根,而且在领出侧;
  同时关掉 `tiaoWangValue` 的 `drawOut`,让吊主(要大主)与调王(要队友)分家。
  最后一墩补上三个部件:抠底倍数按 `reserveUnit` 张数估(不再是常数 2)、
  阶梯目标含那笔待翻倍的底分、护底项与当墩 ev 用同一个 `pointWeight`。
  重标定 `bossBase` 40 → 34。**vs v0.5.9 +0.79 ±0.88;vs v0.5.7 +5.07 ±0.89,
  庄家丢最后一墩 42.5% → 25.8%。** 两条否定结论:护底封顶即使在 `bossBase` 降低后
  仍然掉分(推翻了 v0.5.8 的推断);`drawTrumpValue` 的符号偏差修了更差,
  因为它实际是一条正确的先验。
- **v0.5.9(2026-08-15,仅测试版)** 调王的诊断与三次失败的尝试。查清了
  `tiaoWangValue` 把「调王」和「吊主」写成了一件事;三条沿旧思路的补法两条被实测否掉。
  行为上 +0.01 ±0.96(等于没动),产出是诊断本身。
- **v0.5.8(2026-08-15,仅测试版)** 九条实战反馈的根因修复。九条 → 4 个根因 + 1 个流程缺口:
  `controlPremium` 误加在主牌上(把大小王当便宜牌)、「最省的吃法」缺少「必赢」分支、
  「断门必毙」只写了一半又漏了 `!isLast`、调主与接调主写在互斥的阶段里、
  非钢板领出没有 EV 模型、抢庄红利没在「已亮的是队友」时抵消、护底项是阶跃函数。
  全部带 `AIP` 开关可单组件消融。**同桌配对对照 3000 副:+4.31 分/局(SE 0.76,t=5.68),
  庄家丢掉最后一墩 40.5% → 28.0%。** 护底四条改动里有两条被实测否掉
  (封顶、边际下限),记在第 7 节 —— 它们卡在 `bossBase` 那个比真实收益还大的标定常数上。
  新增第 4 层验证:回归场景库 `ai-scenarios.js`。
- **v1(M3)** 记牌 + 构造式候选 + 手写权重打分;理由字符串与教练模式。
- **v2(M7,2026-07-18)** 读四篇实战攻略后提炼:缺门推断 `makeVoids`(修「钢板 ≠ 必赢」)、
  埋底 v2(造缺短门/不埋分/不拆对/留 A K/绝不埋主)、垫牌 `discardSort`、主动甩牌、
  吊主清场、分对谨慎、亮主长度门槛。效果:同副牌对照,防守方平均抓分从随机基线 ~80 提到 ~136。
- **v3.0(2026-07-31)** 三阶段全部公式化:亮主/造反打分 `aiDeclDecide`、扣底适合度 `aiDiscard`
  重写、出牌改为「期望分收益 − 机会成本」。用 `pSurvive` 把「队友暂大不是稳大」量化,
  用 `globalIdx`/`controlPremium` 修掉两个排序层的老毛病,用超几何期望修掉亮主的线性外推。
  相对 v2 每局多抓约 3 分,胜局率持平。
- **v4(2026-08-05)** 先量化再动手。三个实测数字定优先级(庄家 39% 丢最后一墩 = 每局 9.3 分、
  37% 的将牌领出是低将牌、将牌墩 80% 由领出者赢下所以毛病在领出侧)。
  修掉 `canOverride` 让人**反自己**的规则漏洞;收官口径全部改由**底分**驱动
  (`PHASE_K.end` / `endHorizon` / 只发给一手的 `reserveUnit` / 闲家按「庄家不埋主、
  倾向不埋分」倒推底分);领出侧第一次有了 Tempo 项,吊主单独成类并删掉硬编码的 46;
  主牌的留手价值从绝对刻度(`ordIdx`)改为相对刻度(在外还有几张压得住);
  造反从绝对门槛改为**增量判据**,无将的 Fit 从期望长度改为 12 张主牌的牌力占比;
  队内配合的第一条「送毙」落地。相对 v3.1 每局多抓 6.4 分、胜局率 53.7%。
- **v3.1(2026-08-01)** 一轮实战复盘带来的修正。三个「参数形同虚设」的 bug
  (`voidValue` 被贪心的台阶函数吃掉、毙牌存活率漏算「对手也得先断门」、甩牌根本不生成
  毙牌候选)、三处口径错误(无将级数加成方向搞反、`easeEnds` 分母凭手感、
  扣底门长没按埋后算)、三个新机制(阶梯型动态目标、从出牌方式反推缺门、牌权价值),
  以及亮单/亮对的权衡与打分驱动的加固。相对 v3.0 每局多抓 8.2 分、胜局率 53.0%。

### 教练模式的一处连带改动

评分量纲一变,教练的「失误」门槛就得跟着改。实测领出的候选分差(中位 27.8)约是
跟牌(中位 7.0)的 4 倍 —— 因为领出的分里含「拿住牌权」的战略值。若共用一个门槛,
教练会对领出唠叨个不停、对跟牌却几乎失明。故领出的差值先除以 `LEAD_GAP_SCALE = 4`
再与门槛比,门槛本身也重标为「这一手大约亏了几分期望分」:高 6 / 中 12 / 低 22。

---

---

*两份已实施的旧方案(最初架构规划、AI v4 提升方案)已移入 `NOTES/archive-plans.md`。*

---

## 附:参考文献

* Long, Sturtevant, Buro, Bowling. *Understanding the Success of Perfect Information Monte Carlo
  Sampling in Game Tree Search.* AAAI 2010. — PIMC 的两类固有错误(strategy fusion / non-locality)
  与三个预测指标(leaf correlation / bias / disambiguation factor)。
  <https://webdocs.cs.ualberta.ca/~nathanst/papers/pimc.pdf>
* Cazenave, Ventos. *The αµ Search Algorithm for the Game of Bridge.* — 用世界向量的 Pareto front
  修 strategy fusion;αµ(1) 即 PIMC,可增量升级。
  <https://www.lamsade.dauphine.fr/~cazenave/papers/AlphaMu.pdf>
* Buro, Long, Furtak, Sturtevant. *Improving State Evaluation, Inference, and Search in
  Trick-Based Card Games.* IJCAI 2009. — Kermit 的特征分解式推断(+22%),
  以及「用人类牌局训练优于用双明手解训练」。
  <https://skatgame.net/mburo/ps/ijcai09-skat.pdf>
* Rebstock, Solinas, Buro, Sturtevant. *Policy Based Inference in Trick-Taking Card Games.*
  IEEE CoG 2019. <https://arxiv.org/abs/1905.10911>
* Shan. *ShengJi+: Playing Tractor with Deep Reinforcement Learning.* UC Berkeley EECS-2023-127.
  — 直接研究本游戏的两篇之一,Deep Monte Carlo,无显式价值分解。
  <https://www2.eecs.berkeley.edu/Pubs/TechRpts/2023/EECS-2023-127.pdf>
* Wang. *Training A Tractor Agent with Deep Reinforcement Learning.* Stanford CS230, 2020.
  — 另一篇,DQN,跳过扣底阶段。
  <http://cs230.stanford.edu/projects_spring_2020/reports/38935383.pdf>
* *Losing-Trick Count* / *Law of Total Tricks* —— 桥牌里"每张牌一个数再相加"这条路的百年结论。
  <https://en.wikipedia.org/wiki/Losing-Trick_Count>
