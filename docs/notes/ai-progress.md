# AI 线:进度、做法与下一步(交接页)

**最后更新 2026-09-26(main 测试版 v0.7.28;本线 v0.7.29 待合,PR #32)。** 每次停下来之前更新这一页。
结论和数字在 [`CHANGELOG`](../CHANGELOG.md);推演在 [`DESIGN`](../DESIGN.md) §7.11~§7.13;
否掉的在 [`negative-results`](negative-results.md);怎么量在 [`measurement`](measurement.md)。这一页只管「接着干活需要知道什么」。

---

## 一、现在在哪

| | 版本 | 说明 |
|---|---|---|
| 正式版 `index.html` | v0.7.19 | 没有产品方同意不动 |
| 测试版 `80fen-test.html`(main) | v0.7.28 | AI = v0.7.26(守底计划、第 3 家接钓主、末家必毙);v0.7.25 / v0.7.27 / v0.7.28 是功能线 |
| 本线待合 | **v0.7.29** | `lastOverW0 0.5`(末家台面 0 分时不太肯拿大主盖),+0.12 ±0.05 分/局;v0.7.28 被功能线占用 |
| 下一步 | — | 第 5 项(守底概率在新打法下重拟合 + 「被钓出来」的风险);第 1 项等牌谱 |

**分工(产品方,2026-09-26):** 这条线主做**智能**;功能与界面由另一条线做(分支 `claude/80fen-test-attachment-sngsnx`)。
两边都改 `80fen-test.html` 和 `docs/CHANGELOG.md`。**开 PR 之前**:`git fetch`,看 main 上最新的测试版是哪一号、
另一条分支有没有已经占用下一个号(v0.7.25 就被它占了,所以这边用了 v0.7.26),先把 main 合进来,再跑 `check-sync --full`。
两边的 `versionTag` 那一行和 CHANGELOG「当前」行必然文本冲突,留版本号大的。

## 二、产品方定下的原则(做 AI 改动时对照)

1. **问题不是孤立的,一起改、一起测。** 单点小修用同一个 AI 当队友和对手去评估,会被其他毛病掩盖(§7.11 撤回的 B1 结论)。
2. **不能按下东边起西边。** 修一处要同时量它会不会在别处送分(守底计划的整局对照就是为这个)。
3. **牌权的价值 = 拿到牌权之后接下来 n 墩的预期收益**,领出与跟牌口径不同;台面 0 分、不是末家时也不是 0。
   `measure-tempo` 量的是**当前打法下**的值 —— 打法改了要重量(打法 → 量价值 → 按价值改打法 → 再量)。
4. **角色很重要:** 庄家自己负责守底;帮家不替庄家守底,中盘用王 / 级牌帮牌权过渡(钓主 → 帮家抢权 → 打庄家断门 → 交回);
   庄家对守住最后一墩没把握时不埋分。
5. **术语统一用「钓」:** 钓主(普通话)/ 钓王(沪语)。
6. 试玩反馈优先用**牌谱**核实:结算框「复制牌谱」→ `node test/replay-record.js <牌谱.json>` 逐手还原。

## 三、做法(每一条都踩过坑)

- **开关约定:** 新行为一律进 `AIP`,`0` = 旧行为。先做**零点对照**:开关关着时 `ai-h2h` 新旧两边行为不同的种子必须是 0。
- **整局对照:** `node test/ai-h2h.js <新> <旧> 350`,`SEED0=` 分批,`OVN=` / `OVO=` 覆盖 AIP。至少 4 批 × 350 种子,
  用 `node test/pool-h2h.js <目录> <前缀>` 逆方差合并。**级/局是主指标**,分/局次之。单批不下结论。
- **长实验用 `test/queue.sh`**:断点续跑、4 路并行;跑之前把要测的 html **复制成快照**,队列读快照,代码可以接着改。
  容器会被回收,结果文件放在 scratchpad 里,跑完的结论要当场写进文档。
- **先诊断再改:** 先写一把尺子把问题量出来(估计 vs 实际、按局面分层),再动打分;改完用同一把尺子复量。
  本轮的尺子:`audit-guard`(守底)、`fit-guard`(守底概率拟合)、`diag-third`(第 3 家逐候选 + 末家真应)、
  `calib-third`(第 3 家按末家情形校准)、`audit-coop`(两家配合链)、`measure-tempo`(牌权值)、`audit-bury`、`audit-third`。
- **「估计准了」≠「选择变了」:** `lastRuffWill` 把校准偏差从 7 降到 2,却只改了约 5% 的种子;
  `trumpTakeAll` 那次是**候选集里压根没有正确的那一手**。估计、候选集、打分三处要分开查。
- 推送前:`node test/check-sync.js --full`(自测 + 文档同步)、浏览器打开无报错(playwright,见 §12)。

## 四、下一步(按优先级)

1. **中盘单领大王**(试玩反馈里一直没降,0.38~0.41 次/局)。
   **2026-09-26 诊断(`test/diag-bigj.js`,1200 副、472 个局面):** 庄家 0.17 / 闲家 0.17 / 帮家 0.06 次/局;理由 303 次「主牌不占优,先不动主」
   (理由与动作矛盾)、169 次「大主牌留作后手」。同一局面把大王换成其他前 8 名候选、同一个 AI 打到底(`ALL=1`):
   **没有哪一类领法明显更好** —— 副牌非钢板单张 +0.9 ±0.9 分、主对 +1.3 ±1.7、副牌钢板单张 −0.3 ±2.0、小主 −0.3 ±1.6,级数都在 ±0.1 内;
   大王是所有候选里最好的占 56%。也就是说在自对弈里它不是坏棋 —— 替代它的多半是「小牌探路」(另一条反馈)。
   人看到的损失可能是人会利用(大王提前没了,后面关键墩控不住),自对弈的续打量不出来。**产品方定(2026-09-26):先给牌谱再定**
   —— 等产品方发来单领大王明显亏的局,用 `replay-record.js` 逐手还原;这期间先做第 3、4 项。理由多是「主牌不占优,先不动主」—— 钢板主单张领出那一支
   (`scoreLeadCore` 的 `allBoss` 分支,`drawTrumpValue` 负半边 + `leadTrumpPenalty`)在别的候选都更差时胜出。
   守底计划只管庄家有底分时;闲家和帮家、底里没分的庄家不受它约束。要查的是:这手领出本身的价值怎么算、和它比较的候选为什么都更差。
2. **第 3 家出小牌被抓**:`lastRuffWill` 校准了估计,选择变化很少。等产品方的牌谱逐手看;自对弈里可用 `diag-third` 的思路
   把副牌墩也拆开(现在只拆了将牌墩)。
3. **帮家压过庄家接权 → 打庄家断门 → 交回**(任务 #14)。**2026-09-26 诊断(`test/diag-coop.js`,1500 副、185 个局面,v0.7.26):**
   这种局面只有 0.12 次/局;庄家暂大时帮家**已经有 62% 会压过庄家接权**(顶大接 29%、便宜接 33%)。同一个 AI 续打:
   顶大接 vs 跟小 +0.5 ±2.0 分、便宜接 −1.8 ±1.3 分 —— 接权本身量不出好处,**不再加定价**;便宜接略负可以留意。
   有问题的话同单领大王,等真实牌谱。原记录:`feedTempo` 只在压过**对手**时计入;钓主墩里庄家那张多半已经最大,
   帮家要抢权得压过**自己队友**,走 `takeOverScoped` 那条窄路。v0.7.26 帮家拿下钓主墩 10% → 20%,链条还没真接上。
4. **`trumpSealW` 从常数变成算出来的**。**2026-09-26:** `diag-third` 在 v0.7.26 下,台面 0 分时末家拿不带分的大主来盖的意愿
   只有估计的一半(顶大接估 79~81% 实 90~91%、封分接估 59% 实 76~77%),台面有分时准。加 `lastOverW0`:末家、将牌墩、
   我这张已压过在外所有主分牌、台面 0 分时,同门盖过来的概率再 ×0.5;校准后各行偏差 ≤8 个百分点(便宜接不打折 —— 他用主分牌拿走是愿意的)。
   整局 +0.12 ±0.05 分/局(t=2.5)、级持平,v0.7.29 默认开。更完整的形式(按他的牌权收益 vs 留手价值算)还没做。原记录:末家肯不肯拿大主来盖 = 他拿下后的牌权收益 vs 这张主留着守末墩的价值。
   试过直接用 `oppSpendCeil`(`sealSpend`)—— 对手被估得太抠,已否。现在是按台面有没有分两档(`trumpSealWPts`)。
5. **守底概率重拟合**:`guardW` 是在 v0.7.23 打法下拟合的(`node test/fit-guard.js 80fen-test.html 1500`),打法改了要重拟合。
   主牌张数那一项多半是相关不是因果;只看钢板主(`guardBossOnly`)又太弱 —— 中间的正确形式是「被钓出来」的风险
   (我的主牌长度 vs 对手的),还没建。
6. **牌权值重量 + 权重**:打法改了之后重跑 `measure-tempo`,再决定 `tempoWeight` / `leadTempoWeight`(×0.3 在旧打法下 +0.015 级/局,
   但会让队友更不在乎牌权,没设默认)。
7. 任务 #10:小主领出的重复收费(`leadWeakTrump` 与 `trumpHold` 两处都扣)。

**等产品方:** 沪语「灭底」与 RULES §5 / §K 的口径冲突,待核对。GUI 目前能导入牌谱复盘,还不能自动重播。
