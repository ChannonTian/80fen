# 被实测否掉的改动

**动机站得住 ≠ 改对了。** 这一页把「试过、测了、否掉」的改动集中起来,
免得下一轮(包括我自己)再走一遍。每条都留了开关,数字是配对对照的净收益(分/局)。

想看当时的完整推演,去 [`ai-journal.md`](ai-journal.md) 的同版本章节。

---

## 一、护底与最后一墩

| 改动 | 开关 | 实测 | 为什么错 |
|---|---|---|---|
| **护底封顶** | `endKittyCap` | 1.0 → **−1.51 ±0.48**(丢最后一墩 40.5%)<br>1.5 → −0.91 ±0.39(36.0%)<br>2.5 → −0.35 ±0.29(32.3%)<br>不封顶 → 基准(28.3%) | 量纲论证是「守最后一墩这个期权不可能比它保护的那笔分更值钱」,听着无懈可击。**测下来不封顶才是对的** —— 那个大数值不是在表达期权价值,是在表达收官阶段的一条硬约束。v0.5.8 猜「等 `bossBase` 重标定后再试」,v0.6.0 重标定后重扫,**推断被推翻** |
| **边际护底下限** | `reserveFloor` | −0.47 | 动机对(`max(0, p最好 − p次好)` 在大王 0.95 + 小王对 0.90 时只有 0.05,「牌越好护底越弱」),补法不对:下限是个常数,真正该算的是**两手护底被同时耗光的联合概率**。这个正确形式至今没建 |
| **埋分的影子成本** | `buryPtShadow` | 0.15 → −0.18 ±0.12<br>0.3 → −0.13 ±0.15<br>0.6 → +0.25 ±0.25 | 推论对:`riskBury` 只算直接损失,没算「为守住这笔底分整局都要留手」。但这个杠杆**推不动** —— shadow 加到 0.6,平均埋底分只从 11.8 掉到 10.7。2×2 交互也显示少埋分只把中盘花主的代价收回三分之一,而丢最后一墩纹丝不动 |

## 二、吊主 / 调主

| 改动 | 开关 | 实测 | 为什么错 |
|---|---|---|---|
| **把 `drawTrumpValue` 的「一家 vs 两家」偏差修对** | `drawTrumpVsOne` | unit 3 → **−0.74 ±0.61**(丢最后一墩 35.3%)<br>unit 2 → −0.89(35.5%)<br>+cap 18 → −0.83(35.3%)<br>不修 → 基准(28.3%) | 诊断没错:`edge` 拿我一家的主牌数去减两个对手合计的,插桩均值 ≈ −18.8,恒为负。但它**实际起的作用是一条强烈反对主动领主牌的先验,而那条先验是对的**。式子的符号约定错了,产生的行为却是对的。数值(`drawTrumpUnit` / `drawTrumpCap`)是围着这个偏差标定出来的,**不能只修一半** |
| **`tiaoEdgeVsOne`** 打开调主闸门 | `tiaoEdgeVsOne` | +0.11 ±0.96 | 调主从 174 次涨到 475 次,但**末家用分牌赢反而从 4.5% 涨到 8.3%**。打得更频繁 ≠ 打得更对 |
| **`tiaoUnlock`** 把收益接到「解锁我方副牌钢板」 | `tiaoUnlock` | 形同虚设 | 插桩均值 0.05,开与不开调主次数 557 vs 559 —— **一次选择都没改变**。按规矩,形同虚设的参数不带着上线 |

## 三、跟牌 / 毙牌

| 改动 | 开关 | 实测 | 为什么错 |
|---|---|---|---|
| **接过队友这一手** | `takeOverScope` | 全开 −1.0 / 限将牌墩 −0.5 / 限断门毙分 −0.3 | 候选生成本身没问题(旧版整组吃/毙候选被 `if(!partnerWinning)` 挡在门外,一张都不生成),但各档都掉分。后来只保留了非收官阶段的一小支 |
| **毙得够高(按持有分布定门槛)** | `ruffGuard`(v0.7.1 版) | 四批合并 **−0.92 ±0.15**,丢最后一墩 +6.4~8.3pp | 门槛问错了问题(「他有没有更大的主」)。断门那家主牌密度高,能过门槛的只剩大小王 —— 178 次触发 85% 打的是王,而 **78% 的墩台面是 0 分**。掏一张王去赢一墩空气。v0.7.3 换成「他肯不肯花」后转正 |
| **毙得够高 + 只在中盘、台面 ≥10 分** | `ruffGuardEnd=0` | +0.04 ±0.06,但 440 次门槛只选中 **9 次** | 「打平」不说明任何事 —— 它挣到的那一点全在收官段,而收官段正是它亏钱的地方。**触发次数要先数** |

## 四、结构性重构:改干净了,但没挣到分

这三条都**默认关**,代码留着。它们的共同点:结构上更对,分数上打平或略负。

| 改动 | 开关 | 实测 |
|---|---|---|
| 钢板领出也走统一期望分口径 | `leadEV2:1` + `bossShapeBonus:22` | −0.04 ±0.69(打平),但「庄家丢最后一墩」好 2 个百分点,结构上钢板支与非钢板支终于是同一个式子 |
| 「不要主动领主牌」这条先验显式化 | `trumpLeadPrior` / `dtNegDamp` | 默认关,行为与上一版一致 |
| 对手已知断门的条件概率 / 持有区间收紧 | `voidCondK` / `holdKRange` | +0.08 ±0.25(量不到)。模型对,没换来分 |

## 五、量不到的,不等于没用

| 改动 | 开关 | 说明 |
|---|---|---|
| 不反自己对门 | `grabPartnerKeep: 0.08` | 配对自对弈 **0.00** —— 自对弈出不了这种题。验收放在场景库 |
| 拖拉机被压概率再收一道 | `tractorTighten: 0.6` | 插桩确认**参数是活的**(0.3/0.6/1.0 直接取值 0.0301/0.0603/0.1005),但拖拉机被压的概率本身太小,翻不动任何一次 argmax。**「参数不在效」和「参数低杠杆」是两个不同的诊断**,别混 |

---

## 从这些失败里提炼出来的四条

1. **动机对 ≠ 方向对。** v0.7.1~v0.7.4 整整四个版本在调「毙多高」,而正确答案是「别毙」。
   先用定点反事实确认方向,再调强度。
2. **量纲错误的大常数,先问它在代偿什么。** 护底不封顶、`bossBase`、`drawTrumpValue` 的符号 ——
   三处答案指向同一个缺口。答不上来就别拆。
3. **触发次数先数。** 一条只触发 9 次的规则,分数打平不说明任何事。
4. **参数形同虚设 vs 参数低杠杆,是两个不同的诊断。** 插桩分清楚再下结论 ——
   这个项目在「以为参数没接上」这件事上错过至少两次。
